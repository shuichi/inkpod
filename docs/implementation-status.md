# Implementation status

This document is a compact snapshot of the current implementation, active
differences, and latest representative verification. Requirement-by-requirement
status and evidence live in [`compatibility.md`](compatibility.md); detailed
ownership and data-flow contracts live in [`architecture.md`](architecture.md).
Completed phase plans and chronological verification history remain available in
Git history and are not duplicated here.

## Current implementation

| Area                   | Current state                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Product surface        | The native Windows application connects the maintained drawing, fill, selection, layer/plane, transform, light-table, sequence, vector, filter/effect, adjustment, import/export, Batch, document-tab, split-editor, multi-window, activation/recovery, locator, reference-viewer, workspace-preset, recent-file, and offline-help operations from production commands through the C ABI or the owning Windows adapter. All 331 production commands are menu-accessible and have configurable displayed shortcuts.                                             |
| Rust Core              | `inkpod-core`, `inkpod-image`, and `inkpod-format` are OS-independent. Immediate document edits use one owning, stale-checked transaction for no-op detection and atomic document/revision/history/cache publication; specialized preview, cancellable, reload, and long-running paths retain their staging ownership and share its final publish boundary. Core-owned identities/revisions and private document/device coordinate, dimension, offset, rectangle, and zoom values use distinct internal types while the public Rust API, C ABI, and file DTOs retain their established fixed-width boundaries. One typed view transform owns flip/zoom/pan conversion; locator, snap, stroke/effect input, transform helpers, and snapshot geometry convert at explicit boundaries. Documents use sparse tiled rasters, copy-on-write snapshots/history, typed layer/plane rules, StateId savepoints, savepoint-aware Undo/Redo, and bounded aspect-preserving per-layer RGBA thumbnails. The three canonical procedures additionally publish a Rust-owned append-only journal with retained branches; their inverse/COW history cache can be released after validation and rebuilt from Genesis plus procedures. Paint and fill retain the selected generic-raster plane ID across preview, commit, and history instead of falling back to another layer's color plane. Crate roots and the vector, Batch codec, transform, and view module indices are small declaration/re-export indices; their implementation is split by responsibility, private-invariant unit tests remain colocated, and public Core workflows compile as a separate multi-module integration target. |
| Procedure history redesign | M0, M1, and M2 are complete. M2 defines journal authority for the canonical procedure slice established by M1; all-production-route journal authority is an explicit M6 completion condition. The approved Genesis/Assets/PROC/META/EditorState/optional-CKPT product contract, no-HIST rule, stable primitive namespace, journal ordering, StateId/revision separation, canonical scalar/digest bytes, exact limits, and BLAKE3 adoption/license conditions remain fixed. While history remains within that slice, `SetMainLineColor`, `ReplacePalette`, and bounded `ApplyRasterStroke` atomically publish their canonical procedure, visible history entry, StateId, document revision, persistent high-watermarks, and closed append-only `Commit`/`HistoryMove`/`BranchCut` journal. Branch cuts retain inactive redo tails, savepoints remain StateIds across Undo/Redo/jump/branching, and a fixed-seed state machine compares live state with cache-free Genesis-plus-journal replay. Runtime inverse/COW data is optional only for a complete journal. An unmigrated document edit sets the explicit incomplete marker; verification and cache release then fail closed while established cached Undo/Redo remains available. No generic snapshot/diff procedure was added. The source-derived inventory classifies 202 public Rust routes/mutations and file-I/O boundaries, all 168 C ABI exports, and all 331 Windows production commands with one category/owner. Remaining primitive migration is M6; ABI remains v2, and `.inkpod` remains exact-current v2 without journal persistence. |
| Windows frontend       | UI/Input, Core engine, and Renderer are separate long-lived threads. `Application` owns startup/message-loop/shutdown, including exact quoted-Unicode bounded multi-path parsing. A current-user/session named-mutex and named-pipe activation service forwards validated paths to the primary process before a secondary creates Common Controls, Core, renderer, or workspace windows; timeout never falls back to an independent editor. The UI thread routes copied value requests to the captured last-focused workspace/active group or an explicit new workspace through the same identity-aware native/raster open path as the File menu. MainWindow owns message normalization and command routing, feature controllers own typed UI state, and pane/dialog modules use typed inputs/results without receiving the complete application state or Core. The canonical workspace is applied by a UI-thread `DockHost` from a bounded HWND-free model: full-width tool options, an 80-DIP left tool strip, central EditorArea, and a 320-DIP right color/layer stack remain the one-action Coloring default, while the four primary panes can use descriptor-constrained dock, tab/split, float, hide, and restore operations. A bounded HKCU version 4 record stores window/split/dock/pane placement and the selected or user-named preset without document paths; it migrates versions 2/3, rejects malformed records to defaults, and clamps windows to current monitor DPI/work areas. Coloring, Line Cleanup, Reference Check, Batch, and Focus presets plus save/save-as/restore/reset are production commands. Resource-titled standard-button edge strips expose secondary-pane AutoHide pointer actions and screen-reader names; the 2026-08-03 native run found that F6/Tab/Shift+Tab cannot reach those buttons. Narrow/compact adaptation does not overwrite saved layout. Its 20 tool buttons use readable single-word Japanese labels rather than one-character abbreviations. Color-consuming commands keep independent current colors, initialize pencil to black and coloring commands to the coloring default, and synchronize the active value into the color editor on command changes. The color pane separately labels and previews the Core-owned main-line color and active coloring drawing color. Its software-antialiased hue ring is cached by size/DPI, the SV triangle by hue, and the opacity band by RGB; drag events use a color-only synchronization path instead of rebuilding all dock models. Resize invalidates both the pane's old layout and the size-dependent owner-drawn picker, so the HSV/opacity surface repaints without a tab switch. Font-metric-aligned HSV/hex/opacity values, RGBA fields, and the shared-command eyedropper remain connected. One composed swatch surface renders the main-line color as a hollow ring behind the alpha-aware filled drawing-color circle without sibling-window overlap. Mouse drag and keyboard updates preserve RGBA8/16 depth and alpha; a colorless eyedropper continues to update the preceding command's color. Committed fills synchronize the Core-selected standard coloring or generic raster plane across the plane palette and status, while zero-pixel no-ops retain the prior selection. Eraser options expose explicit `主線`/`彩色` targets synchronized with the active plane palette row, menu commands, and status. The diameter input uses a smaller DPI-scaled font and text-metric control height centered within the tool-options row. Layer and plane tiles expose selection, visibility/editability, actions, thumbnails/metadata, and drag reordering while sharing command state with menus. New-plane kind and storage format use standard text dropdowns in the owner-centered common modal; OK asks Core to validate the prospective plane and leaves the dialog open with an error for an invalid combination. Command routing and command-state catalogs cover every production command exactly once. |
| GUI modernization      | G0 through G13 are complete. Its hardening slice adds category resource telemetry by Core document, renderer surface route, and workspace pane; a 512 MiB application-wide GPU tile budget with cross-surface inactive-tile LRU; a 64 MiB UI-thread application-wide layer/sequence thumbnail budget with cross-workspace LRU and session/generation/content namespace; per-session CoreHost queue-latency telemetry; tab/group/view/focus keyboard navigation with an unresolved native AutoHide reachability defect; and MSAA/UI Automation bridge regression coverage for dirty tabs, splitters, panes, AutoHide, targets, and job status. Native smoke now fixes and explains the four G13 resource scenarios, including four visible groups across two windows; `CoreHost` consequently accepts the bounded 16 visible Canvas sinks allowed by eight workspaces with two groups each. The Windows x64 five-run benchmark reference/review threshold, bounded five-run lifecycle soak, fault matrix, dependency/third-party notice review, telemetry privacy review, x64 Debug/Release gates, and reproducible accessibility checklist are recorded. The 2026-08-03 native run confirmed part of the UIA/keyboard route, failed AutoHide keyboard reachability, and left high contrast, 200% DPI, complete screen-reader, and IME rows blocked. Those observations remain assigned to their existing `In progress` requirements for separate resolution; the user accepted G13 as complete. About's English title and description in the Japanese UI were confirmed by the user as expected behavior. On 2026-08-04 WER/PDB/disassembly mapped the ARM64 execute access violation to `CoreHost` calling a released split-workspace `CanvasSnapshotSink`; non-final workspace close now synchronously retargets Core notifications to a surviving window and atomically unregisters every affected sink before Canvas destruction, new-workspace primary Canvases register symmetrically, and resource telemetry requires one registered sink per visible group. Native GUI smoke subsequently passed ARM64 Debug/Release and x64 Debug, while x64 Release passed five consecutive runs. G10 passed the x64 Debug/Release native gates and ARM64 Debug/Release fresh configure, strict cross-build, structural checks, package smoke, and MSIX assembly. On 2026-08-02 the user explicitly accepted that evidence as the G10 ARM64 gate on the AMD64 host. G11 adds a UI-thread-owned, value-only `TabDragCoordinator` and Common Controls tab subclass. Issue-time workspace/group/session/view/generation tokens defer all placement changes until release, then route reorder, group/window move, Ctrl-copy/new-view, and tear-out through the same application model operations as menu, context-menu, and keyboard commands. Commit revalidates stale source/target, placement, active stroke, modal/effect state, and capture; cancellation, close, monitor/DPI/theme changes, or transfer failure preserve or restore exact view order, routing, Canvas binding, dirty, and history. Native tab-item drag images inherit the active Windows theme, DPI, and high-contrast rendering. A bounded UI-thread `WorkspaceWindowRegistry` owns up to eight heap-stable workspaces, current/last-focused selection, and persistence slots. Each window owns its top-level/child `HWND`, menu/status state, `DockHost`, `EditorArea`, panes, focus history, and layout, while `ApplicationHost` shares the document registry, one `CoreHost`, one `RendererHost`, clipboard, shortcuts, jobs, and G12 activation service. Window close prompts only for dirty sessions whose final view is removed, preserves surviving windows and shared sessions, and only the final workspace starts application shutdown. Each `DocumentSession` retains one Core binding; same-session views in one or more windows share document/history/dirty/savepoint while presentation state and one Canvas per visible editor group remain separate. G12 adds single-logical-instance multi-path activation, all-candidate session-namespaced recovery metadata and individual restore/discard, plus privacy-sensitive previous-document restoration as a separate explicit default-off setting. |
| Rendering and input    | Canvas uses client device pixels with one typed document-to-device transform and no Core-side OS DPI scaling. Immutable snapshots move through a bounded ownership queue in session/view/Canvas/document-generation/surface-generation and document/view-revision envelopes. New pending frames replace only the older frame for the same surface; stale, hidden/occluded, queue-full, close, and shutdown paths release ownership without rerouting. Stroke/view-gesture payloads are Canvas-owned until the workspace takes a token plus surface generation; custom messages carry no C++ object pointer. The interaction-end notification carries only CanvasId and surface generation values and releases deferred workspace presentation for that exact surface. Every swap chain/target is renderer-thread-owned, while the D3D11/DXGI/Direct2D device graph is shared. RendererHost publishes aggregate and per-surface value-only retained/pending snapshot, GPU tile, swap-chain, surface, queue, stale, resource-limit, and device-reset telemetry; the per-surface copy preserves document/view/Canvas/generation routing without exporting pointers. Active tile admission and cross-surface inactive LRU share a 512 MiB application budget. Host-wide device loss rebuilds GPU state for all surfaces from retained snapshots without changing Core documents. Raster, vector, guide, and grid geometry remains in document coordinates. Raster hit quantization uses the same half-open pixel-cell rule as locator sampling and nearest-neighbor display, including magnified lower/right clicks, final-row/final-column clipping, and flipped views. |
| ABI                    | Public C ABI v2 exposes 168 versioned fixed-layout functions, including a Core-owner-thread read-only logical resource-usage query, read-only prospective plane validation, atomic mixed-format sequence import, caller-owned size-query buffers for bounded layer/sequence thumbnails and locator neighborhoods, and Core-owner-thread subpalette view/apply/sample/snapshot functions. Header/export parity and a direct contract-test reference for every function are enforced. Opaque Core/snapshot/task ownership, copied/buffer-query records, panic containment, validation, and double-release behavior are covered at the boundary. |
| Persistence and codecs | `.inkpod` v2 uses bounded semantic `DOCM`, `LTBL`, `VECT`, and `ADJT` sections; `.inkbatch` is checksummed and versioned. The M2 Core journal is runtime-only: v2 save/reopen neither encodes the branch graph/history nor changes the current-version codec. Atomic save/recovery and PNG/TIFF/TGA/BMP import/export preserve supported depth, alpha, dimensions, and DPI. Each autosave has a versioned bounded sidecar containing session/generation, document UUID, original identity/path, source, and time; startup enumerates every candidate, including metadata-invalid artifacts, without silent deletion. Workspace layout, explicit default-off previous-document paths, and crash recovery remain separate records and contracts. Before an explicit format freeze, application-owned formats accept only their exact current version and every serialized schema change increments the top-level format version. |
| Windows packaging      | CMake is the build entry point and drives the Rust static library, strict MSVC C++20 build, resources, tests, unsigned x64/ARM64 MSIX assembly, and semantic-versioned x64/ARM64 portable ZIP assembly. Rust MSVC uses `+crt-static` and every C/C++ configuration uses `/MT`; final-PE and Rust-library checks reject dynamic CRT linkage before packaging. MSIX retains `.inkpod` registration but no app-local CRT DLLs. Each portable ZIP contains exactly lowercase `inkpod.exe`, `README.txt`, `LICENSE.txt`, and `ThirdPartyNotices.txt`; `windows-arm` denotes ARM64. One bounded build number is shared by the About version line, EXE version resource, and MSIX identity, while ZIP and GitHub tag names use the three-part project version. Unpackaged builds rely on the user's Windows **Open with** choice and continue to use the user profile/HKCU for application state. |
| Reliability            | Public readers reject bounded malformed dimensions, lengths, checksums, IDs, references, and unsupported layouts without partially replacing the current document or output. Deterministic mutation tests cover native, Batch, PNG, TIFF, TGA, and BMP readers; large sparse/COW and bounded dense-image paths plus sparse/dirty snapshots, view cache reuse, Undo/Redo, light table, vector, and Batch Core workflows have fixed-input benchmark coverage. The canonical journal slice has fixed-seed live/full-replay comparison, branch/event ordering, atomic two-event branch-cut publication, no-ID-on-no-op/failure, and cache-release/rebuild coverage; mixed legacy history rejects replay/cache release instead of accepting an incomplete journal. Core resource counters are deterministic and read-only; CoreHost queue wait and renderer queue/cache/device-reset counters make the G13 latency and resource boundary observable without relaxing thread affinity. Queue saturation, close-time input, active stroke, stale snapshot, device loss, save replacement failure, allocation failure, and shutdown-pending work have reproducible fault coverage; failed save preserves dirty, revision, path, and recent files. |

The 2026-08-03 native x64 Release accessibility observation partially exercised
the G13 checklist. UIA names, dirty-tab state, tab/group navigation, pane target,
keyboard menu routing, and Undo restoration behaved as expected. The Reference
Check AutoHide edge buttons were exposed by UIA but were not reachable with
F6/Tab/Shift+Tab. About's English title and description in the Japanese UI were
confirmed by the user as expected behavior on 2026-08-03. High contrast,
200% DPI, and Japanese IME composition were blocked by host-automation limits.
These results supersede the earlier “not run” note. On 2026-08-03 the user
accepted G13 as complete and deferred the unresolved accessibility observations
to their existing requirements; the full record remains in
[`windows-g13-release-checklist.md`](windows-g13-release-checklist.md).

## Active user-facing gaps

The following pre-GUI-modernization feature requirements remain `In progress`.
Their underlying Core/C ABI models and menu commands are retained. The layer and
plane lists share the primary Layer pane managed by DockHost; the remaining work is listed
per requirement. G0's new `WIN-002`, `VIEW-004`, `WORKSPACE-001/002`, and
`SESSION-001` state is summarized above and tracked in the G0 baseline and
`compatibility.md` rather than duplicated in this feature table.

| Requirement          | Available now                                                                                                                                               | Remaining UI                                                        |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| `DOC-002`, `DOC-003` | Typed stable-ID layer/plane topology and transactional commands; the dockable Layer pane exposes separate vertically ordered layer and plane tiles, exact stable-ID selection, visibility/editability, opacity/type metadata, shared actions, split resizing, and direct drag reordering | Multi-target editing presentation |
| `LT-001`             | A modeless target-aware Light Table palette exposes set/item administration, transform/color/mode/opacity, reference alignment, Canvas movement, sampling, reload, dirty-safe edit-image swap, frame navigation, and a dedicated read-only reference viewer | Previous/next-N bulk registration and automatic opacity-step controls |
| `SEQ-001`            | Natural-order import/export, automatic matching-sibling file preview, a modeless target-aware thumbnail list, direct cell selection, and first/previous/next/last/goto commands | Automatic-save-on-switch and endpoint-loop preference controls |

See [`compatibility.md`](compatibility.md) for the authoritative status, evidence,
and exact known difference of every requirement.

## Known differences

- M2 is complete for the canonical procedure slice established by M1; the
  procedure-authoritative successor as a whole remains partially implemented.
  The Core journal, branch graph, StateId savepoint, and cache-free replay apply
  while history consists of main-line color, palette replacement, and one
  raster-stroke procedure. Any remaining document route marks the
  journal incomplete and retains the runtime inverse/COW cache; it is not
  converted into a synthetic snapshot or diff procedure. ABI is v2, and
  `.inkpod` v2 still saves materialized state and resets runtime history on open.
  The implemented slice is not evidence for the later all-route, ABI, format,
  or cross-architecture cutovers.
- The M1-only schema-2 document digest temporarily repeats the current
  standalone Document ID in the hierarchy Cell slot because Genesis does not
  own a distinct Cell object yet. No successor file accepts or emits that alias;
  M4 must allocate the distinct Cell ID and advance the digest/replay/format
  versions before the successor-container cutover.
- The M0 inventory records current Windows composition debts for hidden-layer
  and all-guide deletion loops, new-cell post-conversion, vectorize destination
  creation, converted-paste plane creation, and chart palette/editor updates.
  Their all-or-nothing Rust ownership remains M6 work; no new wrapper was added
  in M0.
- Normal user-initiated `.inkpod` save/open still waits synchronously for the
  Core-engine work item. Autosave and image-processing tasks use asynchronous
  queue/task paths.
- All sessions still share one `CoreHost` execution lane. Per-session latency,
  the native delay test, and the fixed G13 scenarios show that accepted input is
  retained without partial commit; current measurements do not justify relaxing
  the single-writer owner-thread model.
- The current Windows UI resource set is Japanese-only. English display-language
  fallback therefore remains Japanese and is recorded in the G13 release
  checklist instead of being represented as completed English localization.
- Vector layers preserve their mutual z-order, but the current snapshot renderer
  draws vector content after precomposited raster tiles instead of supporting
  arbitrary raster/vector interleaving.
- Batch output currently writes native `.inkpod` cells.
- Native v2 blobs are separated but not compressed; compression remains an
  optional future container feature.
- Administrator MSIX install/installed-ABI/uninstall smoke remains optional
  validation rather than an implementation gap.
- A future sandboxed frontend still needs byte/stream I/O and platform file-
  authority adapters; no Windows dependency is allowed in Rust domain crates.

## Recent verification

These rows intentionally keep only the latest representative results for each
currently relevant platform boundary.

| Date       | Platform                                             | Verification                                                                                                                                | Result                                                                                                                                                                                                                                        |
| ---------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-04 | Windows 11 ARM64 host, native ARM64 and emulated x64 Debug, Visual Studio 2026 / MSVC 19.51, Rust 1.97.1 | M2 completion: procedure-journal/Undo/Redo semantics for the three M1 canonical procedures; branch/cursor/savepoint ordering, branch-cut atomicity, retained redo tails, cache-free Genesis replay, fixed-seed state machine, exact bounds/failure atomicity, Rust fmt, zero-warning all-target/all-feature clippy, all-feature workspace tests, Core quick benchmark, strict rustdoc; fresh ARM64/x64 Debug configure/build, ABI smoke, static-CRT, MSIX, portable-ZIP, GUI smoke, and CTest | The source-derived guard classified 202 Rust routes/mutations and file-I/O boundaries, 168 C ABI exports, and 331 Windows commands with zero missing or duplicate owners. Rust passed 238 tests plus 1 doctest and all 7 quick benchmark scenarios. Both MSVC targets built all 111 targets; ABI smoke, static-CRT verification, unsigned MSIX assembly, and ARM64 portable-ZIP smoke passed. ARM64 CTest passed 26/27 and x64 CTest passed 26/28. Both GUI-smoke failures returned 53 after reading the prior Canvas fit bounds before the renderer consumed the visibility/resize transition; pre-M2 ARM64/x64 Release binaries reproduce the same failure, so this is not an M2 regression. The x64 portable-ZIP test additionally reached its fixed 60-second CTest timeout, while the identical verification script completed successfully without that timeout in about 57 seconds. M2 journal authority is complete for the three canonical procedures; all-production-route authority is M6 and journal persistence/reopen is M8. The production C ABI and exact-current `.inkpod` codec remain v2. |
| 2026-08-04 | Windows 11 ARM64 host, native ARM64 and emulated x64 Debug, Visual Studio 2026 / MSVC 19.51, Rust 1.97.1 | M1 canonical primitive kernel for main-line color, palette replacement, and bounded raster stroke; semantic state/payload digest goldens and fresh-Core replay; Rust fmt, zero-warning all-target/all-feature clippy, all-feature workspace tests, Core quick benchmark, strict rustdoc; fresh ARM64/x64 Debug configure/build, ABI smoke, GUI smoke, static-CRT, MSIX, portable-ZIP, and CTest | The source-derived guard classified 198 Rust routes/mutations and file-I/O boundaries, 168 C ABI exports, and 331 Windows commands with zero missing or duplicate owners. Rust passed 220 tests plus 1 doctest and all 7 quick benchmark scenarios. Both MSVC targets built all 111 targets, including the pinned BLAKE3 closure (and x64-only `cpufeatures`) plus the Rust static library; static-CRT, unsigned MSIX, ABI smoke, and GUI smoke passed. Native ARM64 CTest passed 27/27. Emulated x64 CTest passed 27/28; its portable-ZIP smoke reached the fixed 60-second timeout after the standalone ABI smoke itself took 55.45 seconds, while the identical portable verification script completed successfully without that timeout in about 66 seconds. The production C ABI and exact-current `.inkpod` codec remain v2; no journal or v4 reader/writer was introduced. |
| 2026-08-04 | Windows 11 ARM64, Visual Studio 2026 / MSVC 19.51, Rust 1.97.1 | GUI lifetime repair and portable-distribution regression: WER/PDB/disassembly diagnosis; synchronous workspace Canvas-sink detach; deterministic sink-count telemetry; Rust format, zero-warning clippy, and all-feature workspace tests; x64/ARM64 Debug/Release strict build, non-GUI CTest, GUI smoke, static-CRT, unsigned MSIX, and exact four-file portable ZIP gates | The execute access violation was a released split-workspace `CanvasSnapshotSink` retained by `CoreHost`; workspace close now retargets Core notifications to a surviving `HWND` and atomically unregisters all sinks under the publication mutex before destroying child windows; newly created primary Canvases register symmetrically. GUI smoke passed x64 Debug, ARM64 Debug, and ARM64 Release once each and x64 Release five consecutive times. Non-GUI CTest passed x64 Debug/Release 27/27 and ARM64 Debug/Release 26/26; all four strict builds and distribution gates passed. Rust workspace tests passed with 180 tests plus 1 doctest. |
| 2026-08-03 | Windows 11 x64, Visual Studio 2026 / MSVC 19.51, Rust 1.97.1 | Embedded offline Help: EXE `RCDATA`, F1 production command/state/shortcut ownership, build-versioned LocalAppData preparation, exact-byte cache reuse, stale-copy atomic replacement, strict x64 Debug build, MSIX assembly, and full non-install CTest | MSVC `/W4 /WX /permissive-` build and unsigned MSIX assembly passed. CTest passed 27/27, including the new embedded-manual contract test, command route/state/shortcut structural checks, ABI smoke, and GUI smoke. Automated smoke suppresses only the external default-browser launch. |
| 2026-08-03 | Windows 11 x64, Visual Studio 2026 / MSVC 19.51, Rust 1.97.1 | G13 resource/latency/navigation/fault completion: Rust format, zero-warning clippy, and all-feature workspace tests; x64 Debug/Release configure, strict build, unsigned MSIX assembly, full non-install CTest, and the local monotonic build-number wrapper with deterministic duplicate-PATH tool resolution and shared build 46; ARM64 Debug/Release strict cross-build, structural/package CTest, and MSIX; Core/per-surface/per-pane resource telemetry, CoreHost cross-session latency and 16-visible-sink bound, GPU and thumbnail budget/LRU, four fixed resource scenarios, keyboard/accessibility bridge, save-failure smoke, and the reproducible accessibility checklist; warmed five-run Rust benchmark baseline and five-run owner/Core/renderer/GUI bounded soak | 180 Rust tests plus 1 doctest passed. x64 Debug and Release CTest passed 26/26, including the fixed 1/1/1, 1/4/2, 2/4/4, and large-image/reference/history/device-loss scenarios. ARM64 Debug/Release each passed 14/25 host-independent tests after a successful strict cross-build and MSIX assembly; all 11 native ARM64 executables were not runnable on AMD64 and are not reported as passed. On 2026-08-03 the user explicitly waived those 11 native executables for G13. The outstanding high-contrast, 200%-DPI, screen-reader, and IME observations remain recorded under their existing `In progress` requirements; on 2026-08-03 the user accepted G13 as complete. |
| 2026-08-02 | Windows 11 x64, Visual Studio 2026 / MSVC 19.51, Rust 1.97.1 | G12 completion: current-user/session named-mutex and versioned bounded named-pipe activation; multi-path and explicit-new-window routing; versioned recovery metadata, all-candidate/individual recovery, and default-off previous-document setting; malformed/security/timeout/queue-failure and multi-dirty-session integration coverage; Rust format, zero-warning clippy, all-feature workspace tests; x64 Debug/Release configure, strict build, unsigned MSIX assembly, and full non-install CTest | 178 Rust tests plus 1 doctest passed. x64 Debug and Release CTest each passed 25/25, including activation/recovery structural and native tests, ABI/renderer tests, and GUI smoke. Secondary activation exits before Core/window/renderer creation, and autosave retains dirty/savepoint while all session candidates remain enumerable. |
| 2026-08-02 | Windows 11 x64, Visual Studio 2026 / MSVC 19.51, Rust 1.97.1 | G10 completion: Rust format, zero-warning clippy, all-feature workspace tests; x64 Debug/Release configure, strict build, unsigned MSIX assembly, and full non-install CTest; ARM64 Debug/Release fresh configure, strict cross-build, structural checks, package smoke, and ARM64 MSIX assembly with `Hostx64\arm64\cl.exe` and Rust `aarch64-pc-windows-msvc`. The user accepted the cross-build evidence as the ARM64 G10 gate on this AMD64 host. | 178 Rust tests plus 1 doctest passed. x64 Debug and Release CTest each passed 21/21. ARM64 Debug and Release structural/package CTest each passed 12/21; the nine ARM64 native executables, including ABI, renderer, and multi-window smoke, were not runnable on the AMD64 host and remain recorded as not run rather than passed. |
| 2026-07-30 | macOS ARM64, Rust 1.95.0                          | Rust format, zero-warning clippy, all-feature workspace and Core tests; fixed-seed Core state machine; Core quick benchmark; strict Core rustdoc; CMake configure/build and platform-independent CTest; whitespace check | 176 workspace tests plus doc-tests and all 110 Core tests passed. All seven quick benchmark scenarios retained the M2 semantic checksums and counters after the internal ID/revision, document/device coordinate, and responsibility-module migrations. Generic CMake configure/build passed and CTest passed 5/5; Windows x64 Debug/Release builds, CTest, native DPI/Canvas smoke, and MSVC ABI layout remain for Windows CI. |
| 2026-08-02 | Windows 11 ARM64, MSVC 19.51, Rust 1.97.1            | G8 locator progress: fresh ARM64 Debug MSVC configure, strict build, unsigned MSIX assembly, and full non-install CTest. The prior G7 ARM64 Release gate remains representative for Release. | ARM64 Debug build and MSIX assembly passed with no new warnings; CTest passed 20/20 with the new locator ABI, pane target, command catalog, native locator, retained multi-session/split-editor, RendererHost/CoreHost isolation, and package payload coverage. The administrator-only MSIX install/uninstall smoke was not run. |

## Maintenance rule

Update this file only when the current implementation summary, an active gap, or
a representative latest verification result changes. Replace superseded entries
instead of appending a chronological log. Update `compatibility.md` when a
requirement's status, evidence, or known difference changes.
