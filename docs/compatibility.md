# Compatibility status

This document tracks only the current status, representative evidence, and
known difference for every requirement ID defined in
[`../SPEC.md`](../SPEC.md). Detailed behavior belongs to `SPEC.md`, ownership
and data flow to [`architecture.md`](architecture.md), exact format bytes to
[`file-format.md`](file-format.md), and current platform verification to
[`implementation-status.md`](implementation-status.md).

Status is one of `Not started`, `In progress`, `Experimental`, `Verified`, or
`Blocked`. A user-facing requirement is `Verified` only when its production UI
route and observable result are tested; infrastructure requirements may be
verified at their natural build, ABI, Core, format, or renderer boundary.

## Requirement matrix

| Requirement | Status | Representative evidence | Known difference / next work |
| --- | --- | --- | --- |
| `ARCH-001`, `ARCH-002` | Verified | CMake/Cargo source tracking, crate-boundary guards, OS-dependency scans, public integration tests, strict Rust/Windows builds | Rust domain crates contain no Windows API dependency |
| `ABI-001`, `ABI-002` | Verified | Header/export/catalog parity for ABI v14; fixed-width language-neutral `InkpodHistoryEntryKind` with exhaustive Windows `UiStringId` mapping; C11/C++20 layout, NULL/short/stale/wrong-thread/ownership tests; point-in-time history builder progress/cancel and caller-owned sequence-step/annotation/shooting-frame/vanishing-point records, snapshot-owned annotation/angled-frame/point/radial spans, explicit instruction-export Rust buffer, and existing Cut/transform/selection/task/runtime-object contracts; snapshot queue, device-loss and GUI smoke | ABI v1-v13 callers must rebuild against the current header |
| `IO-001` | Verified | V26/runtime epoch-23 stream/layout/digest/current-only tests; exact v25 rejection; document metadata/archive schema 5 vanishing-point, shooting-frame and annotation records; `EditVanishingPoints/canonical-v2`; current-only Cut descriptor schema-2/epoch-23 codec; existing journal/checkpoint/recovery/compaction contracts; save/reopen/replay and malformed-data tests | Non-v26 files are rejected without migration; compression code 0 only |
| `IO-002` | Verified | PNG/TIFF/TGA/BMP 8/16-bit, alpha/DPI round-trips; caller/file lifetime, sequence, export, ABI and Windows smoke | Flat common-raster export remains distinct from native save |
| `WIN-001` | In progress | Startup/shutdown; versioned HKCU System/Japanese/English preference with malformed-record System fallback; first-preferred `ja` system resolution with non-Japanese/detection-failure English fallback; checked Settings > Language commands; one generated, typed Japanese/English catalog for product-owned text with exact format-signature parity; exact equality of all 642 ja-JP/en-US resource identifiers, including all 384 command IDs, with explicit exact-language loading and no Win32 fallback lookup; typed Tool labels shared by owner draw, tooltip, MSAA and UIA, with both full product smoke modes retrieving every real ToolTip value and comparing it to the same label ID; Layer/Plane kind/format/visibility/editability and accessible rows resolved before drawing; unchanged 42-by-42-DIP Plane badges use pre-resolved typed compact labels of at most two lines without ellipsis, while adjacent detail and accessibility retain the full kind; Layer/Plane owner-draw state cells measured from all four localized state captions in the active 9-point font plus DPI padding, with shared draw/hit-test geometry and dedicated Japanese/English 96/120/144/192-DPI contracts; typed Color/Palette/Chart tabs; DockHost-owned inspector-header font matched at runtime to the pane-tab 9-point Segoe UI ClearType font and recreated for workspace DPI changes; idempotent Locator presentation suppressing unchanged target-label `WM_SETTEXT`, check-state updates, and neighborhood invalidation, while shorter replacement text receives one background-erasing synchronous redraw, with an actual-window unchanged-message/shortening-restoration regression and a static repaint-contract gate; language-neutral typed history kinds mapped exhaustively to catalog IDs; strict source gate rejecting Japanese bypasses, direct wide-string assignment to product dialog/effect presentation fields, language-branch/raw document captions, history string keys, restoration of the single-line ellipsized Plane badge, and restoration of the fixed owner-draw action width; no hook, partial, or hybrid translation; language-neutral generated document data and explicit user-text composition preserving edit/document-tab/target-document/Light-Table-set text, Japanese file names, and full paths verbatim; measured localized button widths with responsive action-row wrapping; Japanese/English 96/120/144/192-DPI-equivalent font layout tests plus full GUI checks for actual visible button bounds; full English and Japanese GUI smoke covering Tool and Layer/Plane owner draw, ToolTip/MSAA/UI Automation names, Saved/Modified, Locator/Sequence/Light-Table follow/pin/follow, zoom, Core/renderer/device-loss and workspace lifecycle; Help/About; owner-centered work-area-clamped modal dialogs; DPI/theme/layout; keyboard and accessibility coverage; offline Help `謝辞` identifies the locked BLAKE3/PNG production dependencies and full-notice authority through the embedded-help route with dependency-drift, extraction, menu/state and smoke coverage; `Inkpodウェブページ` opens the canonical HTTPS URL through the shell adapter; reproducible [release checklist](windows-release-checklist.md) | High contrast, a physical 200% DPI desktop, complete screen-reader and Japanese IME work remains; the embedded offline Help body is Japanese-only |
| `WIN-002` | Verified | Multi-window registry, focus/routing, close/shutdown, sink lifetime, single-instance activation, malformed IPC, x64/ARM64 native smoke | — |
| `WORKSPACE-001` | In progress | All eight modeless surfaces hosted by DockHost; one-direction split stacks with resource-titled headers retained for singleton inspector panes; visible system-color 4-DIP dock and Layer/Plane splitters with pointer/keyboard focus states; explicit Layer/Plane action-target label and accessible button names; Color above the default Layer/Plane + Light Table + Subpalette/Reference tab stack; no duplicate pane-local Close control in tabbed Light Table content while Esc/floating close routes remain; dock/tab/float/hide/AutoHide policy; five presets; V7 round-trip and targeted V4/V5/V6 migration; transient non-persisted Job Progress; malformed HKCU recovery; monitor/DPI clamp; model, static-contract, accessibility and native workspace smoke | Reference Check AutoHide edge buttons are not keyboard-reachable with F6/Tab |
| `WORKSPACE-002` | Verified | Issue-time command context, follow/pin/job policy, stale rejection, cross-window drag/move/copy and target-aware pane smoke | — |
| `SESSION-001` | Verified | File identity, multi-session/view isolation, savepoints, close scopes, Save As conflicts, autosave/recovery, malformed-open and native lifecycle smoke | Long operations serialize on one observable `CoreHost` lane |
| `SAFE-001` | Verified | Corruption/mutation corpus, bounded V14/checkpoint/asset/edit-target/selection-option/color-replace rejection, staged publication, failure atomicity, fuzz-target declarations | Coverage-guided fuzz execution is not part of the latest local run |
| `PERF-001` | Verified | Fixed ten-scenario quick/full and native workloads; checksum/revision/reuse/rebuild/payload/sample/Present/resource gates; approved ARM64 revision-max/native envelopes and x64 RGBA16 output-color-guard five-run envelope | Canonical `revision-max` limitations remain; `checkpoint_open` has no retrospective timing envelope |
| `PKG-001` | Verified | Static-CRT x64/ARM64 builds, dependency checks, exact four-file ZIP, unsigned MSIX and extracted ABI smoke | ZIP has no file association; administrator MSIX install/uninstall is optional validation |
| `PORT-001` | Verified | Zero-Windows-import acceptance guard and shared Linux/macOS replay/workspace checks | Future sandboxed frontends still need byte/stream and file-authority adapters |
| `DOC-001` | Verified | Metadata, paper/frame/DPI equality, mixed-paper alignment, native round-trip and Windows dialog smoke | — |
| `CELL-001` | Verified | Public image/frame-mode table, every initial layer and RGBA8/16 save/reopen, invalid/overflow/UUID failure atomicity; owned-plan ABI negatives; one-dialog Core-plan summary; three-session Windows production smoke; x64 Release image/frame-mode, 16-bit three-Cell, properties and Cancel manual check | — |
| `CUT-001` | Verified | Individual same-directory Cell-reference descriptor; independent Cut metadata/defaults/history/savepoint; persistent `(CellId, document UUID)` member identity with document-local numeric Cell IDs; default copy only at Cell creation; no-op/invalid/Cancel/stale/overflow/failure/Undo/Redo/save/reopen/recovery and identity/path rejection tests; ABI v14 ownership/negative coverage; issue-time workspace/session/generation and Cut-revision rejection in Windows New Cut/Properties/Sequence-pane/open/save/history production smoke; x64 Release user-visible creation, existing-Cell independence, Undo/Redo and save/reopen confirmation | — |
| `SEQ-STRUCT-001` | Verified | Bounded ordered insert/remove/move-before/move-after/range-renumber transaction; stable pair identity; separated asset table and ordered membership history; one-step Cut Undo/Redo; current-v26 save/reopen and exact-v25 rejection; caller-owned strided ABI with per-operation failure index; Sequence-pane buttons, drag, keyboard and renumber dialog production smoke with stale active-Cell retarget rejection; x64 Release five-Cell reorder/renumber/remove/Undo/save/reopen confirmation | — |
| `DOC-002` | Verified | Stable-ID typed topology; grouped tree operations, validation, save/reopen, ABI spans, Windows marker/accessibility/status/menu/smoke, and x64 Release confirmation | — |
| `DOC-003` | Verified | Tree-ordered bounded multi-target EditorState; grouped duplicate/delete/visibility/editability/compatible merge/convert capability matrix; no-op/invalid/stale/Undo/replay/save-reopen; ABI spans; Windows marker/accessibility/status/menu/smoke; x64 Release grouped main-line/color paste, one-step Undo and active-plane-only brush manual check | — |
| `ANNOTATION-001` | Verified | Bounded stable-ID Text/Stroke/Leader/Value objects; atomic multi-edit and stroke transactions; UTF-8/geometry/no-op/invalid/Cancel/stale/overflow/failure/Undo/Redo/replay/save-reopen public contracts; ABI v14; Windows Japanese text editor, instruction draw, keyboard selection/move/delete, selection handles, font-fallback and product smoke; x64 Release Japanese text/instruction edit, save/reopen, and PNG exclusion confirmation | — |
| `SHOOTING-FRAME-001` | Verified | Stable-ID optional angled frame; deterministic milli-pixel/binary-turn geometry and five anchors; create/update/delete and preview/Cancel; no-op/invalid/stale/overflow/nonuniform-resample rejection atomicity; Undo/Redo/replay/current-v26 save/reopen; ABI v14 ownership/negative cases; Canvas properties/handles/renderer, explicit instruction-raster production smoke, and x64 Release user-visible properties/handles, Cancel, save/reopen, and normal/instruction export confirmation | — |
| `VANISHING-POINT-001` | Verified | Bounded stable-ID multiple points including Canvas-exterior coordinates; exact RGBA8/16, opacity, visibility, 1-degree/custom interval and 180-degree phase wrap; viewport-clipped radial snapshot cap; create/update/delete/delete-all and noncumulative preview/Cancel; no-op/invalid/stale/overflow/failure and nonuniform-resample atomicity; Undo/Redo/replay/current-v26 save/reopen and flat-export exclusion; caller-owned ABI records and snapshot-owned spans; Windows dialog/Canvas handles/renderer/device-loss production smoke; x64 Release two-point, radial snap, Cancel/Undo, and save/reopen user confirmation | — |
| `RENDER-001` | Verified | Existing mixed raster/vector/adjustment evidence plus immutable annotation, shooting-frame, vanishing-point and radial-guide render spans; DirectWrite Canvas text/fallback warning; fixed angled-frame and bounded radial overlays with device-loss rebuild; Core-owned deterministic thumbnail/flat raster; normal/instruction inclusion policy goldens; x64 Release vanishing-point rendering confirmation | — |
| `VIEW-001`, `VIEW-002` | Verified | Zoom/box/fit/1:1/pan/flip, ruler/guide/grid/snap/transparent-view Core, FFI, coordinate and Windows gesture/render smoke | The main-window zoom slider is intentionally absent |
| `SNAP-001` | Verified | View-targeted device/document point resolution; explicit-guide/radial-guide/grid precedence, inclusive threshold and deterministic tie-break; Ctrl bypass; bounds/extreme transform/flip/DPI/stale tests; additive ABI negatives; Windows menu checked-state, line geometry/digest, off/bypass and Undo/Redo production smoke; x64 radial snap user confirmation | — |
| `VIEW-003` | Verified | Bounded locator sampling/neighborhood, sparse allocated-tile selection bounds with document-revision caching, DPI/flip/half-open coordinates, active-stroke/filter-preview display priority, committed eyedropper isolation, one-running-plus-latest async coalescing with processed-intermediate presentation, Begin/Append preview plus End/Cancel resynchronization without revision/history publication, FFI buffers, target/pin/stale handling, and native continuous-input pre-mouse-up/capture-cancel pane smoke | — |
| `VIEW-004` | Verified | Multi-view revision/history, per-view device-coordinate stroke placement, split/group/window move/copy, same/different-session isolation, active-stroke and native lifecycle smoke | — |
| `VIEW-005` | Verified | Per-view typed AA/centerline/endpoint flags; explicit stable endpoint topology; exact/near/closed/connected Core contracts; current-version save/reopen and noncurrent rejection; bounded borrowed ABI records; Windows menu/shortcut/checked state, Direct2D pixel golden, device-loss smoke, and x64 Release user-visible confirmation | — |
| `HIST-001` | Verified | Journal state-machine, full/checkpoint replay equivalence, inactive branches/assets, savepoints, cache rebuild, compaction and Windows Undo/Redo/dirty smoke | Compaction intentionally creates a new Genesis with empty history after explicit confirmation |
| `HIST-002` | Verified | Commit-event ordering including inactive branches; deterministic typed primitive/argument formatting; point-in-time bounded replay steps with progress/cancel and queue-tail re-entry; direct maximum-64×64 post-commit composition; query/cancel/no-document stability; Rust-owned ABI builder/snapshot ownership, size-query and negative cases; nonblocking published menu/status state; dynamic open-native-session submenu, one modeless owner-data list per document, loading row, shared Job Progress, cooperative visible-row prefetch, dialog reuse/close and Windows production smoke | The dialog snapshots the in-memory journal when opened; reopen it to reflect later edits |
| `PAINT-001`, `PAINT-003` | Verified | Stable-target stroke/cleanup/width primitives; pressure, extreme/per-view device coordinates, preview/cancel, replay, ABI, Canvas and Windows smoke | Pencil is fixed at 1.0 document pixel; brush/eraser share the documented diameter range |
| `PAINT-002` | Verified | Raster/vector capability-table and pixel/geometry goldens for line, two-stage curve, rectangle, ellipse, regular polygon, and click polyline; resolved canonical geometry v2, preview begin/update/commit/Cancel, constraints, Undo/Redo/replay/save-reopen, bounded ABI negatives, Windows staged gestures/renderer route, ARM64 geometry confirmation, M15 production snap automatic coverage, and x64 user-visible snap/bypass/zoom confirmation | — |
| `PAINT-004` | Verified | Round/square and pressure footprints; fixed-point smoothing off/on and append batching equality; immutable exact native Binary/Grayscale/RGBA8/16 start-color predicate including alpha, detached same-color hits and selection clip; no-op/invalid/stale/overflow/cancel/Undo/Redo/replay/save-reopen; ABI v8 negatives, Windows Tool Options/Canvas smoke, and x64 Release narrow two-color repair, shape/smoothing, Cancel and Undo manual verification | — |
| `FILL-001`, `FILL-002`, `FILL-003` | Verified | Connected/inclusion/closed-region goldens, tolerance/selection/gap/overflow/cancel/no-op/Undo, FFI, Windows target smoke, and non-mutating closed-fill drag preview | Gap close follows the documented deterministic native rule |
| `COLOR-REPLACE-001` | Verified | Exact native-depth raster and whole stable vector path/fill replacement; pen/rectangle/polyline/lasso and selection intersection; preview/Cancel, no-op/invalid/stale/overflow, Undo/Redo, replay/save-reopen; ABI negatives, Windows production gesture smoke, and x64 Release range isolation/Cancel/Undo/whole-vector-object manual verification | — |
| `COLOR-001` | Verified | Exact RGBA8/16, RGB/HSV editing, eyedropper sources, main-line procedures, codecs, history/replay and Color-pane smoke | Display conversion does not replace stored exact-depth color |
| `COLOR-002` | Verified | Independently persisted palette/chart/subpalette; legacy white/native-alpha checks; chart generation/comparison; codecs, history/replay, Windows production routes and ARM64 user-visible confirmation | — |
| `COLOR-CHART-PREVIEW-001` | Verified | Same-base noncumulative RGBA8/16 extraction; alpha, maximum/quantization/overflow goldens; immutable UUID/revision-bound preview and task ownership; Cancel/invalid/lock/stale/cross-document/failure atomicity; exact-name and cursor retention; one-step Undo/Redo; current v26 save/reopen; Windows latest-wins Job Progress/comparison/Apply production smoke; ARM64 user-visible generation/retry/Cancel/Apply/Undo/save-reopen confirmation | — |
| `COLOR-OUTPUT-QA-001` | Verified | Conservative BT.709 Y′CbCr rational half-up boundaries; exact RGBA8/16 raster/vector visible straight-alpha composite and transparent skip; sparse selection algebra, no-op/Cancel/stale/failure atomicity, Undo/Redo/replay/current-v26 save/reopen; caller-owned ABI negatives; versioned HKCU default-profile codec; Windows task/status production smoke; quick/full guard semantic gates and approved x64 wall-clock envelope; x64 Release profile/overlay/pixel-invariance/Undo user-visible confirmation | The sole profile is intentionally labelled as an inkpod conservative QA guard, not formal standards conformance |
| `SEL-001`, `SEL-002`, `SEL-003` | Verified | All shape/mode algebra, color/width operations, layer conversion, vector modes, preview/cancel, ABI and Windows gesture smoke | — |
| `SEL-004` | Verified | Normal/Tight/EnclosedInterior/Drawing/Boundary 8/16-bit goldens; edge/hole/tile/final-pixel bounds; aspect/center/rotation/45-degree and round/square pressure/screen trace; no-op/invalid/stale/Undo/Redo/replay/save-reopen; ABI v8 negatives, Windows production preview smoke, and x64 Release manual mask/geometry/modifier check | — |
| `CLIP-001` | Verified | Ordered multi-plane typed clipboard; exact RGBA8/16, origin and tree order; cross-plane vector fill topology; coordinate preservation, converted-plane transaction, external DIB, ownership, Cancel/Undo/replay and Windows grouped copy/paste smoke | Standard import accepts validated 24/32-bit DIB layouts |
| `XFORM-001`, `XFORM-002` | Verified | Destructive document transforms, frame/guide consistency, floating selection lifecycle, retry/cancel/Undo, FFI and Windows dialog/handle smoke | — |
| `XFORM-003` | Verified | Half-open five-anchor absolute X/Y contract; shared raster/vector scale→clockwise-rotate→position Core path; noncumulative preview/retry/Cancel; canonical-v3 replay and v26 save/reopen; ABI v10 short/unknown-anchor negatives; Windows dialog/handle/renderer production smoke; x64 Release 31/31 CTests and user-visible five-anchor dialog/handle, Cancel and Undo confirmation | — |
| `LT-001`, `LT-003` | Verified | Set/item administration plus previous/next/both-N registration; linear distance opacity, chronological z-order, UUID duplicate preservation, preview/Cancel/no-op/invalid/stale/overflow/failure atomicity, one-step Undo/Redo, replay/save-reopen; canonical-v2/current v26/epoch-23, caller-owned ABI negatives, Windows pane/production smoke, quick/full benchmark gates, and ARM64 user-visible order/opacity/Undo/save-reopen confirmation | — |
| `LT-002` | Verified | Alignment, move/sample/reload/edit-image swap, reference viewer, history isolation, ABI and target-aware Windows smoke | — |
| `SEQ-001` | Verified | Natural-order discovery, thumbnails, direct selection and navigation; Prompt/Autosave-before-switch setting; immutable source/target identity and revision; durable exact-native autosave association; staged restore with dirty/history/editor preservation; failure/stale/ABI negatives; Windows setting/owner/production smoke and ARM64 user-visible confirmation | — |
| `SEQ-ENDPOINT-001` | Verified | Closed Stop/Wrap policy; empty/single/stopped/advanced/wrapped Core plans; natural-order gap skipping; issue-time sequence/source/target identity and stale/failure atomicity; additive ABI v14 96-byte caller-owned plan with short/unknown/unsupported negatives; versioned HKCU codec with Stop fallback; application-wide menu/shortcut/checked/status route; motion-loop separation; x64 production smoke and user-visible Stop/Wrap, checked-state persistence, restart, and dirty endpoint no-op confirmation | — |
| `SEQ-002` | Verified | Motion Core/ABI state and Windows FPS/loop/pause/step/shortcut smoke | — |
| `SHORT-001` | Verified | Full command catalog, prefix/conflict/reset/persistence, text-focus guard, FFI resolve and Windows key/menu smoke | Palette `1`-`0` retains its documented no-match fallback |
| `FILTER-001`, `FILTER-002` | Verified | Exact 8/16-bit selection/alpha goldens, catalog, preview/cancel/Undo, shared Job Progress pane, ABI and Windows editor smoke | Filter work runs on the Core worker and reports task progress |
| `FILTER-PREVIEW-001` | Verified | Same-base A→B Core acceptance; existing ABI v9 begin/update/apply/cancel ownership and negatives; 120ms dialog notification debounce; one-running/one-pending latest-wins queue with cooperative cancel and issue-time target generation; shared dialog/Job Progress reporting; Windows production smoke with four preview generations, Cancel, one Undo/Redo; ARM64 user-visible confirmation | — |
| `EFFECT-001` | Verified | Gradient/airbrush/boundary/blur/stamp acceptance, deterministic pressure/gesture/bounds, ABI and Windows smoke | — |
| `ADJUST-001` | Verified | Non-destructive order/source/opacity/visibility/save-reopen, malformed metadata, alpha RGB preservation, ABI and Windows smoke | — |
| `BATCH-001`, `BATCH-002`, `BATCH-003` | Verified | Current-version graph round-trip, complete operation catalog, dry-run/preview/shared Job Progress/cancel/failure/atomic output, FFI and Batch-pane smoke | Native `.inkpod` is the only output format; boundary airbrush begins with two colors; `.inkbatch` v2 remains production until the separately accepted InkScript cutover |
| `BATCH-004` | Verified | Bounded multi-row seed/pair authoring; exact native-depth two-cell extraction with explicit one-to-many resolution; typed replacement/selection/main-line/color/native-file separation; immutable per-run configuration; `.inkbatch` v2 and current `.inkpod` v26/epoch-23 replay; FFI ownership/negative tests, Windows production smoke, and x64 Release user-visible manual confirmation | InkScript's 1:N grouped-step projection is specified but remains private and unimplemented |
| `SCRIPT-001` | In progress | M00 closed meta-schema and approved command-independent language v1 registry; M01 source/lexer; M02 lossless CST/bounded parser; M03 semantic AST/canonical emitter; M04 typed orchestration; M05A type/namespace/parameter/immutable each-run models; M05B bounded test-schema scalar/list results, immutable typed step/group/result and occurrence-preserving dependency graph with 5 public contracts plus the expanded Core/FFI/Windows non-reachability gate; lexer/parser fuzz targets remain checked | Selector/assert execution, production catalog, Core compiler/executor, authority/plan, ABI, UI, and product `.inkscript` acceptance remain unavailable before their owner milestones; M05B awaits user binary regression confirmation |
| `SCRIPT-002` | In progress | M00 owner manifest is a tested bijection over all 84 replayable primitives, with stable planned command/equivalence IDs and one explicit session-only exclusion | Catalog draft intentionally has no command entries; typed adapters/execution begin at M06/M07 and stay private through M22 |
| `SCRIPT-003` | Not started | Normative authority, planning, per-item atomicity, native persistence and report contract in `INKSCRIPT.md`; milestones M09–M12/M26–M29C assigned | No InkScript plan, runner, output, save/reopen, or report implementation exists |
| `SCRIPT-004` | In progress | M05B private typed step/group closure includes only transitive parameter/binding/asset and in-range producer dependencies, rejects outside mutations unless explicitly converted to a source-UUID/nonzero-ID strict binding, deduplicates equal assets, and performs deterministic atomic value/asset/group alpha-renaming with canonical reparse | Journal exporter, destination rebind/paste transaction, Batch/History clipboard and source-preserving structured editor do not exist yet |
| `SCRIPT-005` | In progress | Approved M00 traceability fixes `.inkbatch` as current production, makes M29C the shadow-parity gate, and reserves M34/M35 for cutover/removal; benchmark approval remains M13→M14 | InkScript remains unreachable from production and no cutover has occurred |
| `VECTOR-001`, `VECTOR-002` | Verified | Geometry/render goldens, path editing, width/connect/select/vectorize/rasterize transactions, mixed-order zoom/save/replay, ABI and Windows Canvas smoke | Rasterize preserves its source layer |

## Maintenance rule

Keep one current row for every `SPEC.md` requirement ID. Replace evidence and
known differences instead of appending milestone or dated verification logs.
Move exact samples to the relevant baseline document and current platform
results to [`implementation-status.md`](implementation-status.md).
