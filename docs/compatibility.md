# Compatibility status

This is the single record of implementation status, known gaps and representative
verification for the requirement IDs in [SPEC.md](../SPEC.md). Detailed behavior
belongs to SPEC; ownership to [architecture.md](architecture.md), exact bytes to
[file-format.md](file-format.md), and test procedures to [verification.md](verification.md).

Status is `Not started`, `In progress`, `Experimental`, `Verified`, or `Blocked`.
`Verified` requires tests of the requirement's observable result at its production
UI boundary, or its natural Core/ABI/build boundary for infrastructure. It does
not imply verification on every platform or physical device. Historical version
numbers in evidence describe that test run, not an accepted legacy format.
Current version authority is SPEC section 20 and the exact header/codec contracts.

## Requirement matrix

| Requirement | Status | Representative evidence | Known difference / next work |
| --- | --- | --- | --- |
| `ARCH-001`, `ARCH-002` | Verified | CMake/Cargo tracks all five crates; architecture guards cover dependency direction, OS imports, private backend isolation, declarative module roots and public integration boundaries | Rust domain crates remain platform-independent; only the private `inkpod-io` filesystem backend uses OS file APIs |
| `ABI-001`, `ABI-002` | Experimental | Exact-current ABI v34 adds the bounded 128-byte line-correction request, direct/task-preview entry points and explicit expected-revision/background/gesture contracts. Existing owner-thread and release rules remain. Rust/C11/C++ layout, invalid/stale/cancel/output-sentinel and task-ownership tests pass | ABI v1-v33 callers must rebuild. Unrun configurations/platform/manual checks remain tracked under `IO-003` |
| `IO-001` | Experimental | Current-only native v34/runtime epoch 29; standard layers require one MainLine and one Color plane with optional Raster planes. A fully opaque common-raster import keeps its exact editable RGBA MainLine over a SolidWhite Genesis underlay, while an import with any non-opaque alpha retains a Transparent underlay. The immutable Genesis asset preserves canonical decoded dimensions, RGBA8/16 depth, straight alpha and pixel values; byte-for-byte source-container preservation is intentionally not promised. Native save/reopen retains the full journal, Undo/Redo cursor, inactive branches/assets, EditorState and both savepoints. A real SavePair → raster-resolver reopen → Revert/Redo regression additionally proves canonical Genesis retention, complete journal/cursor, inactive-branch assets and a companion containing the current composite rather than the original Genesis. DOCM is mandatory; current selection, ordered stable-ID saved masks and sparse fill protection are outside the image tree. Archive schema 7, metadata schema 9, document digest schema 13/domain 11 and snapshot-composite schema 5 are current; Cut descriptors are rejected | Non-v34 files are rejected without migration; compression code 0 only. Pair paths and the three states `Planned`/`Committed`/`None` remain runtime-only; approved line-correction/background/selection semantics and the new primitive cause this native/replay update. Remaining physical/manual checks are tracked under `IO-003` |
| `IO-002` | In progress | PNG/TIFF exact RGBA8/16 codec and pair-companion coverage; TGA/BMP preserve their supported decoded depths and reject a 16-bit normal companion instead of quantizing. Alpha/DPI round-trips, all seven standard TGA 2.0 image types, true-color/index/palette depths, grayscale, uncompressed/RLE, four origins, old/new footer, Extension/Developer Areas, alpha semantics, color correction, postage stamp, scan-line table, malformed/mutation corpus and `tga_v2` fuzz target remain. Shared Core/ABI import regressions plus the build-134 synthetic TGA product route remain representative | A current full product end-to-end normal-pair Save/reopen check for 16-bit PNG and TIFF remains. Save As creates a new pair only; flat raster is Export. Product TGA export keeps the stable top-left 32-bit uncompressed default while alternate TGA storage is exposed by the typed Rust format API |
| `IO-003` | In progress | One application-owned manager retains the existing bounded workers, path/identity locks, encoded/decoded limits, pair resolver, authority and recovery fences. Sequence attachment keeps all source tiles/thumbnails resident, drops the same pixels' dense decoder-cache owner, and installs up to 64 complete inactive editable targets in a COW resident bank. Exact same-manager/path/stamp/generation/format/metadata provenance constructs sidecar-less targets from resident tiles and a precomputed asset ID with zero read/decode/dense copy/hash/full retile. Existing sidecars enter a clean-target LRU only after replay and exact companion validation; it is 0–1024 MiB configurable (1 GiB default), 64 targets maximum, and exact revisits COW-share Core/assets/tiles with zero read/decode/replay. The live cell retains pair authority without duplicating its Core in the inactive bank. Stamp/namespace/final TOCTOU validation remains independent of cache hits. ABI v34 exposes resident and preparation APIs | The v0.3.1 clock-dependent CI fixture now establishes a later filesystem change time before its single rewrite; coarse-clock and invalidation-mutation diagnostics verify the correction. Equal complete stamps still cannot prove content equality after an external rewrite; explicit reload bypasses cached bytes. First visits to existing sidecars still replay/compare during initial catalog attachment. Backends without an equally strong namespace observer deliberately remain uncached. Unrun configurations/platform/manual checks remain. Pair save does not promise filesystem-level atomicity across two files |
| `WIN-001` | In progress | Startup/shutdown, Common Controls registration, native client colors and DWM title-bar-only dark opt-in; generated Japanese/English resource and format-signature parity; menu/command-state ownership; persisted language selection; typed Fluent icons with localized text fallback; modal/layout and owner-draw contracts; actual Tooltip/MSAA/UIA name checks; both localized product smokes; offline Help/dependency-attribution and shell-opening routes. Sources: Windows localization, Fluent-icon, dialog, DockHost, owner-model and application smoke tests; [release checklist](windows-release-checklist.md) | Client UI intentionally does not follow Windows app Light/Dark preferences; native control visual styles and system colors are retained. Canvas/Light Table/Subpalette content stays unchanged; high contrast, a physical 200% DPI desktop, complete screen-reader and Japanese IME work remains; the embedded offline Help body is Japanese-only |
| `WIN-002` | Verified | Multi-window registry, focus/routing, close/shutdown, sink lifetime, single-instance activation, malformed IPC, x64/ARM64 native smoke | — |
| `WORKSPACE-001` | In progress | DockHost retains the persistent fixed-width Tool strip, Color, Layer/Plane and auxiliary surfaces, while Tool Options remains a bounded owned popup. The Job Progress pane and its Window-menu command are removed; transient statusbar progress is scoped to each workspace and excluded from layout persistence. The singleton Tool strip has no zone-extent splitter, cannot float or AutoHide, and is controlled only by its direct visibility toggle. The right zone is a bounded dynamic list of stable-ID tabs: every tab is nonempty, pane membership is unique, panes and tabs reorder, panes move between tabs, insufficient minimum height creates another tab, and deleting the selected last pane chooses previous/next/first deterministically. The first pane supplies the label; the full pane list supplies the accessible description; narrow-width suppression is transient. Right-pane resize uses one shared two-layer same-parent transaction. Every right pane registers final child geometry in a fixed-capacity `PaneDialogLayoutPlan`; commit skips platform-normalized unchanged bounds, applies the changed set with `BeginDeferWindowPos`/`DeferWindowPos`/`EndDeferWindowPos` plus `SWP_NOREDRAW | SWP_NOCOPYBITS`, verifies final and rollback geometry, and publishes sticky inner failure to the pane root. Sequence commits geometry before guarded ListBox metrics/top-index updates. Color then places its Common Controls tab at `HWND_BOTTOM`, raises visible sibling pages in stable keyboard order, verifies z-order, and attempts and verifies old-order restoration on failure; restoration verification failure remains a transaction failure. While DockHost places tabs, same-parent pane roots and splitters, an ancestor-visible property defers inner completions. Synchronous DockHost mutations snapshot DockLayout/right-tab models and pane host flags; outer or inner failure restores those models, attempts and verifies physical geometry/local-visibility rollback, and reapplies the old chrome projection without committing the candidate model. DockHost then synchronously redraws one bounded dirty union; Structure explicitly includes old/new right-zone and tool-tab bounds so selected-tab add/remove cannot leave an old frame. Structure may resynchronize DockHost chrome tab items, but Geometry and Structure preserve surviving pane control/list `HWND` identity, pane-owned tab/list contents, selection and top index and issue no Color/Layer `LB_RESETCONTENT`. Relayout does not arbitrarily reset focus, while a successful show command retains the SPEC 88 destination selection and natural-first-target focus rule. Floating/expanded AutoHide/reparenting complete under their destination parent. Color's swatch, labels and picker retain themed-tab pixels and cache invalidation; its 300-DIP minimum/360-DIP preferred floating height and adjacent splitter minimums remain. Settings JSON still round-trips readable tab keys/order/membership/selection/next-key without registry/old-format migration. The actual-`HWND`, DockHost boundary, workspace-layout and owner-model focused tests plus the x64 Debug MSVC `/W4 /WX` build pass. All 44 current x64 Debug tests other than the product smokes and unrelated sequence-performance test pass in 28.00 seconds; final English/Japanese product smokes pass in 110.55/113.32 seconds. The product probe covers same-tab Reference add/remove geometry, vacated-pixel sentinels, parent/child/owner/tool-tab update regions, zero Color/Layer `LB_RESETCONTENT`, list count/selection/top-index retention, stable pane/tab/control identity, new-pane and survivor focus, and rejected-`WM_COMMAND` Reference-placement/right-tab-model/focus/failure-result rollback. | Reference Check AutoHide edge buttons are not keyboard-reachable with F6/Tab. Outer DockHost OS-level placement/rollback fault injection, physical high-DPI repaint, non-x64-Debug configurations and the unrelated sequence-performance test remain pending for the shared resize change. Outer DockHost OS-level placement/rollback failure injection remains pending |
| `WORKSPACE-002` | Verified | Issue-time command context, follow/pin/job policy, stale rejection, cross-window drag/move/copy and target-aware pane smoke | — |
| `SESSION-001` | In progress | Existing multi-session/view isolation, close scopes, savepoint fencing, Save As conflict checks and autosave/recovery ownership remain. The adopted contract registers both pair members as one logical identity and resolves Save through `Committed`, `Planned`, then `None`; repair-needed is a `Committed` substate. Explicit Save As derives a new pair, while flat raster remains Export. Current-document Revert requires the same native path/UUID, retains inactive sequence recovery and all live view identities/logical states, and republishes the pair identity, shell path and active binding at the new owner generation. Product regressions cover Committed and Planned explicit Save As (including uppercase `.TGA`), unchanged old pairs, new authority/binding, next normal Save without a dialog, raster-resolver reopen, multi-workspace Revert and post-publication failure revocation/retry | Unrun configurations/platform/manual checks remain under `IO-003` |
| `SAFE-001` | Verified | Corruption/mutation corpus, bounded V14/checkpoint/asset/edit-target/selection-option/color-replace rejection, staged publication, failure atomicity, fuzz-target declarations | Coverage-guided fuzz execution is not part of the latest local run |
| `PERF-001` | Experimental | All nine Core workflows retain their counter/checksum and canonical `revision-max` gates. Sequence switching now exchanges complete COW resident Core states, separates dirty autosave publication, prepares up to 64 CPU/GPU sources under 1 GiB limits, reduces renderer frame-latency retry to 4 ms, and coalesces only secondary UI projection for 75 ms. Warm x64 Release A/B/A and A/B/C/B/A paths retain zero read/decode/upload and one snapshot per step; latest handler p95 is 2.01/2.48 ms and snapshot-submission p95 is 1.49/2.09 ms | First existing-sidecar catalog attachment still validates/replays. Current non-foreground DWM Present p95 exceeded its target despite zero frame-latency timeouts; initial/edited/recovered/background latency and unrun configurations remain. No approved workload or envelope changed |
| `PKG-001` | Verified | Prerelease v0.3.2 clean x64/ARM64 Release builds pass static-CRT/dependency, embedded-version, exact four-file ZIP and unsigned-MSIX checks; extracted x64 portable smoke and all 50 native x64 CTests pass. The selected Fluent SVG provenance/hashes/MIT notice are fixed and the generated atlas is embedded in the packaged executable, so normal and distribution builds remain offline | ZIP has no file association; administrator MSIX install/uninstall is optional validation. ARM64 native execution and physical interaction checks were not repeated for v0.3.2. An earlier build-145 x64 run had one English product-smoke status-I/O keyboard failure; the isolated rerun and replacement clean build 146 passed, but the intermittent cause remains unresolved |
| `PORT-001` | Verified | Architecture guards prohibit Windows dependencies in Core/image/format and confine OS file identity/replace operations to the private `inkpod-io` backend. Shared Linux/macOS replay/workspace checks remain in CI; Fluent asset names and icon IDs stay in Windows sources | Non-Windows execution was not rerun locally for this migration. Future sandboxed frontends still need platform file-authority adapters; the shared I/O manager is not an OS permission bypass |
| `DOC-001` | Verified | Metadata, paper/frame/DPI equality, mixed-paper alignment, native round-trip and Windows dialog smoke | — |
| `CELL-001` | Verified | Public image/frame-mode table, every supported initial MainLine/Color format and RGBA8/16 save/reopen, invalid/overflow/UUID failure atomicity; owned-plan ABI negatives; one-dialog Core-plan summary; three-session Windows production smoke; x64 Release image/frame-mode, 16-bit three-Cell, properties and Cancel manual check | — |
| `DOC-002` | Verified | Stable-ID typed topology; grouped tree operations, validation, save/reopen, ABI spans, Windows marker/accessibility/status/menu/smoke, and x64 Release confirmation | — |
| `DOC-003` | Verified | Tree-ordered bounded multi-target EditorState; grouped duplicate/delete/visibility/editability/compatible merge/convert capability matrix; no-op/invalid/stale/Undo/replay/save-reopen; ABI spans; Windows marker/accessibility/status/menu/smoke; x64 Release grouped main-line/color paste, one-step Undo and active-plane-only brush manual check | — |
| `SHOOTING-FRAME-001` | Verified | Stable-ID Canvas-only angled frame retains deterministic geometry, preview/Cancel, atomic no-op/invalid/stale/overflow rejection, Undo/Redo/replay and current native save/reopen. Normal PNG/TIFF/TGA/BMP export remains identical with/without the overlay, with white-background composition both enabled and disabled. Instruction export and its persistent flag are removed | Current physical/manual checks remain tracked separately; historical instruction-export evidence no longer describes product behavior |
| `RENDER-001` | Verified | Immutable raster render spans plus the independent shooting-frame overlay; standard-layer order, visibility, opacity and alpha are shared by Canvas, thumbnail and flatten. Snapshot-composite schema 5 contains no adjustment LUT or radial-guide record; device-loss rebuild uses the retained snapshot | — |
| `VIEW-001` | In progress | The zoom/box/fit/1:1/pan/flip baseline now includes permanent native Main-Canvas bars, checked accepted-only `SCROLLINFO`, `q = -pan`, half-viewport padding, per-view sticky/frozen range state, 32-DIP line and overlapped page targets, no-optimistic relative input, and targeted/session reset envelopes retained across queue rejection. Pure-model and CoreHost tests pass; the actual-`HWND` test reaches projection on two document-bound Canvas instances and line-routing assertions | Complete page/thumb, two-view, post-Core renderer-rejection reconciliation, resize/final-frame, disabled-state, MSAA/UIA and cache-hit performance evidence remains. Current normal-desktop RendererHost reruns pass; the older locked-desktop failure does not count as completed UI/platform coverage. The main-window zoom slider remains intentionally absent |
| `VIEW-002` | Verified | Ruler/guide/grid/snap/transparent-view Core, FFI, coordinate and Windows gesture/render smoke | — |
| `SNAP-001` | Verified | View-targeted device/document point resolution; explicit-guide/grid precedence, inclusive threshold and deterministic tie-break; Ctrl bypass; bounds/extreme transform/flip/DPI/stale tests; additive ABI negatives; Windows menu checked-state, line geometry/digest, off/bypass and Undo/Redo production smoke | — |
| `VIEW-003` | Verified | Bounded locator sampling/neighborhood, sparse allocated-tile selection bounds with document-revision caching, DPI/flip/half-open coordinates, active-stroke/filter-preview display priority, committed eyedropper isolation, one-running-plus-latest async coalescing with processed-intermediate presentation, Begin/Append preview plus End/Cancel resynchronization without revision/history publication, FFI buffers, target/pin/stale handling, and native continuous-input pre-mouse-up/capture-cancel pane smoke | — |
| `VIEW-004` | Verified | Stable view/session caption lookup preserves untitled/named tab labels and model order while selecting tabs. Closing the final canvas tab leaves an empty workspace with New/Open available. Owner/CoreHost/command regressions and complete ARM64 Debug/x64 Release English/Japanese product smokes cover these routes, including failed New/Open/Save, cancellation, pinned/following pane captions, another workspace, and split-failure restoration. Retained baseline: multi-view revision/history, per-view device-coordinate stroke placement, split/group/window move/copy, same/different-session isolation, active-stroke and native lifecycle smoke, DPI-scaled 9-point Segoe UI document-tab font for initial and split groups, bounded per-view Common Controls close buttons with system-color DPI-scaled icons, exact stable-view `Close View` routing, inactive-tab close with active-view preservation, and drag/close separation | See [SPEC.md](../SPEC.md) sections 2 and 4; physical/platform coverage gaps remain under `WIN-001` |
| `VIEW-005` | Verified | Per-view typed AA/centerline/endpoint flags; explicit stable endpoint topology; exact/near/closed/connected Core contracts; current-version save/reopen and noncurrent rejection; bounded borrowed ABI records; Windows menu/shortcut/checked state, Direct2D pixel golden, device-loss smoke, and x64 Release user-visible confirmation | — |
| `HIST-001` | Verified | Journal state-machine, full/checkpoint replay equivalence, inactive branches/assets, savepoints, cache rebuild, compaction and Windows Undo/Redo/dirty smoke remain. SavePair/reopen/Revert/Redo regressions preserve canonical Genesis, the complete journal/cursor, inactive branches/assets, EditorState and both savepoints. Current-document Revert requires the exact native path/UUID, restores that serialized state, retains runtime sequence plus all live view IDs/logical states, invalidates stale render caches and reconciles authority/binding in Core and English/Japanese product smokes | Compaction intentionally creates a new Genesis with empty history after explicit confirmation |
| `HIST-002` | Verified | Commit-event ordering including inactive branches; deterministic typed primitive/argument formatting; point-in-time bounded replay steps with progress/cancel and queue-tail re-entry; direct maximum-64×64 post-commit composition; query/cancel/no-document stability; Rust-owned ABI builder/snapshot ownership, size-query and negative cases; nonblocking published menu/status state; dynamic open-native-session submenu, one modeless owner-data list per document, loading row, shared native statusbar name/bar/selector/cancel, bounded generation-tagged task entries for simultaneous document dialogs, cooperative visible-row prefetch, dialog reuse/close and Windows production smoke | The dialog snapshots the in-memory journal when opened; reopen it to reflect later edits |
| `PAINT-001` | Verified | Stable-target stroke primitives; target-aware MainLine versus tool paint-color capture; exact start-pixel Pencil auto-erase for Binary and imported straight-RGBA MainLine; pressure, extreme/per-view device coordinates, preview/cancel, Undo/Redo, replay, ABI, Canvas and Windows product smoke. The imported-raster product route keeps Pencil green while drawing black MainLine, then verifies black → opaque-white auto-erase, the explicit Eraser route, Undo/Redo, and erased-state native save/reopen in renderer pixels | Pencil is fixed at 1.0 document pixel; brush/eraser share the documented diameter range |
| `PAINT-003` | Verified | Approved dust/background/full-component semantics, permanent endpoint connection and independent raster thicken/thin/uniform width now have local tool and selection/whole-plane menu routes. Binary, Grayscale8/16 and RGBA8/16, exact changed sets, full-source component sizes, white/transparent/custom backgrounds, inclusive gap/count thresholds, native one-level/alpha=1 custom-background morphology, competing endpoints, local masks, preview/cancel, no-op/error atomicity, one Undo/Redo and native/journal/InkScript replay are covered. Core 29-case audit target, image 9-case target, FFI negatives and actual Windows menu/Canvas pixel assertions are recorded by [Core contracts](../rust/inkpod-core/tests/line_selection_audit.rs), [image contracts](../rust/inkpod-image/tests/line_correction.rs), FFI and Windows line-correction smoke | Verification is scoped to the recorded x64 Debug/native synthetic paths. Physical pressure/touch, all physical DPI combinations and other configurations are unrun. Uniform intersections may exceed the chosen straight-section width; outlines/AA may change |
| `PAINT-002` | Verified | Raster pixel/geometry goldens for line, two-stage curve, rectangle, ellipse, regular polygon, and click polyline; resolved canonical geometry v2, preview begin/update/commit/Cancel, constraints, Undo/Redo/replay/save-reopen, bounded ABI negatives, Windows staged gestures/renderer route, ARM64 geometry confirmation, M15 production snap automatic coverage, and x64 user-visible snap/bypass/zoom confirmation | — |
| `PAINT-004` | Verified | Round/square and pressure footprints; fixed-point smoothing off/on and append batching equality; immutable exact native Binary/Grayscale/RGBA8/16 start-color predicate including alpha, detached same-color hits and selection clip; no-op/invalid/stale/overflow/cancel/Undo/Redo/replay/save-reopen; ABI v8 negatives, Windows Tool Options/Canvas smoke, and x64 Release narrow two-color repair, shape/smoothing, Cancel and Undo manual verification | — |
| `FILL-001`, `FILL-002`, `FILL-003` | Verified | Connected/inclusion/closed-region goldens, tolerance/selection/gap/overflow/cancel/no-op/Undo, FFI and Windows target smoke. Common-raster open retains exact RGBA8/16 source pixels in the protected main-line plane and fills the separate color plane. Fully opaque imports use a SolidWhite paper underlay while imports containing any non-opaque alpha retain Transparent paper. A successful fill selects Color; an explicit user MainLine choice exits Fill while automatic pane refresh keeps an intentional Fill active. Synthetic four-codec tests and the supplied 1754x1240 TGA verify five closed fills, edge overflow abort, unchanged main-line checksum, Undo/Redo and native save/reopen. | Grayscale coverage remains display-only under the MainLine pixel-format contract; imported RGBA main-line pixels use exact nonwhite/positive-alpha boundaries. |
| `FILL-004` | Verified | Dedicated sparse fill-protection tiles, hard-boundary behavior in seed/closed/extension fill, wall-tile proportional allocation, replacement semantics, Undo/Redo/replay/save-reopen and snapshot/cache revision tests; complete Rust suite and x64 Debug/Release plus ARM64 Debug product smokes | — |
| `COLOR-REPLACE-001` | Verified | Exact native-depth raster replacement; pen/rectangle/polyline/lasso and selection intersection; preview/Cancel, no-op/invalid/stale/overflow, Undo/Redo, replay/save-reopen; ABI negatives, Windows production gesture smoke, and x64 Release range isolation/Cancel/Undo manual verification | — |
| `COLOR-001` | Verified | Independent main-line and per-command RGBA8/16 state and restoration, RGB/HSV editing, eyedropper sources, main-line procedures, codecs, history/replay and Color-pane smoke. Yellow Fill → explicit MainLine → green Pencil state → Fill round trips the command colors without replacing the black document MainLine color; actual MainLine Pencil/Brush strokes capture black while Color/Raster strokes capture their tool paint color | Display conversion does not replace stored exact-depth color, and changing an RGBA MainLine drawing color does not recolor existing pixels |
| `COLOR-002` | Verified | Independently persisted palette/chart state; legacy white/native-alpha checks; chart generation/comparison; codecs, history/replay, Windows production routes and ARM64 user-visible confirmation. Current-color and Subpalette registration query the issuing document's palette and reuse the first exact native-depth straight-RGBA match across all groups, including a full palette. Duplicate registration selects the existing row without changing document revision/history/dirty; distinct alpha/depth/component values still append through the existing Rust primitive. `RunPaletteRegistrationSmoke` covers both buttons, group/row selection, repeated registration, exact-depth distinctions, Undo/Redo, and capacity failure atomicity | Existing and file-loaded duplicates are retained. External reference-image behavior is owned separately by `SUBPALETTE-001` |
| `SUBPALETTE-001` | Experimental | ABI v34 retains a standalone read-only Rust catalog/view with no hidden editable Core, Genesis or history. Permanent native bars use an independent accepted-only sticky range and source-scoped reset causes; exact Shift+Arrow/Page pans while unmodified Arrow/Page keeps source navigation. Catalog replacement keeps a stable auxiliary route and advances `presentation_epoch`; visible rejection preserves the old catalog/view/snapshot, while hidden replacement clears the retained old snapshot before deferred publication | Exact post-scroll sampling, durable hidden-publication retry, strict active-image renderer-rejection rollback, complete page/thumb/resize/accessibility evidence, interactive file-dialog and unrun platform checks remain |
| `COLOR-CHART-PREVIEW-001` | Verified | Same-base noncumulative RGBA8/16 extraction; alpha, maximum/quantization/overflow goldens; immutable UUID/revision-bound preview and task ownership; Cancel/invalid/lock/stale/cross-document/failure atomicity; exact-name and cursor retention; one-step Undo/Redo; historical v27 save/reopen; Windows latest-wins statusbar-progress/comparison/Apply production smoke; ARM64 user-visible generation/retry/Cancel/Apply/Undo/save-reopen confirmation | — |
| `COLOR-OUTPUT-QA-001` | Verified | Conservative BT.709 Y′CbCr rational half-up boundaries; exact RGBA8/16 raster visible straight-alpha composite and transparent skip; sparse selection algebra, no-op/Cancel/stale/failure atomicity, Undo/Redo/replay and historical v27 save/reopen; caller-owned ABI negatives; human-readable settings-JSON default-profile field; Windows task/status production smoke; quick/full guard semantic gates and approved x64 wall-clock envelope; x64 Release profile/overlay/pixel-invariance/Undo user-visible confirmation | The sole profile is intentionally labelled as an inkpod conservative QA guard, not formal standards conformance |
| `SEL-001` | Verified | Connected same-color four-neighbor wand and actual trace-band selection pass full-mask assertions. Wand builds virtual line boundaries before exploration: the specified one-pixel border break selects only the 196 interior pixels with gap=1 and reaches outside with gap=0. Source pixels remain exact. Inclusive gap/tolerance, native alpha/16-bit low bits, Binary/Grayscale coverage, multiple gaps, tile/image edges, four selection operations, Undo/Redo, replay and save/reopen pass. Windows actual wand menu/Canvas click checks compare every selection pixel; see [Core contracts](../rust/inkpod-core/tests/line_selection_audit.rs) and [Windows pixel smoke](../apps/windows/app/line_correction_smoke.cpp) | Virtual closure follows the approved facing-endpoint/ambiguity rules; it does not close arbitrary geometry unconditionally. Physical pen/touch, full DPI combinations and other configurations remain unverified |
| `SEL-002`, `SEL-003` | Verified | Raster mode algebra and color/width selection operations remain. Current selection is document-owned; stable-ID named masks support save, replace/add/subtract, rename, delete, Undo/Redo, geometry transforms, native round-trip, ABI and Windows commands without a Selection layer/plane | These mask operations do not implement PAINT-003 existing-line width correction |
| `SEL-004` | Verified | Existing Normal/Tight/EnclosedInterior/Drawing/Boundary geometry contracts remain. The open L fixture matches all 180 pixels and preserves source. Round/square, pressure interpolation, sparse/repeated/clipped/self-crossing samples, four operations, no-op/invalid/stale, Undo/Redo/replay/save-reopen pass. Windows actual Canvas tests cover preview/Cancel, view flip, document-fixed and gesture-start screen-fixed diameter while zoom/pan/flip change mid-gesture; see [Core contracts](../rust/inkpod-core/tests/line_selection_audit.rs) and [Windows pixel smoke](../apps/windows/app/line_correction_smoke.cpp) | Physical pen/touch pressure and all physical DPI combinations remain unverified. Display-only preview is bounded to 128 sampled points; the committed mask uses the complete input |
| `CLIP-001` | Verified | Ordered multi-plane raster clipboard; exact RGBA8/16, origin and tree order; coordinate preservation, converted-plane transaction, external DIB, ownership, Cancel/Undo/replay and Windows grouped copy/paste smoke | Standard import accepts validated 24/32-bit DIB layouts |
| `XFORM-001`, `XFORM-002` | Verified | Destructive document transforms, frame/guide consistency, floating selection lifecycle, retry/cancel/Undo, FFI and Windows dialog/handle smoke | — |
| `XFORM-003` | Verified | Half-open five-anchor absolute X/Y contract; raster scale→clockwise-rotate→position Core path; noncumulative preview/retry/Cancel; canonical-v3 replay and historical v27 save/reopen; ABI negatives; Windows dialog/handle/renderer production smoke; x64 Release 31/31 CTests and user-visible five-anchor dialog/handle, Cancel and Undo confirmation | — |
| `LT-001`, `LT-003` | Verified | Set/item administration plus previous/next/both-N registration; linear distance opacity, chronological z-order, UUID duplicate preservation, preview/Cancel/no-op/invalid/stale/overflow/failure atomicity, one-step Undo/Redo, replay/save-reopen; historical canonical-v2/v27/epoch-24, caller-owned ABI negatives, Windows pane/production smoke, quick/full benchmark gates, and ARM64 user-visible order/opacity/Undo/save-reopen confirmation | — |
| `LT-002` | Verified | Alignment, move/sample/reload/edit-image swap, reference viewer, history isolation, ABI and target-aware Windows smoke | — |
| `SEQ-001` | In progress | The horizontal thumbnail row, natural-order discovery, clean/dirty/recovery fencing, `NOOP`/`BIND`, common resolver and Revert retention remain. All attached source tiles/thumbnails and up to 64 complete editable Core states stay simultaneously resident with COW-shared graph/asset/tile backing; dense decode-cache duplicates are released. A resident switch exchanges outgoing/target state without file job, replay, decode or full rebuild, shows no `セルを読み込んでいます`, and starts dirty autosave as a separate job. Full-catalog CPU preparation and renderer GPU pre-upload survive device rebuild; Canvas selection updates immediately while secondary panes coalesce for 75 ms | First sidecar catalog attachment/recovery still validates and may replay. Current DWM Present timing and unrun platform/manual checks remain |
| `SEQ-ENDPOINT-001` | Verified | Closed Stop/Wrap policy; empty/single/stopped/advanced/wrapped Core plans; natural-order gap skipping; issue-time sequence/source/target identity and stale/failure atomicity; ABI v17 caller-owned plan with short/unknown/unsupported negatives; human-readable settings-JSON `animation.sequenceEndpoint` field with Stop fallback; application-wide menu/shortcut/checked/status route; motion-loop separation; x64 production smoke and user-visible Stop/Wrap, checked-state persistence, restart, and dirty endpoint no-op confirmation | — |
| `SEQ-002` | Verified | Motion Core/ABI state and Windows FPS/loop/pause/step menu and configurable-command routes | FPS, frame, and pause actions remain in the complete shortcut catalog but have no inkpod-specific built-in key assignment; `Esc` remains the standard motion-cancel route |
| `PREF-001` | In progress | Settings are exact-current v5 JSON. Animation retains Sequence thumbnail width and localized validated-sidecar cache MiB with 0–1024 bounds, default 1024 and 0 disable. Apply/OK persists before changing the shared runtime cache, and later shortcut/layout failure restores both; current-only old-version handling is unchanged. The current native v34/replay 29 and ABI v34 carries runtime cache control/telemetry and resident preparation APIs | Physical accessibility checks and unrun configurations remain |
| `SHORT-001` | In progress | The complete production command catalog remains available in the grouped editor with stable command keys, primary/secondary assignments, command/key/context filters, changed/unassigned/conflict categories, record/clear/reset, conflict navigation/clear/swap, dedicated `None`/`Ctrl`/`Shift`/`Alt`/`Win` modifier layers, and physical-key visualization. Existing exact-conflict staging, prefix rejection, action capability validation, Global/Canvas/Timeline/Pane resolution, logical/physical matching, Auto/JIS 109/US ANSI 104 layouts, exact-current v3 bounded human-readable `.inkshortcuts` JSON persistence/import/export, text-focus guard, sequence timeout, Hold restoration, and Toggle repeat suppression remain the implementation base. The current contract replaces the former complete/Q-led built-in preset with sparse Windows/VS Code defaults, adds direct Keyboard Shortcuts and Focus Group 1/2 commands, leaves every inkpod-specific command unassigned without removing it from the editor, preserves valid custom/imported profiles for surviving commands, gives every static menu caption a collision-free English-derived mnemonic, and retains nine direct Window pane toggles before Views and Tabs, Editor Groups, Windows, and Workspace submenus | Focused structural/localization/shortcut verification and both English/Japanese product smokes pass. Palette `1`-`0`/Tab and motion FPS no longer have fallback bindings. Native handling owns bare Alt, unmodified F10, Alt+Space, and top-level Alt-letter mnemonics; new record/rebind rejects them, while existing custom v3 bindings stay valid and round-trip but cannot fire. Alt+F4 remains the standard Exit exception. No global hook intercepts OS-global combinations. A 9-or-more-document dynamic History Visualization paging regression remains |
| `FILTER-001`, `FILTER-002` | Verified | Exact 8/16-bit selection/alpha goldens, catalog, preview/cancel/Undo, shared native statusbar name/progress bar/selector/cancel, ABI and Windows editor smoke | Filter work runs on the Core worker and reports task progress |
| `FILTER-PREVIEW-001` | Verified | Same-base A→B Core acceptance; existing ABI v9 begin/update/apply/cancel ownership and negatives; 120ms dialog notification debounce; one-running/one-pending latest-wins queue with cooperative cancel and issue-time target generation; shared dialog/statusbar job reporting; Windows production smoke with four preview generations, Cancel, one Undo/Redo; ARM64 user-visible confirmation | — |
| `EFFECT-001` | Verified | Gradient/airbrush/boundary/blur/stamp acceptance, deterministic pressure/gesture/bounds, ABI and Windows smoke | — |
| `BATCH-001`, `BATCH-002`, `BATCH-003` | Experimental | Exact-current `.inkbatch` v5 retains the fixed Input → ordered operations → Output graph, natural order/range/dedup, bounded collision preflight and isolated contact-sheet preview. File/folder work uses the shared Rust manager through ABI v34. Issue-time active-document state, cancellation, one-Undo publication and staged new-tab results remain | Physical screen-reader/high-contrast checks and unrun platform/configuration checks remain |
| `BATCH-004` | Experimental | Public authoring remains Color Replace, Move to Color Plane, Masking and Erase. Semantic targets are Color or Raster plane roles; fixed-ID targets and overlap deduplication remain. MainLine fixed IDs now fail before lowering even with missing=skip. All operations use the unchanged `ApplyBatchOperations/canonical-v3` payload and one Undo unit; `.inkbatch` v5/operation v4 and ABI v34 are preserved | Physical accessibility and unrun configurations remain; the MainLine correction is an explicit bug fix, not old-route parity |
| `SCRIPT-001` | In progress | Exact-current language/file/fragment v3, catalog/owner manifest v8 (75 commands), replay epoch 29, native v34 and ABI v34 align. The approved batch/canonical input profiles and folder/active/new-tabs output envelope are typed, bounded and normalized; file v2 and earlier are rejected. Catalog-owned closed Batch variants retain their required/forbidden fields and bounds | Source lifecycle/editor, product `.inkscript` acceptance and the full M17 gate remain pending |
| `SCRIPT-002` | In progress | `apply_batch_operations` reuses one canonical executor for the entire enabled four-operation list. Public [operation contracts](../rust/inkpod-core/tests/inkscript_batch_operations.rs) cover RGBA8/16 independent pixel/mask goldens, all-layer replacement versus first-target other operations, direct/script journal equality, one Undo/Redo, save/reopen/cache-free replay, no-op/error/cancel/stale and source/expanded/pixel bounds. [Binding contracts](../rust/inkpod-core/tests/inkscript_batch_binding.rs) cover initial targets, producer owner checks and Resize parity. Owner v8 assigns all 75 commands exactly once; native catalog fingerprint is unchanged. M3 is complete in its Core/existing-ABI scope after the approved checksum update and original quick gate passed | Product integration and later owner gates remain pending. Allocation-failure injection is unrun; resource-limit tests are not allocator-fault evidence. UUID/owner strict-target rejection is the approved stronger script contract |
| `SCRIPT-003` | In progress | M4 is complete in its Core-only scope: the shared-manager adapter, typed plans, independent dry results, active/new-tab ownership and isolated image preview are covered by public [planning](../rust/inkpod-core/tests/inkscript_io_plan.rs), [shared I/O](../rust/inkpod-core/tests/inkscript_shared_io.rs), [active output](../rust/inkpod-core/tests/inkscript_active_output.rs) and [preview](../rust/inkpod-core/tests/inkscript_image_preview.rs) contracts. Explicit overwrite now succeeds and reopens with the same document digest. Public [authority contracts](../rust/inkpod-io/tests/authority.rs) cover writer exclusion, final-check name changes, cancellation, readonly install failure, exact temporary cleanup and released guards. The [approved save contract](inkscript-overwrite-design.md) uses same-directory atomic rename without TxF | Final-check external name replacement can still win or be overwritten; strict no-lost-update is intentionally not promised. New staged/preview results remain disconnected from C ABI and Windows UI until M5; old adapters reject unsupported staged plans. Product Batch v5 remains. Canonical native-folder explicit-overwrite retains its existing root-versus-child exact-authority limitation. Non-Windows guarded publication has no weaker fallback. Other filesystems, allocator-fault and physical acceptance are unrun. An unrelated existing Windows CoreHost intermittent test failure remains recorded below |
| `SCRIPT-004` | In progress | The canonical exporter now lifts expanded `ApplyBatchOperations` order into one command, preferring typed producer references and using UUID-bound strict selectors otherwise. [Public export contracts](../rust/inkpod-core/tests/inkscript_batch_export.rs) compare exact journal replay, Undo/Redo, save/reopen/cache-free replay, dependency closure, explicit rebound execution and resource/cancel nonpublication. Existing generation-bound C ABI copy/ownership contracts remain; disabled nested references remain in fragment closure | Destination paste/rebind UI, Batch/History clipboard and source-preserving structured editor remain pending. Journal export cannot recover omitted UI grouping, disabled operations or comments |
| `SCRIPT-005` | In progress | Batch v5 and the 75-command InkScript surface retain v34/epoch-29 canonical semantics and separate product routes. M3's approved catalog-8 checksum gate is retained as historical evidence. M4's explicitly approved file-v3 checksum correction passed the original Release quick gate in ten independent processes | Only the approved checksum literal changed; workload, counters, hash walk, timing interval and envelope are unchanged. M4 is complete in its Core-only scope after the approved overwrite repair; this did not change the performance contract. `.inkbatch` v5 remains the product route. M15 cutover and full M17 remain independent and pending |

## Representative verification

Results are scoped to the stated revision and environment. Current local Rust
checks and retained Windows implementation results are separate observations;
document maintenance does not promote unrun Windows or physical-device coverage.

| Scope / recorded environment | Result and limit |
| --- | --- |
| M4 approved overwrite repair, 2026-09-07, source `0733ce05d9f8f60d9f502926e391dba22eb2679b` plus this repair; Windows 26200 / Rust 1.98.0 / LLVM 22.1.8 / ARM64 Debug and local NTFS | Before repair, authority was **5 passed / 2 failed** (including the added guard/cancel regression) and Core shared-I/O was **14 passed / 1 failed**, both exit **101**. The new name-change contract also failed before implementation with sharing error 32. After explicit approval of final fingerprint checks with the documented post-check name-race boundary, overwrite alone permits DELETE sharing while retaining WRITE exclusion. Final fmt, strict workspace Clippy, **916 workspace tests / 0 failed / 2 pre-existing Release-only entries ignored**, warnings-denied workspace rustdoc and all **9 Core Release quick scenarios** passed, exit **0**. This includes authority **9/9**, M4 plan/shared-I/O/active/preview **37/37**, save/reopen and **22 registry/route contracts**. The ARM64 quick run verifies unchanged semantic counters/checksums; it does not establish a new timing envelope or replace the previously approved x64 InkScript gate. Local links (121 targets and two design anchors), whitespace and independent code/contract review passed. No new test was ignored; no version, workload, checksum literal or envelope changed. Core-only scope and the completed user decision require no new manual product-UI acceptance; M5 and broader acceptance remain pending. |
| M4 repair Windows boundary verification, same source; Windows ARM64 host / x64 Debug execution / MSVC 19.51.36256 / Rust 1.98.0 / static CRT | Initial configure failed to execute the existing WinGet Ninja entry. A verified VS-bundled Ninja 1.13.2 override in the generated cache made configure and the entire build pass, including C11/C++20, static CRT and packaging. x64 public authority **9/9** and shared-I/O **15/15** passed. Selected CTest was **6 passed / 4 failed** under restricted permissions; the same failing binaries/fixtures/thresholds under normal permissions passed private InkScript engine route, file authority and ABI smoke, but CoreHost still failed. The identical CoreHost binary then passed a standalone exit-code diagnostic (0); its intermittent failure is **unresolved**, not erased by that success. Its test contains no InkScript call and cannot reach the changed shared publication path, so it is separate from the verified M4 overwrite contracts. All launched sessions exited and final process inspection found no retained CTest/Inkpod/target-test process. Full CTest, visible smoke, physical input, Windows Release, other-filesystem/fault coverage and M5 are not established by this run. |
| InkScript M3 functional verification, 2026-09-06, source `31bd657a9db55b8a9585f14aa249cc01ad17b988` plus M3 changes, Windows 26200.9278 / Rust-Cargo 1.98.1 / LLVM 22.1.8 / MSVC 19.51.36256 / x64 Debug / static CRT / native build 148; separate Core Release quick run | fmt, strict all-target workspace Clippy, **858 workspace tests** (0 failures; two pre-existing Release-only entries ignored in Debug), warnings-denied all-workspace rustdoc, generated reference and registry/route checks passed. This includes 12 new operation, 3 binding, 4 export, 2 format and one new FFI contract. All nine unchanged Core quick scenarios passed. A pre-existing I/O rustdoc link was corrected after its first documented failure. Windows configure initially could not execute the WinGet Ninja link; using the VS-bundled executable in the generated cache made configure and all 210 build steps pass. Restricted-environment CTest passed 43/50 and failed 7 (authority IO_ERROR, no Present/DXGI occlusion); the same 7 binaries/fixtures/thresholds passed under normal Windows permissions, including private engine/authority/ABI, renderer/sequence and English/Japanese visible product Batch smoke. Original failures are retained as environment-specific evidence, not erased or classified as resolved intermittent failures. Tracked wrapper/CTest/Inkpod processes all exited. The optional Python JSON Schema validator was unavailable; the repository's 20 registry contracts passed. ARM64, Windows Release, allocator-failure injection and manual physical acceptance were not run; product cutover and full M17 are pending. |
| InkScript M3 checksum decision and accepted quick gate, 2026-09-06, same approved current x64 Release/static-CRT/Balanced environment, catalog v8 | The original quick gate's exit **101** at actual `8c5b98c2c721c868` / expected `3568e2ed6fb803d5` is retained. Before approval, the unchanged diagnostic ran one warm-up plus nine independent samples, all exit 101 with **only checksum drift**, identical counters and median **89,006,200 ns**. After explicit approval of exactly one checksum literal, the original gate passed **all ten independent processes**, with retained median **88,762,100 ns**, all nine samples within the unchanged 64–107 ms range and every non-time field identical to the diagnostic. [All samples, counters, cause and applied correction](core-benchmark-baseline.md#m3-catalog-v8-checksum-decision) are recorded. M3 is `[x]` in its Core/existing-ABI scope; no new product UI needs manual acceptance under INKSCRIPT 16.2.8–9. This follow-up changes only the test expectation and documents; earlier workspace/Windows results above were not repeated. Product acceptance, unrun configurations and full M17 are not promoted to Verified. |
| Same M3 checksum-approval follow-up, local verification | fmt, **21 registry/route tests**, generated reference, 61 local links/anchors in the three edited documents, milestone markers and diff whitespace checks passed. An exact comparison against HEAD confirmed the performance source differs only in the existing catalog header update and the approved checksum literal. Independent read-only review found no workload, counter, hash, interval or envelope changes. M4–M18 remain untouched. |
| InkScript M4 Core-only functional verification, 2026-09-06, source `35e6366d3b2ec0fca7b4ab60bd4d2af249778c2a` plus M4 changes; same Windows 26200.9278 / Rust-Cargo 1.98.1 / LLVM 22.1.8 / x64/static-CRT environment | Final `cargo test --workspace --all-features --no-fail-fast` exited **101**: **911 passed, 2 failed, 2 pre-existing Release-only entries ignored**, across 39 suites including doctests. The only failures are the required overwrite successes described below. fmt, strict all-target/all-feature workspace Clippy, warnings-denied all-workspace rustdoc, generated reference and 22 registry/route contracts passed. New public suites include plan 10, output validation 3, active output 6, image preview 6 (including 14 read-fault classifications) and shared I/O 14 passed / 1 failed. Tests cover no-op/cancel/stale, atomic nonpublication, ownership/drop, one Undo/Redo, both savepoints/path, save/reopen/cache-free replay, source-copy isolation, cleanup failure and bounded output. No allocator-failure injection was run; bounds are not allocator-fault evidence. |
| M4 shared filesystem authority before the 2026-09-07 repair, same environment | Shared Rust I/O pins original output ancestors through preparation and publication, creates/reopens/cleans temporary files relative to retained parent handles, excludes temporary writers, rejects collision and junction/ancestor replacement, and retains completed prior items. The moved-created-subtree regression failed before its correction and now passes; install has no fallible post-publication check. IO tests total **80 passed / 1 failed**. Required `shared_explicit_overwrite_remains_supported_and_aliases_fail_before_work` and `guarded_publication_installs_once_and_rejects_collision_stale_digest_and_parent` still expect successful overwrite and fail with `UnsupportedAtomicOverwrite` / `UnsupportedAtomicPublication`; their dependent post-success assertions remain unreached. Holding a source handle that denies external write/delete caused the attempted POSIX replacement on this NTFS environment to return `STATUS_SHARING_VIOLATION` (`0xc0000043`). [Windows sharing rules](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilew) apply until handle close and include rename in delete access. Releasing that guard would restore a lost-update race; no such fallback, driver or TxF implementation was introduced. At that point the successful-overwrite requirement was unresolved and no contract reduction was approved. The explicitly approved 2026-09-07 repair and its current limits are recorded above. |
| M4 Windows boundary and visible-path verification, MSVC 19.51.36256 / x64 Debug / static CRT, builds 149 and 150 | Initial build-149 configure/build passed; restricted CTest passed **43/50** and failed 7 with private authority IO_ERROR or Present/DXGI conditions. The **same 7 binaries/fixtures/thresholds passed under normal permissions**, including private engine/authority/ABI, renderer, sequence performance and English/Japanese visible product Batch smoke. After the final Core/shared-I/O review, build **150** passed all 19 build steps and **47/47 non-local-only CTests** under normal permissions, including C11/C++20 headers, export/ABI, private engine and packaging/CRT checks. The three local-only paths retain their build-149 evidence: subsequent changes add Core-only APIs/backend protection, with no product UI change. Original environment-specific failures remain recorded and are not reclassified as resolved intermittent failures. Windows Release, ARM64, non-Windows execution, physical input/accessibility acceptance, M5 product integration, M15 cutover and M17 full are unrun. |
| M4 verification interruptions and rerun conditions | The initial workspace check found OS declarations outside the private IO backend; they were moved into the backend and all 7 architecture contracts now pass without changing the allowlist. An intermediate workspace invocation caught the new test during a syntax edit; the compiler failure log was retained and the final source was checked again. During final verification, bundled PowerShell wrappers stalled before Cargo was observed running. The identified build child was stopped (native wrapper exit **-1**); queued Rust checks subsequently completed, and direct-shell reruns retained their real exit codes. Windows PowerShell with `-NoProfile` then completed the repository build script. Owned verification wrappers and children were confirmed exited; unrelated app service wrappers were left intact. No startup-stalled command was counted as passing, and the underlying startup cause is not claimed resolved. The optional Python JSON Schema validator was unavailable; the repository registry contracts passed. |
| M4 performance verification and approved checksum correction, 2026-09-06, same current x64 Release/static-CRT/Balanced environment; approval follow-up source `65becd5758ee2f2640cdf80ac218099d95a36be1` plus one test literal and documents | All **nine unchanged Core quick scenarios passed** with their existing checksums/counters before this follow-up. The original InkScript quick gate's exit **101**, actual `d72add6ac2e137c7` / former expected `8c5b98c2c721c868`, including a final-source run, is retained. The unchanged diagnostic completed one warm-up plus nine independent retained samples, all exit 101 with only checksum drift and identical non-time observations; median **86,476,200 ns**. After explicit approval of the one-literal correction, the original gate passed **all ten independent processes**, with retained median **89,397,900 ns**, all nine samples within the unchanged 64–107 ms envelope and every non-time field identical to the diagnostic. [All samples, counters, cause and applied correction](core-benchmark-baseline.md#m4-file-v3-checksum-decision) are recorded. No workload, counter, hash, interval, envelope or version changed. Earlier workspace/ABI/Windows/visible results were not repeated for this literal update. M4 remains `[!]` for its two required guarded-overwrite success-test failures; M5 and product cutover remain pending. |
| Same M4 checksum-approval follow-up, local verification | fmt, **22 registry/route tests**, generated reference, 71 local links including 16 anchors in the three edited documents, milestone markers, sample/log agreement and diff whitespace checks passed. Exact comparison against HEAD confirmed the performance source differs only in the approved checksum literal. No production code, workload, counter, hash, interval, envelope or version changed; no additional manual UI acceptance is needed for this test-only correction under INKSCRIPT 16.2.8–9. This does not complete M4 or replace its unresolved overwrite validation. |
| InkScript M2 accepted quick gate, 2026-09-06, same current Windows 26200.9278 / MS-7E26 / Ryzen 9 9950X3D / Rust 1.98.1 / MSVC 19.51.36256 x64 Release / static CRT / Balanced environment | After explicit approval of the five literals and this environment's unchanged 64–107 ms range, the original gate passed all ten independent processes (one warm-up and nine retained samples). Retained median **86,872,500 ns**, checksum `3568e2ed6fb803d5`; all counters identical and exact, including 384 Commit/384 no-op, 4/1/1 installed/Save/cancel and four cache-free reopens/256 replayed Commits. This resolves the deterministic native-byte mismatch; it does not resolve unrelated intermittent Windows failures or implement the full fixture. |
| InkScript M2 diagnostic, 2026-09-06, source `1c65f9db029de56034e2a501c55ce472f708e28b` plus private test-only observer/document changes, Windows 26200.9278 x64 Release, Ryzen 9 9950X3D/MS-7E26, Rust/Cargo 1.98.1 / LLVM 22.1.8, MSVC 19.51.36256, static CRT, Balanced power | Original gate reproduced exit 101 at 6,192 actual / 5,968 expected before timing, and retained that failure after observer refactoring. Authorized diagnostic warm-up + nine independent processes all exited 101 with nine version-sensitive mismatches and identical non-time fields; median 86,796,100 ns, checksum `3568e2ed6fb803d5`. All non-byte, Save/cancel/nonpublication and cache-free replay assertions passed. Full samples/byte derivation/candidate constants are in the benchmark baseline. This is not an accepted performance result on the current environment. |
| Same M2 change, local verification | Final fmt, strict workspace Clippy and workspace tests passed: **834 passed, 0 failed, 2 ignored** (Release-only original gate and diagnostic; the original gate was run separately above). Final documentation checks also passed: registry 19 tests, generated reference, 99 local links/anchors, milestone markers and diff whitespace. The first Clippy run rejected a constant diagnostic guard; it was removed without suppression. An initial workspace run caught the temporary public-API fixture's milestone-labelled path; that owned scratch path was renamed semantically, and unchanged workspace checks passed. Independent public-API fixtures encoded four 6,192-byte inputs and four 22,896-byte outputs, each with 64 Commit/64 no-op and FullReplay clean reopen; this Debug derivation is not runner/timing evidence. No public API/ABI/format/registry or production code changed. Windows build/native/visible/physical paths, standalone rustdoc and full benchmarks were not run. |
| InkScript M1 contract preparation, 2026-09-06, source `1c65f9db029de56034e2a501c55ce472f708e28b` plus documentation-only changes, Windows x64 Debug, Rust/Cargo 1.98.1 / LLVM 22.1.8, MSVC 19.51.36256, static CRT | Existing Core registry/route/public InkScript tests: 27 passed; Core Batch-filtered contracts: 24 passed; FFI Batch filter: 8 passed; FFI InkScript filter: 6 passed (65 executions, 63 distinct tests). All exited 0 with no failures/ignored tests. Initial `--lib contracts::batch` selected zero tests and is not counted; the integration target was then used. After document edits, all 19 registry tests passed again; generated reference, 92 local links/anchors, milestone markers and diff checks passed. D1–D4 were subsequently approved; M1 is complete as a contract decision, not product acceptance. No implementation/registry/version changed in M1. Full workspace/lint/rustdoc, Windows/physical checks and Release gates were not run in that document-only preparation; M2 evidence is listed separately. |
| Documentation maintenance, 2026-09-03, source `2f0d20f4d8c9fcabd81cdec5f4c4939916bcac3a`, aarch64-apple-darwin, Rust/Cargo 1.95.0 / LLVM 22.1.2 | fmt, all-feature workspace tests (**832 passed**, one existing ignored Release-only test), all nine Core quick scenarios, warnings-denied Core rustdoc, generated InkScript reference and documentation links passed. Semantic benchmark assertions passed; this host has no approved wall-clock envelope. Production algorithms, ABI/format code and benchmark expectations were unchanged. Windows configure/build/CTest and physical checks were not run on macOS. |
| Same local source, strict Clippy | Exit 101: existing `clippy::collapsible_match` diagnostics at `rust/inkpod-format/src/inkscript/fragment.rs:735` and `:749`. This source was unchanged; no warning suppression or unrelated code fix was applied. |
| Same local source, separate Release InkScript quick gate | Original failure retained: exit 101 at `rust/inkpod-core/src/script/performance.rs:604`, **6,192 actual / 5,968 expected** during fixture construction before timing/checksum acceptance. The separately approved M2 correction and independent Release success above resolve this deterministic mismatch; Debug success alone did not cover it. |
| Line correction and selection, 2026-09-03, Windows x64 Debug, native v34/replay 29/ABI 34 | fmt, strict workspace Clippy, 834 Rust tests, warnings-denied rustdoc, all nine unchanged quick Core scenarios and MSVC configure/build passed. One existing Release-only InkScript test was ignored in Debug. Core 29-case and image 9-case targets, FFI failure/ownership and canonical/export/replay cases passed. |
| Same implementation, actual Windows menu/Canvas | English/Japanese product smokes passed (346.91/351.02 s); the complete CTest run was **48/49**, with RendererHost Sequence first-edit readback failing with `S_FALSE` and 21 latency timeouts. Later isolated RendererHost/ABI passed (67.39/45.27 s) without changing assertions or waits. The intermittent cause remains unresolved. |
| Final RGBA16 alpha=1 / one-level-contrast correction | All Rust gates, MSVC rebuild and isolated RendererHost/ABI were rerun successfully. Full English/Japanese product smoke was recorded before this final pixel-comparison correction and was not rerun afterward. ARM64/Windows Release, non-Windows and physical pen/touch/DPI were not revalidated in that implementation run. |
| Resident Sequence performance, earlier 2026-09-03 ARM64/x64 Release normal-desktop runs | Each run covered 128 A/B/A and A/B/C/B/A warm switches with zero reads/decodes/uploads/timeouts and one snapshot per switch; full Release CTest passed at that revision. The separate 1-ms UI-handler p95 goal remained unmet. This does not establish current-version, edited/recovered, first-sidecar or physical scanout latency. Reproduction: [sequence-switch-performance.md](sequence-switch-performance.md). |
| Right-pane same-parent Structure/layout | Actual HWND tests and localized product smoke covered add/remove resize, final pixels/update regions, unchanged control identity/list contents/selection/top index and valid scroll retention. Inner failure and rejected command preserve state; outer OS-level placement/rollback failure injection remains pending. |
| Current release package record, v0.3.2 / commit `404b2d9fbde9fed71cfc8a85dc98031a5a960329` | x64 build 146 and ARM64 build 147 packages passed static-CRT/version/payload checks, and x64 CTest passed 50/50 at the tagged release revision. An earlier x64 build 145 full run passed 49/50 with one English product-smoke status-I/O keyboard failure; the same test then passed alone and the replacement clean build passed fully, without changing code or acceptance conditions. The intermittent cause remains unresolved. ARM64 native execution, administrator MSIX lifecycle and physical accessibility/DPI/IME were not repeated. |

## Cross-cutting limits

- Common-raster PNG/TIFF codec and pair contracts retain 16-bit pixels, but current
  full product normal-pair Save/reopen evidence for both 16-bit formats remains pending.
- Pair save is one logical transaction with bounded journal/backup recovery, not an
  atomic replacement of two filesystem objects. Unresolved journal/cleanup evidence
  keeps a destination busy. A nonparticipating process may still race the final
  alias scan; portable non-Windows replacement retains its documented narrow ABA window.
- Namespace-observer-backed inventory reuse is Windows-only. Other backends
  re-enumerate; a future sandboxed frontend still needs a file-authority adapter.
- Shared image I/O does not cover GUI icons, palette/chart, shortcut/settings,
  clipboard memory images or test fixtures. Native compression accepts code 0 only.
- `revision-max` retains the scalar-aliasing and transparent-recomposition limits
  explicitly accepted in SPEC. These are not silently replaced by a stronger cache key.
- Physical high contrast, 200% DPI, screen-reader, IME and pen/touch coverage must
  be recorded independently. Reference Check AutoHide remains unreachable by F6/Tab.
- InkScript has private Rust/ABI/application smoke routes but no product file
  filter, command, pane or clipboard route. Full performance/cutover gates remain
  pending; independent `.inkbatch` v5 is not an InkScript product acceptance result.

## Maintenance rule

Keep one row per SPEC requirement. Replace affected evidence and known gaps;
do not append task histories or duplicate this table in another status document.
Keep only representative results with revision/configuration and explicit limits.
Preserve exact benchmark samples and approved gates in the baseline document.
