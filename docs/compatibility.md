# Compatibility status

Compatibility means operation semantics, data separation, coordinates, and
saved results—not replication of a legacy UI or assets.

| Requirement                   | Status      | Implementation                                                                                                                                                                                                                                                                                                     | Tests                                                                                                                                                                                                                                                                     | Known difference / next work                                                                                                                                                 |
| ----------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ARCH-001                      | Verified    | Rust crate roots and Core vector/Batch codec/transform/view module indices are declarative indices; Core/image/format/FFI use responsibility-specific submodules, Core private-invariant tests are colocated and `cfg(test)`-gated, and configure-aware recursive CMake tracking connects every production Rust source to Cargo `inkpod-ffi` staticlib/rlib byproducts | Architecture guard checks every root and the Core responsibility split, enforces inline-test gating and CMake source tracking, and Core public workflows compile as a separate integration crate; format, zero-warning clippy, workspace tests, strict Windows builds, and CTest | Cargo is not invoked when tracked inputs are unchanged; Windows CI is pinned to the Windows Server 2025 image with Visual Studio 2026                                         |
| ARCH-002                      | Verified    | Safe, OS-independent Core/image/format crates with no frontend dependency                                                                                                                                                                                                                                          | All three domain crates' source/manifest scan, clippy, workspace tests                                                                                                                                                                                                    | No Rust Windows API dependency                                                                                                                                               |
| ABI-001                       | Verified    | ABI v3 exposes 196 semantic fixed-layout functions. Its 21 new exports provide type/generation-tagged Core/snapshot/task/color/sample/raster/thumbnail/export IDs, bounded registration/copy/release, and a pointer-free opcode/schema primitive request for the existing canonical procedure slice. Legacy per-operation wrappers and the v3 request share `Core::execute_primitive`; EditorDefaults/EditorState and the remaining established records retain their tested contract. | Header/export/catalog parity and a direct contract-test reference for all 196 exports; source-derived route ownership; C11/C++20 layouts; ABI/application smoke; Rust NULL/short/nested-structure/unknown-enum/opcode/schema/stale/wrong-type/wrong-generation/no-op/double-release/ownership/atomicity tests; caller-buffer mutation after registration; Windows typed-queue saturation, active-stroke, close, and shutdown tests. | ABI v1/v2 callers must update the header and rebuild. Unmigrated production procedure families remain isolated behind the M6 legacy invoke bridge; production `.inkpod` remains exact-current v2 until M8. |
| ABI-002                       | Verified    | Immutable snapshot owns batched raster/overlay/vector data. ABI v3 can retain it by generation-tagged ID and copy pointer-free metadata, tile descriptors/pixels, guides, segments, fills, and boundary IDs through bounded owner-thread calls; the established opaque snapshot/RendererHost path continues to move ownership through a bounded session/view/Canvas/generation/revision envelope and releases on replacement, rejection, close, and shutdown. | Core zoom invariance; Rust FFI ID/type/generation/batched-copy ownership; C11/C++20 and ABI smoke; C++ validation and D2D smoke; RendererHost structural/native tests cover two-surface routing, revision/stale rejection, queue replacement/full failure, close/shutdown release, and device-loss reconstruction. | Vector records use document coordinates. Legacy opaque borrowed views last for the snapshot lifetime; v3 caller buffers are borrowed only for one bounded copy call and the ID must be released exactly once on the Core owner thread. |
| IO-001 (native save)          | Verified    | `.inkpod` v2 uses bounded semantic `DOCM`, `LTBL`, `VECT`, and `ADJT` sections; adjustment records persist stable IDs plus brightness/contrast, curve, or levels parameters | Adjustment order/parameter/composite save-reopen; exact current-version round-trip; earlier/later version and native missing/duplicate/wrong-layer/invalid-parameter rejection; M3 separately tests the target canonical EDIT frame/DTO round-trip and digest; M4 tests pre-I/O rejection and sentinel-file preservation for an asset-backed Genesis | Production v2 intentionally omits the successor `GENS`/`ASST` graph and EDIT, so it cannot restore M4 immutable asset retention or M3 EditorState. A raster-open asset Genesis is therefore not natively saveable until M8 and returns `INVALID_STATE` before I/O; a representable v2 normal save advances only the document savepoint and cannot clear editor dirty. Successor persistence/reopen remains M8 and is not end-to-end Verified |
| IO-001 (recovery)             | Verified    | Atomic normal save/autosave preserves the existing file and moves path/savepoint only after replacement; recovery opens dirty and pathless; Windows assigns never-saved cells a private recovery path, queues timer/manual autosaves, defers them across an active stroke, and discovers private recovery at startup | Core/FFI recovery and failed-save tests plus Windows active-stroke autosave, private-path discovery, normal-file/recovery checksum, replacement-failure smoke, and asset-Genesis autosave rejection; failed replacement preserves dirty, revision, path, and recent files | Only the newest private recovery is prompted per launch; deferred files remain available for a later launch. Asset-backed Genesis recovery persistence is M8 and is explicitly rejected before I/O in v2 |
| SESSION-001                   | Verified    | The bounded `DocumentRegistry` owns multiple `DocumentSession` values, each with one file/recovery shell, canonical file or untitled identity, and one session/generation-keyed Core binding. `CoreHost` owns one independent Core handle per binding on its owner thread and separates document state, history, dirty/savepoint, input, pending work, diagnostics, and notification routing. G5 exposes those sessions as document tabs; G6 can place their views in either of two visible editor groups; G10 and G11 move, duplicate, or tear out views across workspace windows without moving or duplicating a Core handle. New/Open/Import/Recovery create sessions, duplicate identities select the existing session, Save As conflicts are rejected before writing, and successful saves update identity/recent/recovery presentation together. View/group/document/window/application close scopes are distinct; a window close prompts only for dirty sessions whose final view is removed, while only the last workspace starts application shutdown. G12 writes bounded current-version metadata per autosave with session/generation, document UUID, original identity/path, source, and time; startup enumerates all candidates, retains metadata-invalid artifacts, and offers independent restore/discard/defer. Previous normal paths are restored only through a separate explicit default-off setting. | Core/FFI savepoint, recovery, malformed-open, panic-containment, and save/reopen tests; G3 native CoreHost isolation/race/thread tests; G4 RendererHost routing/lifetime tests; G5 owner/application smoke covers two-document edit/Undo/Redo/selection/dirty/save-reopen isolation, file-ID/path/case/hard-link duplicate identity, Save As conflict, recent-file cleanup, close scopes, and exact dirty-prompt counts; G6 smoke verifies two-group same/different-session routing, active-stroke cancellation, and dirty-safe group close; G10/G11 smoke covers cross-window session sharing and placement invariance; G12 native recovery tests cover metadata/path-record current-version round-trip, malformed rejection, all-candidate ordering, and individual discard, while GUI smoke covers two dirty-session autosaves, crash-style recovery, savepoint/Undo/Redo/save/reopen, and normal-save cleanup; x64 Debug/Release CTest pass | Long operations still serialize on one CoreHost lane; per-session queue telemetry now measures the delay and a native cross-session test proves accepted input is retained, but the fixed scenario baseline and any justified worker split remain G13 work |
| IO-002                        | Verified    | PNG/TIFF/TGA/BMP preserve supported straight-alpha depth, dimensions, and DPI and are available from native Open/Import/Export dialogs with alpha/white export plus atomic mixed-format sequence import/export; raster-open owns a canonical Genesis asset independent of its encoded source/path; opening a numbered raster builds a natural-order preview from matching numbered siblings without including unrelated numbered images | Four-format round-trip/edge cases, canonical-format flat Genesis semantics, cross-codec identity, caller-buffer/file deletion followed by replay/export, mixed-format sequence ABI contracts, and Windows raster/sequence open/file-preview/menu smoke | Native `.inkpod` save of a raster-open asset Genesis is explicitly unavailable until M8; common-raster export remains available |
| DOC-001                       | Verified    | CellDocument paper/DPI, 100/reference/drawing/safe frames, margins, image size, and resolution are editable through native creation/property dialogs                                                                                                                                                               | Metadata equality, mixed-paper alignment, native round-trip, and Windows dialog smoke                                                                                                                                                                                     | —                                                                                                                                                                            |
| DOC-002                       | In progress | Stable-ID typed raster/vector-coloring topology remains connected through Core/C ABI and the Windows pane controller; the DockHost-managed Layer pane shows separate layer and plane tiles with aspect-preserving layer thumbnails, name/type metadata, visibility/editability, active selection, and a resizable split through dock/float/hide/restore; selecting a generic raster keeps its stable plane ID as the paint/fill target | Core/format topology/save-reopen, Core and ABI thumbnail contracts, selected-raster preview/commit/Undo/Redo/fill targeting, pure dock/layout calculation checks, and Windows layer/plane DockHost smoke | Explicit multi-target editing presentation remains                                                                                                  |
| DOC-003                       | In progress | Native menu commands retain transactional create, duplicate, delete, reorder, show, edit, opacity, convert, merge, and hidden cleanup; the dockable Layer pane forwards the same command IDs for the active layer or plane and supports direct drag reordering in both lists. New-plane kind and storage format use standard text dropdowns; OK performs read-only Core topology validation and keeps the owner-centered common modal open on invalid combinations | Core raster/vector tree and prospective-validation tests, ABI no-mutation validation/reorder/thumbnail coverage, and Windows multi-dropdown/validation/centering plus layer/plane action, selection, reorder, and dock/float/hide/restore smoke | Multi-target editing remains |
| HIST-001                      | Verified    | Savepoint-aware transactions, Undo/Redo, whole revert, multi-step history selection, partial layer/selection revert, and exact preview cancel/apply remain native workflows. For the four executor-backed procedures—main-line color, palette replacement, raster stroke, and raster-asset import—Core also owns an append-only `Commit`/`HistoryMove`/`BranchCut` runtime journal, StateId savepoint, retained inactive branches, and optional inverse/COW history cache rebuilt from Genesis plus procedures and a detached asset archive. Unmigrated document edits explicitly mark the journal incomplete instead of creating a synthetic snapshot/diff procedure. | Core history/preview regressions; ordered event, branch-cut atomicity, StateId savepoint, no-ID-on-no-op/stale/cancel, cache-release/rebuild, detached-asset non-sharing, incomplete-journal rejection, and fixed-seed full-replay tests; Windows history-dialog/partial-revert/title/dirty smoke | `.inkpod` v2 does not persist the runtime journal/assets or reopen history. Cache-free replay is available only while every current commit belongs to the four migrated canonical procedures; remaining routes migrate under M6. |
| VIEW-001                      | Verified    | Anchored zoom, Canvas box zoom/pan, fit, 1:1, numeric zoom, viewport resize, and both view flips use independent view revision; the status bar persistently reports zoom                                                                                                                                           | Core mode/box/resize/flip, FFI transform, and Windows gesture/dialog/DPI/status smoke                                                                                                                                                                                     | The main-window zoom slider is intentionally removed                                                                                                                         |
| VIEW-002                      | Verified    | Ruler, guide add/move/delete, grid settings, snap, and transparent view are Core-owned native controls rendered from snapshot overlays                                                                                                                                                                             | Core persistence/snap, C ABI overlay, and Windows menu/dialog/render smoke                                                                                                                                                                                                | —                                                                                                                                                                            |
| VIEW-003                      | Verified    | The six-part status bar and modeless locator expose document X/Y, selection H/V/L, straight RGBA8/16, a bounded 9 x 9 magnified neighborhood, follow/pinned target header, one-pixel fixed editing, and optional edge scroll. Actions retain the click-time session/view context; stale asynchronous results do not update a new active tab. | Core DPI/flip/half-open coordinate and bounded read-only neighborhood tests; FFI size-query/short-buffer/invalid-radius contracts; C++ ABI smoke; pane-policy focus/close tests; native modeless owner/accessibility/menu/pin/fixed, asynchronous neighborhood, invalid edit no-op, and Undo/Redo smoke | — |
| VIEW-004                      | Verified | Same-document Core views share one `DocumentSession` Core binding, document state/history, dirty, and savepoint. Each `DocumentView` binds a strong frontend ID to a Core-local view ID and owns separate presentation state. One or two visible editor groups each own their tabs and one Canvas. G11 adds native tab reorder, group/window drop, Ctrl-copy/new-view, and window-exterior tear-out through the same application operations as menu, context-menu, and keyboard commands. Commit revalidates captured workspace/group/session/view/generation and exact source index; cancellation or failure restores placement without retargeting. `CoreHost` maps frontend views to Core-local views and fans a per-view immutable snapshot to each matching visible Canvas. Every newly created workspace primary Canvas and split Canvas is registered exactly once; group close unregisters its sink and workspace close atomically unregisters all affected sinks before destroying their `HWND`, so publication cannot call a released Canvas. G13 reserves Ctrl+Tab/Ctrl+Shift+Tab for tab traversal, Ctrl+F6/Ctrl+Shift+F6 for group traversal, and Ctrl+F4 for closing the captured view, including while an edit control owns focus. | Core multi-view revision/history tests; owner-model EditorArea split/clamp/reorder/ordered-move/merge and multi-workspace tests; G3 CoreHost and G4 RendererHost isolation tests; G5 tab smoke; G6 command-context/state, CoreHost fan-out, RendererHost unbind, and application smoke cover independent flip, shared selection/history/dirty, different-session isolation, keyboard/Canvas/tab focus, active-stroke cancellation, resize/DPI, move/new-view, and group close; G10/G11 cross-window lifecycle and stale-context smoke; G13 resource telemetry requires registered snapshot sinks to equal visible editor groups and closes a split workspace immediately before snapshot publication; native ARM64/x64 Debug/Release GUI smoke and five repeated x64 Release runs pass | — |
| PAINT-001                     | Verified    | UI/Input, Core engine, and Renderer are distinct; pencil/brush/eraser/auto-erase/pressure samples stream through a bounded queue and preview at frame cadence; the eraser tool options explicitly show `主線`/`彩色` targets and keep them synchronized with the active plane palette row, menu commands, and status; an active generic raster keeps its stable plane ID through preview, commit, and history; raster hit coordinates use the same half-open pixel cells as the locator and nearest-neighbor Canvas at every zoom. One bounded raster stroke is now validated, canonicalized, executed, and replayed by the single Rust primitive kernel, and the established stroke APIs delegate to that owner. | Core tool/resource tests including selected-raster targeting, 6400% device clicks, the final document cell, and flipped views; `primitive_kernel` request/replay digest, atomicity, ID/revision/history/dirty/cache, and tile-order coverage; 256-record FFI test; Windows eraser-target control/palette synchronization, layer-selection target, thread-ID/multi-sample/live-preview, 6400% auto-erase, diameter state/layout/font, and real Canvas-focus commit smoke | The current UI uses one shared 0.1–256.0 document-pixel diameter for brush/eraser; pencil is fixed at a read-only 1.0, and the color picker remains RGBA8                                          |
| FILL-001                      | Verified    | Connected seed fill exposes tolerance and selection clipping through its native option dialog and Canvas tool; a committed standard fill selects the actual coloring plane across the inspector and status, an active generic raster retains its stable plane ID, and a zero-pixel no-op retains the prior selection                                                               | Image goldens, Core selected-raster/no-op/Undo and FFI transactions, and Windows dialog/Canvas-click/standard-generic-plane synchronization smoke                                                                                                                          | —                                                                                                                                                                            |
| FILL-002                      | Verified    | Inclusion/exclusion colors, overflow abort/reporting, gap-close axes/value, and detached regions are native fill options                                                                                                                                                                                           | Required inclusion/gap/overflow/cancel/no-op goldens and Windows option routing                                                                                                                                                                                           | Gap close is the documented native deterministic rule                                                                                                                        |
| FILL-003                      | Verified    | Closed-region range gestures, transparent-only/inclusion behavior, and fill extension are available from native Canvas tools                                                                                                                                                                                       | Closed/open/colored/transparent-only/cancel/extension tests plus Windows gesture smoke                                                                                                                                                                                    | —                                                                                                                                                                            |
| SEL-001                       | Verified    | Rectangle, ellipse, lasso, and polyline expose a closed drag outline; trace exposes its diameter while dragging; wand applies immediately; all six commit to the persistent mask only when their gesture completes. A lasso/polyline gesture with fewer than three points is an empty candidate: new/intersect clears the mask and add/subtract is a no-op without an error                                                                                                  | Core authoring/property, C ABI validation, Windows all-tool gesture smoke, transient-preview revision/cancel/end/tool-transition checks, and short-lasso empty-candidate regression                                                                                                                              | —                                                                                                                                                                            |
| SEL-002                       | Verified    | New/add/subtract/intersect, invert, configurable expand/shrink, and equal/different typed color selection are native operations                                                                                                                                                                                    | Exhaustive mask algebra plus Core/Windows mode/width/color smoke                                                                                                                                                                                                          | —                                                                                                                                                                            |
| SEL-003                       | Verified    | Raster selection-layer conversions and all eight vector selection modes are exposed by native menus/tools                                                                                                                                                                                                          | Deterministic all-mode Core test, ABI count-query buffers, and Windows conversion/mode smoke                                                                                                                                                                              | —                                                                                                                                                                            |
| PAINT-002                     | Verified    | Line, curve, rectangle/ellipse, and polyline Canvas tools render an immutable geometry preview and commit atomically                                                                                                                                                                                               | Core path/Undo, C ABI draw, renderer preview, and Windows gesture smoke                                                                                                                                                                                                   | —                                                                                                                                                                            |
| PAINT-003                     | Verified    | Gap connect, three-mode dust removal, and four line-width corrections have native command/dialog/gesture paths                                                                                                                                                                                                     | Vector transactions, dust preview/cancel/atomicity, ABI, and Windows smoke                                                                                                                                                                                                | —                                                                                                                                                                            |
| VECTOR-001                    | Verified    | Fixed-point cubic paths, variable widths, vector main-line/color-trace/fill topology, trace-before-main paint order, immutable snapshot, and Direct2D path/outline renderer with continuous seams and width-preserving bounded-miter joins                                                                         | Image geometry test; Core zoom/order/golden/save tests; Rust FFI + compiled C11/C++20 renderer and closed-seam/corner geometry smoke                                                                                                                                      | Arbitrary raster/vector layer interleaving still composites vector content after raster tiles                                                                                |
| VECTOR-002                    | Verified    | Native tools cover draw, partial/intersection/full erase, connect, four width modes, all selection modes, vector-to-new-raster-layer rasterize, and raster-to-vector conversion; draw tools are enabled only for vector main-line/color-trace planes                                                               | Vector acceptance, bounds/all-mode/transaction, FFI conversion, Windows command/Canvas smoke, and initial-cell command-state/diagnostic tests                                                                                                                            | Rasterize preserves the source vector layer and commits one Undo unit                                                                                                        |
| FILTER-001                    | Verified    | Fixed sharpen/blur, bounded Gaussian/unsharp, channel invert, and alpha-independent auto contrast support RGBA8/16 with selection clipping; every catalog entry is exposed through a native editor/preview path                                                                                                    | Exact 8/16-bit alpha/selection golden; catalog/image tests; Core/FFI/Windows menu smoke; task progress/cancel                                                                                                                                                             | Filter work runs on the Core engine worker; the UI polls a thread-safe task and never blocks the UI thread                                                                   |
| FILTER-002                    | Verified    | Brightness/contrast, channel Bezier/B-spline curves, levels, HSV, and color balance use normalized 16-bit clamping and one final round; native editors expose parameters, channels, interpolation, and editable curve points | Preview/Undo acceptance, ABI validation, all-feature workspace regression, Windows editor smoke | — |
| EFFECT-001                    | Verified    | Linear/radial 3–16-stop gradient with alpha/dither/45-degree constraint, pressure/fade/spacing/continuous airbrush gestures, boundary-only airbrush, screen-fixed pressure-sensitive blur regions, and round/square pressure-sensitive offset stamp gestures cross image/Core/C ABI/native Canvas | Effect acceptance, deterministic gesture/pressure, extreme-coordinate and boundary tests, Rust/C++ ABI, and Windows editor/gesture smoke | — |
| ADJUST-001                    | Verified    | Stable-ID brightness/contrast, curve, and levels layers compose in palette order with visibility/opacity without changing source pixels, persist in `ADJT`, and update through C ABI; Windows supports multiple create/select/re-edit/toggle/reorder; alpha edit/gradient and grayscale alpha view never alter RGB | Order/source/opacity/visibility/save-reopen acceptance; malformed metadata; alpha RGB-preservation; Rust/C++ ABI and multiple-adjustment Windows smoke                                                                                                                    | —                                                                                                                                                                            |
| CLIP-001                      | Verified    | Private typed plus standard CF_DIBV5/CF_DIB clipboard paths preserve document coordinates; Cut and compatible/selected/converted Paste choices are native commands                                                                                                                                                 | Core cross-paper/failure, ABI ownership, and Windows private plus DIB-only external clipboard smoke                                                                                                                                                                       | Standard import accepts validated 24/32-bit DIB layouts                                                                                                                      |
| XFORM-001                     | Verified    | View-only flips remain separate from destructive horizontal/vertical mirror, 90-degree rotate, image/paper size, and resolution dialogs                                                                                                                                                                            | Core pixel/frame/guide/history and Windows command/dialog revision smoke                                                                                                                                                                                                  | —                                                                                                                                                                            |
| XFORM-002                     | Verified    | Floating selection move/scale/rotate has dialog and Canvas-handle preview plus exact commit/cancel                                                                                                                                                                                                                 | Core transform/retry/cancel, FFI lifecycle, and Windows dialog/handle gesture smoke                                                                                                                                                                                       | —                                                                                                                                                                            |
| SHORT-001                     | Verified    | Every one of the 331 production menu commands has a displayed configurable 1–4-stroke binding; conventional/direct keys, prefix-free categorized sequences, persistence, exact-conflict exchange, text-focus guard, pending status, and reset are connected                                                        | Core prefix/conflict/reset; FFI set/copy/pure-resolve; Windows full-catalog/menu/editor/single+multi key smoke                                                                                                                                                            | Palette `1`–`0` remains a no-match fallback                                                                                                                                  |
| COLOR-001                     | Verified    | Native RGB/HSV/alpha editing preserves RGBA8/16; the DockHost-managed Color pane uses an anti-aliased cached hue ring with a hue-rotating SV triangle, a checkerboard opacity slider, font-metric-aligned values, exact numeric fields, and a text-labeled eyedropper button that delegates to the shared tool command and configured source through dock/float/hide/restore. Pane resize invalidates the old layout and the size-dependent owner-drawn picker so a tab switch is not required to repair the display. Exact-depth main-line color mutation now uses the canonical primitive executor and fresh-Core replay path through its established public wrapper. | Exact-depth Core/FFI plus Windows editor/source/Canvas smoke; Core covers undoable binary/grayscale main-line base colors; `primitive_kernel` canonical execution/replay and semantic document-digest coverage; Windows DockHost smoke covers picker resize full-invalidation, keyboard updates, RGBA16 depth/alpha preservation, accessible labels, swatch target switching, eyedropper routing, and reparented-pane operation | BGRA8 display conversion never replaces stored exact-depth color                                                                                                             |
| COLOR-002                     | Verified    | Palette, chart, subpalette, color-check models, menu commands, and number/Tab shortcuts are connected. The Color pane exposes its follow/pinned target and routes palette select/register/delete/clear/load/save and main-line mutations to that exact session/generation. The modeless subpalette displays the naturally ordered registered/current/adjacent cell in an independent zoom/pan/flip viewport, supports automatic previous-cell and editor-scroll synchronization, samples without editing, and registers the sampled drawing color through the shared palette command. Ordered palette replacement now uses the canonical primitive executor and fresh-Core replay path through its established public wrapper. Palette content remains document-owned while the per-session cursor is EditorState-owned. | Core palette/subpalette, exact-depth view-sample, independent reference-snapshot and document/history isolation, binary/grayscale main-line Undo/Redo; `primitive_kernel` canonical execution/replay, no-op/failure atomicity, and semantic document-digest coverage; 175-function ABI contract/smoke including EditorState; Windows target header/pin, action-button, shortcut/file, dedicated Canvas, fit/sample, multi-session, stale/close, and dock/float/hide/restore smoke | — |
| LT-001                        | In progress | A modeless target-aware Light Table palette exposes Core-owned set/item administration, ordering, visibility, transform/color/mode, item/global opacity, and previous/next cell navigation. Selection IDs are namespaced by session/generation, and follow/pin actions capture one exact target. | Set/item transaction/opacity/native round-trip and sparse/COW workflow tests; Windows modeless owner/style/accessibility, command-state/catalog, selection, follow/pin multi-session isolation, invalid no-op, Undo, and set/item command smoke | Previous/next-N bulk registration and automatic opacity-step controls remain to be exposed |
| LT-002                        | Verified    | Reference alignment, Canvas move/Shift-all, boundary/color sampling, reload, dirty-safe edit-image swap, and the dedicated read-only reference viewer are available from target-aware palettes. Canvas movement and reference view/sample actions retain their issue-time context; stale or switched targets are rejected. The reference Canvas has independent zoom/pan/flip and consumes edit strokes. | Mixed-size/reopen/swap goldens, read-only fill, exact-depth fit/flip/sample and immutable snapshot history-isolation tests, ABI ownership/smoke, and Windows targeted movement/reload/sample/swap/save plus reference Canvas owner/style/target/pin smoke | — |
| SEQ-001                       | In progress | File/menu commands retain natural-order import/export and first/previous/next/last/goto switching. A modeless owner-drawn sequence/file-preview palette exposes bounded RGBA8 thumbnails, target follow/pin state, previous/next/import controls, and direct cell selection. Numbered raster open discovers only sibling names matching the prefix/suffix around the final numeric run. Every action captures one session/generation, and dirty cancel leaves the UUID, tab, and selected cell unchanged. | Core order/gap/endpoint no-op/thumbnail/dirty-switch and mixed-format tests; ABI size-query/short-buffer/invalid-index and atomic-failure tests; Windows modeless owner/style/accessibility, command-state, direct-selection, dirty-cancel/Undo, natural sibling filtering, automatic preview, and file/menu smoke | Automatic-save-on-switch and endpoint-loop preference controls are not yet exposed; the current UI prompts before a dirty switch and stops at endpoints |
| SEQ-002                       | Verified    | Timed motion UI provides all six FPS values, loop, pause/step/first/last, selection/light-table options, and shortcuts                                                                                                                                                                                             | Core motion, C ABI state, and Windows timer/menu/key smoke                                                                                                                                                                                                                | —                                                                                                                                                                            |
| BATCH-001                     | Verified    | Checksummed versioned `.inkbatch` persists file/folder/current-sequence input selectors, ordered operations, conjunctive stable-ID/type selectors, and output/failure policy; a native modeless Batch palette edits and saves/loads the graph | Format current-version/bounds/checksum/atomic replacement tests, Core operation item-count boundary tests, complete operation/filter graph round-trip, FFI copied-span ownership, Windows palette/menu smoke | Before format freeze, schema changes increment the graph version and non-current versions are rejected |
| BATCH-002                     | Verified    | Ordered catalog covers replace/swap, continuous fill, separation, visibility, four line-width modes, the full filter catalog, boundary airbrush, dust, mirror, rotate, resize/DPI, and plane conversion with skip/error target policy | Batch acceptance, complete catalog round-trip, Core operation tests, FFI nested-record validation, and Windows add/edit/reorder/remove/swap/boundary dry-run smoke | Boundary airbrush starts with two colors |
| BATCH-003                     | Verified    | Natural-order preview, seed-color warnings, current-file/UUID selection, current/all dry-run and execution, progress/cancel including cancellable waits, per-output atomicity, explicit continue/stop reports, and non-overwriting default policy run on the Core engine. The Batch pane shows follow/pinned/job target, progress, cancel, and result; preview and execution keep the captured session/generation/job when focus changes and restore the prior policy after job close. | Batch acceptance, current-file/wait/cancel/failure regressions, FFI preview/report/cancel, pane-policy job-close tests, and real Batch palette target/pin/progress/result/dry-run/output application smoke | Native `.inkpod` is the only batch output format currently enabled; overwrite requires explicit policy |
| WIN-001                       | In progress | Japanese Help exposes an F1 `inkpod ヘルプ` command that extracts the EXE-embedded, self-contained UTF-8 manual to a build-versioned LocalAppData cache with byte reuse and atomic stale replacement, then opens it through the default HTML handler. The same menu opens the native About dialog, whose version line shows the semantic version and the shared EXE/MSIX build number; the canonical reset workspace uses resource-titled, DockHost-managed primary panes for full-width tool options, an 80-DIP left strip with 20 readable single-word tool labels, central EditorArea, and a 320-DIP right color/layer stack. Descriptor-constrained dock/float/hide/restore, tab/split, caption-free and MSAA-named splitters, per-pane visibility, DPI-independent session/save layouts, reset/restore, left/right mirroring, and resource-titled standard-button secondary AutoHide strips share command state with menus. F6/Shift+F6 cycle menu, dock, editor, and status focus. Standard controls expose dirty tab, pane, AutoHide, target, and job-status names through the Windows MSAA/UI Automation bridge. | Embedded-manual resource/exact-byte reuse/stale-replacement test plus production-menu and LocalAppData preparation smoke; About geometry/font/icon/version/build smoke and manifest version verification; pure bounded dock model and layout checks at 96/120/144/192 DPI, narrow non-mutating fallback, standard/compact density, and mirroring; native resource-title, owner/style/reparent, tool-label, DockHost geometry/dispatch/visibility/layer-plane synchronization, AutoHide pointer/keyboard/accessibility, splitter caption/accessibility, forward/reverse focus-cycle, dirty-tab accessible child, and Batch target/job accessible-name regression smoke; keyboard-message, shutdown, ABI, and application CTest; reproducible native assistive-technology/IME/language procedure in `docs/windows-g13-release-checklist.md` | Automated smoke deliberately suppresses the external browser launch; runtime checks the `ShellExecuteW` result. The About 574 x 544 reference is device pixels at 144 DPI and is converted once. About's English title and description in the Japanese UI were confirmed by the user as expected behavior on 2026-08-03. The native x64 run found that AutoHide edge buttons are named in UIA but are not reachable by F6/Tab/Shift+Tab. High contrast, 200% DPI, complete screen-reader announcements, and Japanese IME composition remain blocked. |
| WIN-002                       | Verified    | A bounded registry owns up to eight heap-stable `WorkspaceWindow` instances on the existing UI/Input thread, tracks the current and last-focused window, and restores the bounded window count. Each instance owns its top-level/child `HWND`, menu/status state, `DockHost`, `EditorArea`, panes, focus history, and persistence slot while application services remain shared. Window procedures resolve the exact owner from `GWLP_USERDATA`; the message loop enumerates the registry for modeless dialogs and keyboard processing. New-window commands and G11's captured-context window drop, next-window move/copy, and tear-out routes are connected. A non-final close preserves surviving windows/sessions, synchronously retargets Core notifications and pending value tokens to a surviving `HWND`, and atomically unregisters all of that workspace's Canvas snapshot sinks before child destruction; only the final close posts quit. G12 enforces one logical application per user/logon session with a user-scoped named mutex and local named pipe. Validated bounded multi-path requests carry version, length, request ID, open mode, and last-focused/new-workspace preference; an IPC thread posts only value tokens to the UI thread. A timed-out primary never causes the secondary to start an independent editor. | G2 ownership/structural tests plus G10 registry/focus/capacity and cross-workspace command-context tests; structural application-host and persistence gates; native CoreHost tests replace and destroy the original notification owner before further notifications; native smoke covers two-window lifecycle, same/different-session routing, snapshot-sink registration/count/atomic-unregister-before-destroy, G11 drag races, G12 same-file activation/focus and explicit new-workspace routing; activation tests cover Unicode/space/long/multiple paths, duplicate and four concurrent clients, version/truncation/oversize/forged messages, UI-queue failure rollback, and primary timeout; structural startup gate proves secondary exit precedes Common Controls/Core/renderer/window initialization; native x64 Debug/Release and ARM64 Debug/Release full CTest pass, with x64 Release GUI smoke repeated five times | — |
| WORKSPACE-001                 | In progress | The one-action `彩色` reset layout uses a bounded HWND-free `WorkspaceLayoutState`/`DockLayoutModel` and UI-thread `DockHost`; G6's bounded one/two-group `EditorArea` and one Canvas per visible group remain central. Five built-in presets plus user naming, save/save-as/restore/reset, primary docking, and accessible secondary-pane AutoHide are connected. HKCU version 4 stores only bounded window/split/dock/pane placement and preset data, migrates versions 2/3 once, defaults missing known panes, ignores unknown pane IDs, rejects malformed records without failing startup, and clamps main/floating windows to current DPI-aware monitor work areas. G10 stores a bounded workspace-window count and uses per-window persistence slots for layout and palette placement. Narrow/compact adaptation does not mutate saved state. Preset presentation waits for a matching CanvasId/surface generation when input is captured, preserving document/view/history and active stroke. | Pure dock operations plus v4 round-trip/HKCU restart, v2/v3 migration, truncated/corrupt/oversized/unknown/missing-pane, window-count bound, custom-name/no-op, 96/120/144/192-DPI, monitor add/remove/primary change, DPI clamp, standard/compact/narrow/mirror tests; structural persistence/ownership gate; native preset/menu/shortcut, AutoHide pointer/keyboard/accessibility, per-window pane isolation, document revision/history and active-stroke smoke; retained split-editor, Undo/Redo, save/reopen, x64 Debug/Release CTest | The 2026-08-03 native x64 run found that the Reference Check AutoHide edge buttons cannot be reached with F6/Tab/Shift+Tab despite exposing UIA names. |
| WORKSPACE-002                 | Verified    | Strong IDs, pointer-free issue-time context capture, owner-thread routing, generation-bound tokens, multi-workspace/session/group namespaces, and stale rejection are enforced. Cross-window commands and G11 `DragToken` values retain captured workspace/group/view/session/generation and operation instead of resolving a later active document. A bounded pane-policy registry provides `Application`, `FollowActiveView`, `PinnedDocument`, and `Job`; fixed/job actions keep their issue-time target across focus changes, never fall back after invalidation, and close returns to the defined follow/disabled state with an accessible notice. Locator, sequence/file-preview, Light Table, subpalette/reference, Color, and Batch panes expose and use the same target contract. | Command-context/pane-policy unit tests cover multi-workspace capture/move/stale rejection, active follow, pin, application, job bind/close, pinned close notice, focus change, auxiliary Canvas lifetime, missing/unknown/stale scope, and no retargeting; G11 coordinator/unit/structural/native smoke covers value-only drag, stale source/target rejection, close/capture/DPI cancellation, exact rollback, and no raw posted object pointer; native pane smoke covers per-window owner/style/accessibility, headers, pin/unpin, command state, exact-session editing, async result isolation, stale/dirty/queue/close handling, invalid no-op, cancel, and Undo/Redo | — |
| SAFE-001                      | Verified    | Six-file forged-length/dimension corpus with asserted bounded reject paths, deterministic truncation/bit-flip harness, and failed-open Core preservation cover native, batch, PNG, TIFF, TGA, and BMP public decoders/readers                                                                                      | `acceptance_corrupted_file_corpus_is_bounded_and_non_destructive`; `mutation_fuzz_all_file_decoders_never_panics`; `corrupted_open_preserves_the_current_document_and_every_file`                                                                                         | Corpus rejection preserves Core state, input/existing output, and creates no temporary output                                                                                |
| PERF-001                      | Verified    | Maximum-dimension sparse/COW and bounded dense filtering; canonical scalar revision-max render-cache identity; payload/hash-free view-only validation; changed-tile-only recomposition; fixed Core quick/full zoom and dirty-tile workflows; native 512-event/512-Present wheel and 16-stroke/16-Present drawing routes; bounded Core, renderer, thumbnail, and pane resource categories | Revision-max source lock and payload-access counter; semantic checksum/revision/reuse/rebuild/sample/Present/queue/resource assertions; historical detached-old A/B calibration; approved routine range `windows-arm64-apple-silicon-parallels-release-2026-08-05`; RendererHost queue-empty plus zero-in-flight Present-completion regression; exact ranges, samples, and exceptional rebaseline procedure in `docs/core-benchmark-baseline.md`; Rust/Core/FFI/native resource tests and Windows CTest | The revision-max choice intentionally cannot distinguish a high Light Table revision masking a later raster edit, deletion of one equal-maximum source, numeric collisions across revision domains, or every primary/secondary alpha-mode transition; transparent compositions are not negative-cached, and render metadata outside the formula relies on whole-cache invalidation. Native drawing measures a fixed burst through its final Present, and native wheel is display-paced rather than a CPU microbenchmark. On a matching environment, two independent five-run medians above the approved upper edge are rejected; ranges are not shared across unlike environments or widened without explicit approval. |
| PKG-001                       | Verified    | CMake aligns Rust x64/ARM64 MSVC `+crt-static` with Win32 `/MT`, rejects a missing Rust target feature at compile time and dynamic CRT imports before packaging, assembles unsigned x64/ARM64 MSIX without app-local CRT DLLs, and creates semantic-versioned `windows-x64`/`windows-arm` portable ZIPs containing exactly lowercase `inkpod.exe`, `README.txt`, `LICENSE.txt`, and `ThirdPartyNotices.txt`; the local x64/ARM64 wrappers exclusively reserve one monotonic shared Git-ignored build number per invocation and pass it to selected Debug/Release presets; exact wide-argument parsing uses the same identity/Recovery-aware native/raster open route for primary startup or versioned activation | Strict x64/ARM64 Windows build; Rust `crt-static` compile guard and final-PE dependency checks; exact ZIP filename/count/casing/root/hash/PE/version checks plus extracted x64 ABI smoke; unpacked-MSIX association, license/notices, and dynamic-CRT-absence checks; local x64/ARM64 build-number dry runs and launch/activation/save-reopen regression coverage; native ARM64 Debug/Release ABI, GUI, and distribution gates | Portable ZIPs do not register file associations and continue to use the user profile/HKCU; administrator MSIX install/installed ABI/uninstall remains opt-in |
| PORT-001                      | Verified    | All Rust crate source targets, crate/workspace manifests, and resolved lockfile reject Windows imports/configuration/packages; next-frontend API gaps are documented                                                                                                                                               | `acceptance_rust_workspace_has_zero_windows_imports`; Linux/macOS CI workspace checks                                                                                                                                                                                     | Sandboxed frontends still need byte/stream I/O plus platform file-authority adapters                                                                                         |

`PERF-001` calibration note (2026-08-05): the retained native wheel pairs have a
candidate/old paired-ratio median of `1.000015371` (+0.001537%, candidate slower
in 10 of 18 pairs), despite the pooled marginal candidate median being 0.000549%
lower. The user classified this display-paced microdifference as measurement
noise and subsequently approved environment-specific routine ranges. The exact
sign remains historical calibration provenance and is not represented as a
speedup. Routine acceptance now uses the matching range, mandatory semantic
work, and a second five-run batch after an upper-edge median.

For rebaseline or an explicit audit, the old native executable is reproducible
from fresh `3f164db` with the
full-index `tests/revision_max_native_harness_3f164db.patch`. Its SHA-256,
eight-file allowlist, and renderer-instrumentation-only Canvas hunks are locked
by the Windows frontend structural test; the exact application procedure is in
`docs/core-benchmark-baseline.md`.

The Core regression test separately source-locks the complete primary snapshot
validation call graph, rejects payload/hash tokens, proves the counter is active
during the initial compose, and requires zero payload accesses across 128
cache-hit wheel-style zoom snapshots.

## Procedure-history redesign traceability

M0 fixes the successor contract and route ownership, M1 implements one
canonical executor for main-line color, palette replacement, and one raster
stroke, M2 adds the closed append-only runtime journal and cache-free replay for
exactly that canonical slice, and M3 moves session editor defaults/state into
Rust Core with an additive ABI v2 presentation bridge. M3 implements the
canonical target EDIT DTO in memory but deliberately does not persist it in the
production v2 container. The M4 implementation slice introduces a distinct
Genesis Cell ID, typed immutable `SolidWhite`/asset base surfaces, and a
Core-owned content-addressed canonical asset registry with deduplication,
bounded ingestion, and semantic retention roots. The M4 milestone is
final-verified at its Core/runtime, existing ABI-v2 ingestion, and Windows
connection boundaries. M5 adds the ABI-v3 value/ID control plane and closed
typed canonical mutation queue without migrating M6 procedure families. The successor requirements below remain
`In progress` where they include later all-route, determinism, asset
persistence, or production container work.
This does not claim that remaining document
routes, journal persistence/reopen, or the format cutover are complete.
The affected requirement set is tracked explicitly so current v2 evidence is
not mistaken for successor evidence:

| Requirements | Successor status | Current evidence / remaining boundary |
|---|---|---|
| `ARCH-002` | In progress | The pre-M4 OS-independent Rust semantic ownership, eight closed route classes, no-C++ document/history-owner guard, single-owner canonical executor slice, and Rust-owned journal/branch/replay model retain their Verified evidence. M4 keeps Genesis and the canonical asset registry inside the same platform-independent Core boundary; its public Core contracts, ABI-v2 ownership/atomicity contracts, Windows connection, and final ARM64 validation passed on 2026-08-06. Remaining all-route primitive ownership is M6. |
| `ABI-001`, `ABI-002` | Verified | All 196 ABI v3 exports are classified and directly referenced by contract tests. M5 adds 21 type/generation-tagged object, primitive, snapshot/thumbnail/export copy, task, and release exports. Top-level/nested `struct_size`, feature/schema/opcode, NULL/alignment/stride/count, stale/wrong-type/wrong-generation/double-release, no-op, caller-buffer-copy, and failure atomicity are tested. `PrimitiveWork` fixes issue-time session/generation/context/base revision/target/value/ID and excludes raw pointers, closures, and paths; queue saturation rolls back rejected sequence state and accepted work resolves once across active stroke, close, and shutdown. Legacy wrappers for the four canonical procedures call the same Core executor. `LegacyInvokeWork` is an explicit M6 bridge for unmigrated routes, not a second primitive owner. |
| `IO-001` | In progress | The successor target advances to exact Genesis/ASST/PROC/META/EDIT/CKPT v7/epoch-5 with schema/domain-4 hierarchical state commitments because Genesis now owns a distinct Cell ID and typed immutable base surface. Asset/procedure-payload digest schemas remain v1, and Core owns the canonical registry/retention graph that future GENS/ASST records must preserve. M4 adds `ImportRasterAsset/v1`; `ApplyRasterStroke/v2` remains semantics revision 4 and uses inline sample bytes or one canonical sample asset above 4 MiB; the two metadata primitives remain schema v1 and no v1 stroke compatibility decoder remains. The bounded canonical EDIT DTO also remains in memory. Production stays exact-current v2: it serializes neither the M4 Genesis/asset graph nor journal/EDIT, cannot restore EditorState/history, and cannot clear editor dirty on normal save. Asset-backed Genesis normal/recovery/Batch output fails before I/O without changing existing output or live path/savepoint. End-to-end successor save/reopen remains the atomic M8 cutover and is not Verified. |
| `IO-002`, `CLIP-001`, `LT-001`, `LT-002` | In progress | Supported raster-open/import, private clipboard, and Light Table source paths share the M4 canonical ingestion boundary. Equal descriptor-plus-logical-payload inputs may deduplicate independently of path/codec, while replay retains only immutable canonical identity/bytes rather than a path or caller buffer. Existing visible workflows and legacy ingestion exports remain in ABI v3; all-route Import primitive migration is M6 and production asset persistence/reopen is M8. |
| `SAFE-001`, `PERF-001` | In progress | The migrated slice enforces bounded canonical arguments/sample work, atomic event/ID reservation, tile-order-independent replay, fixed-seed journal state-machine comparison, and inverse-cache-independent reconstruction. M4 additionally validates asset kind, format/alpha semantics, dimensions, canonical stride, lengths, identity, aggregate resource use, and retention before publication; deduplication and branch retention are semantic gates rather than wall-clock assumptions. Asset-base composition prepares one local tile view instead of per-pixel registry lookup and charges initial payload access while 128 view-only cache hits charge zero; sparse transparent RGBA/Binary bases enumerate allocated coordinates without introducing a tombstone/negative cache. Editor-only updates leave document/render revision and render content unchanged; the revision-max source lock/formula, changed-tile-only gates, workloads, and approved ranges remain authoritative and unchanged. Full-route enforcement, fuzzing, cross-architecture replay, and replay benchmarks remain outside this slice. |
| `SESSION-001`, `VIEW-004`, `WORKSPACE-002` | In progress | Each Core-backed document session owns one EditorState and one canonical asset registry; same-document views share them, different sessions remain isolated, and session close drains transient users before releasing registry storage. Workspace/view switches re-query a session/generation-bound copied Windows presentation cache without writing old workspace values back. Core/FFI public contracts and Windows switching, raster-open, sequence-attachment, and cache-lifetime smoke final-verify the M4 boundary; production-v2 Genesis/asset reopen restoration remains M8. |
| `DOC-001`, `DOC-002`, `DOC-003` | In progress | M3 moves the active layer/plane pair to EditorState, validates stable IDs, and deterministically re-resolves it after topology changes without frontend fallback. M4 separates stable Document and Cell IDs and makes Genesis base state immutable: blank cells use allocation-free `SolidWhite`, while raster-open-as-document can use an immutable asset base. The raster-stroke procedure captures its exact stable Plane ID at begin. Document/topology routes outside the migrated procedures remain inventoried but not yet executor-backed. |
| `HIST-001` | In progress | M2's three migrated procedures and M4's fourth `ImportRasterAsset` procedure atomically publish document, one visible history entry, StateId, revision, high-watermarks, and a closed runtime journal. M3 editor-only semantic updates advance only EditorRevision/digest/editor dirty. M4 makes Genesis and every retained journal branch/redo tail asset-retention roots, then deep-copies and re-ingests that rooted graph before fresh replay, so an inactive-only asset cannot be collected or accidentally pass through shared ownership. Semantic no-op and invalid/stale/overflow/failure preserve all published state. All-production-route journal authority remains M6 and production journal/asset/EDIT persistence remains M8. |
| `PAINT-001`, `PAINT-002`, `PAINT-003` | In progress | `ApplyRasterStroke` is executor-backed with lifetime-independent bounded canonical samples: at most 4 MiB inline, otherwise one immutable sample asset, with fresh-Core bit-exact replay through the same bytes. M3 captures its begin-time exact-depth color, Q16 diameter, stable target, and options rather than consulting later EditorState changes; the remaining paint families and cross-architecture determinism remain outside this slice. |
| `FILL-001`, `FILL-002`, `FILL-003`, `SEL-001`, `VECTOR-001` | In progress | Fill tolerance/gap/inclusion/overflow/Light-Table options, selection shape/operation/tolerance/diameter, and vector erase/select options are typed per-session EditorState with default/no-op/stale/atomicity tests. Canonical procedure schemas/execution for these families remain M6/M7; M3 does not claim them migrated. |
| `COLOR-001`, `COLOR-002` | In progress | Immutable Rust defaults give Pencil black and every other color-consuming tool the Core-defined initial color. Per-tool RGBA8/RGBA16 color+alpha and Q16 diameter, active/last color-consuming tool, and palette cursor round-trip through Core/ABI without packed-RGBA8 reduction; palette content and main-line color remain document primitives. Exact-depth `SetMainLineColor` and ordered `ReplacePalette` retain canonical executor/replay evidence. Production-v2 EditorState restoration remains M8. |

G13 native accessibility correction (2026-08-03): `WIN-001` and
`WORKSPACE-001` are **In progress**. A native x64 Release run confirmed UIA names,
dirty-tab state, tab/group traversal, pane targets, keyboard menu routing, and
Undo restoration, but the Reference Check AutoHide edge buttons were not
keyboard-reachable with F6/Tab/Shift+Tab. About's English title and description
in the Japanese UI were confirmed by the user as expected behavior. High contrast, 200% DPI, and Japanese IME
composition remain blocked on this host. The dated evidence and unblock
conditions are recorded in `docs/windows-g13-release-checklist.md`. On
2026-08-03 the user accepted G13 as complete and deferred these observations to
the affected requirements; those requirements remain `In progress` until their
implementation is fixed and the native rows pass.

For a user-invoked requirement, `Verified` requires a production UI route through
the Windows adapter, C ABI, Core, and visible result; a smoke-only ABI call does
not substitute for that route. Internal build, ABI, renderer, resilience, and
portability requirements may be verified at their natural non-GUI boundary.
Representative current platform verification is summarized in
[`implementation-status.md`](implementation-status.md), not duplicated here.

PNG dependency licenses are recorded in `docs/third-party-notices.md`.
