# Compatibility status

Compatibility means operation semantics, data separation, coordinates, and
saved results—not replication of a legacy UI or assets.

| Requirement | Status | Implementation | Tests | Known difference / next work |
|---|---|---|---|---|
| ARCH-001 | Verified | CMake completion stamp lists every Rust source, including M5 Core/format/image geometry, and connects Cargo `inkpod-ffi` staticlib/rlib byproducts to MSVC targets | VS2026 x64 Debug/Release build; unchanged repeat reports no work | VS2022 CI remains configured |
| ARCH-002 | Verified | Safe, OS-independent Core/image/format crates with no frontend dependency | All three domain crates' source/manifest scan, clippy, workspace tests | No Rust Windows API dependency |
| ABI-001 | Verified | ABI v1 adds copied cubic/fill/edit inputs, caller-owned vector selection/raster buffers, transactional raster-vector conversion, and strict nested-span/size/count validation while preserving M0-M4 ownership rules | C11 layouts; executed C++20 M1-M5 smoke; Rust short nested record, count-query, conversion, and snapshot tests | New caller pointers are never retained |
| ABI-002 | Verified | Immutable snapshot owns batched raster/overlay data plus flat cubic segment, fill, and boundary-ID spans; ownership moves unchanged through the renderer queue | Core zoom invariance; Rust FFI vector ownership; C++ validation and D2D smoke source | Vector records use document coordinates and are borrowed only for snapshot lifetime |
| IO-001 (native save) | Verified | `.inkpod` v1 adds bounded M3/M4/M5 sections; `M5VT` persists bounded fixed-point cubic paths, variable widths, exact colors, and same-layer fill topology while retaining M1-M4 readers | Fill-topology save/reopen acceptance; malformed cross-layer/open/missing/coordinate topology, counts, relationships, and cross-M3/M4/M5 stable-ID tests | Blob compression is optional and not enabled |
| IO-001 (M2 recovery) | Verified | Atomic autosave preserves the normal path/savepoint; recovery opens dirty and pathless; Windows assigns never-saved cells a private recovery path, queues timer/manual autosaves, defers them across an active stroke, and discovers private recovery at startup | Core/FFI recovery tests plus Windows active-stroke autosave, private-path discovery, and normal-file/recovery checksum smoke | Only the newest private recovery is prompted per launch; deferred files remain available for a later launch |
| IO-002 | Verified | PNG/TIFF/TGA/BMP preserve supported straight-alpha depth, dimensions, and format-capable DPI; TIFF declares unassociated alpha and decode allocation is bounded first | Four-format RGBA8 plus PNG/TIFF RGBA16 round-trip, indexed-PNG, TGA-origin/alpha, padded-24-bit-BMP, alpha-tag/policy, and oversized-dimension tests | TGA has no standard DPI; TGA/BMP reject RGBA16 instead of quantizing |
| DOC-001 | Verified | CellDocument carries transactional DPI, 100/reference/drawing/safe frames, and independent margins | M1 metadata equality plus M4 mixed-paper alignment golden | Native paper/frame property UI remains later adapter work |
| DOC-002 | Verified | Stable-ID typed tree now includes vector-coloring layers with one main-line, one-or-more color-trace, one fill plane, and optional raster planes | Core/format vector topology and save/reopen tests; Rust/C++ ABI node smoke | Vector payloads are semantic geometry, not raster blobs |
| DOC-003 | Verified | Existing transactional tree operations preserve/reassign/delete/duplicate M5 vector objects and boundary relationships in addition to M3 raster semantics | Core vector duplicate/delete/merge paths plus prior M3 acceptance/order/lock tests | Windows full layer panel remains UI work |
| HIST-001 | Verified | M5 path/fill draw, erase, connect, width correction, and raster-vector conversion join prior edits as atomic Undo transactions; no-op/invalid/repeated-connect paths do not commit | Core M1-M5 history tests and transactional VECTOR-002 test | Vector interactive preview UI remains later adapter work |
| VIEW-001 | Verified | Anchored zoom, box zoom, fit, 1:1, pan, viewport resize, and horizontal/vertical view flip use a separate view revision | Core revision/mode/box/flip tests; FFI transform bits; Windows Fit/DPI and flip/mirror smoke | Box-zoom drag gesture is not yet in the Windows Canvas |
| VIEW-002 | Verified | Core owns ruler, persistent guide/grid, snap, and transparent-view state; snap obeys its enable flag, document guides/grid serialize, and snapshot overlay data drives renderer grid/guides/transparency | Core guide/grid/snap test; Rust/C++ overlay ABI and Windows grid/guide render smoke | Windows grid command is the M3 UI slice; remaining controls are ABI-ready |
| VIEW-003 | Verified | Locator reports per-view document coordinates/selection/color; multiple logical views share one document but retain independent transforms | Core locator/multi-view test; Rust/C++ ABI and Windows same-revision snapshot smoke | Separate secondary child-window layout remains later UI work |
| PAINT-001 | Verified | UI/Input, Core engine, and Renderer are distinct; pencil/brush/eraser/auto-erase/pressure samples stream through a bounded queue and preview at frame cadence | Core tool/resource tests, 256-record FFI test, Windows thread-ID/multi-sample/live-preview smoke | M2 UI retains a single brush diameter and RGBA8 color picker |
| FILL-001 | Verified | Deterministic connected seed fill uses normalized 16-bit per-channel tolerance and the persistent M3 selection mask | Closed-region, selection, tolerance, Core transaction, FFI, and Canvas-click tests | Selection authoring is tracked under SEL-001/002 |
| FILL-002 | Verified | Specified/except-specified inclusion (up to six colors), overflow abort with leak coordinate, bounded cancellation, axis gap-close, and detached matching regions | Required gap/inclusion/overflow goldens and invalid/cancel/no-op transaction tests | The native gap rule is documented; proprietary behavior is not claimed |
| FILL-003 | Verified | Closed-region fill handles transparent or colored enclosed components, rejects edge-connected regions, and fill extension expands through a bounded mask/distance; all planners poll cancellation | Closed/open/colored/transparent-only/cancel and extension automation; Core/ABI operation validation | Windows exposes seed fill; separate range-gesture tools are not present |
| SEL-001 | Verified | Persistent bounded binary selection supports rectangle, ellipse, lasso, polygon/polyline, trace, and connected color wand | Core M3 authoring/property test; C ABI validation; Windows rectangle smoke | Full Canvas gesture UI is later work |
| SEL-002 | Verified | New/add/subtract/intersect, invert, expand/shrink, and typed color selection are deterministic document transactions | Exhaustive per-bit boolean property table plus Core/Windows operation smoke | — |
| SEL-003 | Verified | Raster selection-layer conversion remains intact; vector selection returns deterministic cut/touch/contained/line/whole-line/intersection/fill-boundary/fill ranges through Core and caller-owned ABI buffers | Named all-eight-mode Core test; Rust/C++ ABI count-query/buffer smoke | Native gesture UI is not yet exposed |
| PAINT-002 (M5 slice) | In progress | Cubic path input represents line, curve, shape, and polyline geometry and commits atomically | Core path/Undo plus Rust/C++ ABI draw tests | Interactive preview/shape tool UI is not yet implemented |
| PAINT-003 (M5 slice) | In progress | Deterministic nearest-endpoint connect and add/subtract/scale/constant line-width correction are implemented | `vector_002_connect_width_select_and_raster_vector_conversion_are_transactional` | Dust removal remains unimplemented |
| VECTOR-001 | Verified | Fixed-point cubic paths, variable widths, vector main-line/color-trace/fill topology, trace-before-main paint order, immutable snapshot, and Direct2D path/outline renderer | Image geometry test; Core zoom/order/golden/save tests; Rust FFI + compiled C11/C++20 renderer smoke | Arbitrary raster/vector layer interleaving still composites vector content after raster tiles |
| VECTOR-002 | Verified | Transactional draw, partial/intersection/full erase, nearest connect without duplicate zero-gap connectors, four width modes, all selection modes, deterministic rasterize, and bounded RGBA8 run vectorization | Five named M5 acceptance tests plus object-limit/all-mode/transactional and FFI buffer/conversion tests | Windows authoring controls are not yet exposed |
| CLIP-001 (typed/document coordinates) | Verified | Rust-owned private clipboard preserves plane type and absolute document coordinates, locates a compatible destination when the active plane differs, and retains floating state after a failed commit | Core different-paper/failed-commit acceptance test; Rust/C++ ABI; Windows translated-paste smoke | Standard Windows clipboard formats remain `In progress` and are not claimed Verified |
| XFORM-001 (M3 flip/mirror) | Verified | Non-destructive view flip changes view revision only; destructive mirror changes document history/revision | Core and Windows acceptance tests | Destructive rotate/size/resolution remain future work |
| XFORM-002 | Verified | Floating typed selection supports bounded inverse-sampled move/scale/rotate, preview state, one-unit commit, exact cancel, and retry after failure | Core scale/rotate/cancel/coordinate-paste tests; FFI/Windows translated-paste tests | Windows transform editor UI is not yet present |
| SHORT-001 | Verified | Bounded shortcut bindings support rebind, deterministic conflict replacement, resolve, reset, and a native hotkey editor whose Undo/Redo/Copy/Paste bindings drive actual key events | Core/FFI resolve tests; Windows editor/reset/key-resolution smoke | M3 editor exposes the four connected commands |
| COLOR-001 | Verified | Typed grayscale/RGBA values plus selected/topmost/composite/transformed-light-table sources; Color-mode reference sampling preserves exact RGBA8/16 and only display composition converts to BGRA8 | Prior exact-depth tests plus M4 Core/FFI RGBA16 reference sampling | Full color-editor UI remains later scope |
| COLOR-002 (M4 slice) | Verified | Exact persisted palette plus sequence-backed subpalette registration and exact pixel sampling | Palette tests and M4 subpalette/sequence acceptance | Color-chart/palette authoring panels remain broader UI work |
| LT-001 | Verified | Stable-ID persisted set/item management, transform/color/mode, individual/global opacity, immutable source rasters | Set/item transaction, 25% opacity, native round-trip/malformed tests | Native Windows panel is not yet exposed |
| LT-002 | Verified | Reference-frame alignment, boundary/color sampling, validated reload replacement, and dirty-safe edit-image swap retaining display state | Full-tile mixed-size reopen/swap golden; cancelled/read-only fill; Rust/C++ boundary-fill ABI smoke | Reload is caller-driven |
| SEQ-001 | Verified | Natural-order session sequence supports gaps, prev/next, clean-only switch, thumbnails, and common-raster import/export | 1,3,10 gap/order/thumbnail golden and dirty-switch Core/FFI/C++ checks | Sequence catalog is session/workspace state, not copied into every cell file |
| SEQ-002 | Verified | Supported FPS, loop, step, pause, and selection/light-table options are typed motion state with deterministic frame thumbnails | Core motion/subpalette test and Rust/C++ start/step smoke | Native timed playback window remains UI adapter work |
| M0 Windows shell (Help/About) | Verified | Japanese Help command opens a native modal About with the generated app icon, product description, and CMake-derived version | Release Windows smoke creates and closes About through the real menu command; EXE icon/version resource inspection | Uses a macOS-inspired centered hierarchy while retaining Windows modal, DPI, and keyboard behavior |
| M8 Windows package preparation | In progress | Windows App Development CLI 0.4.0 manifest, 48 MSIX PNGs, and five-resolution ICO generated from the project SVG | Asset file/dimension/ICO/version CTest; Release RC/link and Windows smoke | No signed MSIX or clean Windows install/uninstall verification yet |

M4 was re-verified before M5 edits (named Core 5/5 covering all six scenarios;
existing Debug CTest 3/3). All five M5 acceptance scenarios are `Verified` by
named automation. The integrated M5 C++/D2D smoke passed both Debug and Release
CTest presets 3/3; immediate unchanged builds were no-ops.

## Unknown legacy formats

| Item | Status | Reason |
|---|---|---|
| DGA/CEL binary codec | Unknown | No rights-cleared fixture and independent oracle |
| Legacy palette/chart/filter preset layouts | Unknown | Byte layouts are not defined by the internal specification |

No legacy manual, image, icon, wording, proprietary binary assumption, or
third-party artwork was used for M0 through M5. PNG dependency licenses are
recorded in `docs/third-party-notices.md`.
