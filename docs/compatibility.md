# Compatibility status

This document tracks only the current status, representative evidence, and
known difference for every requirement ID defined in
[`../SPEC.md`](../SPEC.md). Detailed behavior belongs to `SPEC.md`, ownership
and data flow to [`architecture.md`](architecture.md), exact format bytes to
[`file-format.md`](file-format.md), and current platform verification to
[`implementation-status.md`](implementation-status.md).

Status is one of `Not started`, `In progress`, `Experimental`, `Verified`, or
`Blocked`. A user-facing requirement is `Verified` only when its production UI
route and observable result are tested; infrastructure requirements may be
verified at their natural build, ABI, Core, format, or renderer boundary.

## Requirement matrix

| Requirement | Status | Representative evidence | Known difference / next work |
| --- | --- | --- | --- |
| `ARCH-001`, `ARCH-002` | Verified | CMake/Cargo source tracking, crate-boundary guards, OS-dependency scans, public integration tests, strict Rust/Windows builds; macOS CMake→Cargo→checked-in-Xcode sub-build with strict Swift 6, canonical-header-only Clang module, value-only CoreHost facade, and fixed Foundation.Thread owner registry | Rust domain crates contain no Windows or Apple frontend API dependency |
| `ABI-001`, `ABI-002` | Verified | Header/export/catalog parity for ABI v15; platform-neutral 16-byte `InkpodShortcutStrokeV2` and 80-byte sequence records with Unicode/named keys plus PRIMARY/SHIFT/ALTERNATE/CONTROL; fixed-width language-neutral `InkpodHistoryEntryKind`; C11/C++20 layout and Swift import/link; NULL/short/unknown-key/modifier/stale/wrong-thread/ownership tests, including macOS fixed-owner lifecycle and snapshot release-all-path | ABI v1-v14 callers must rebuild against the current header |
| `IO-001` | Verified | V26/runtime epoch-23 stream/layout/digest/current-only tests; exact v25 rejection; document metadata/archive schema 5 vanishing-point, shooting-frame and annotation records; `EditVanishingPoints/canonical-v2`; current-only Cut descriptor schema-2/epoch-23 codec; existing journal/checkpoint/recovery/compaction contracts; save/reopen/replay and malformed-data tests; macOS Sandbox Open/Open Recent/Save/Save As/Revert/autosave/recovery/compacted-copy plus M10 multi-folder Batch input/output using balanced security leases, stale bookmark regeneration, job-lifetime authority, staged Core publication, duplicate/collision rejection, bounded recovery sidecars, dirty close and launched product coverage | Non-v26 files are rejected without migration; compression code 0 only |
| `IO-002` | Verified | PNG/TIFF/TGA/BMP 8/16-bit, alpha/DPI round-trips; caller/file lifetime, sequence, export, ABI and Windows smoke; macOS type-gated panels/Finder drop, staged import, atomic common-raster export on real files, and M10 output-folder permission/save-failure atomicity | Flat common-raster export remains distinct from native save |
| `WIN-001` | In progress | Startup/shutdown; versioned HKCU System/Japanese/English preference with malformed-record System fallback; first-preferred `ja` system resolution with non-Japanese/detection-failure English fallback; checked Settings > Language commands; one generated, typed Japanese/English catalog for product-owned text with exact format-signature parity; exact equality of all 642 ja-JP/en-US resource identifiers, including all 384 command IDs, with explicit exact-language loading and no Win32 fallback lookup; typed Tool labels shared by owner draw, tooltip, MSAA and UIA, with both full product smoke modes retrieving every real ToolTip value and comparing it to the same label ID; Layer/Plane kind/format/visibility/editability and accessible rows resolved before drawing; unchanged 42-by-42-DIP Plane badges use pre-resolved typed compact labels of at most two lines without ellipsis, while adjacent detail and accessibility retain the full kind; Layer/Plane owner-draw state cells measured from all four localized state captions in the active 9-point font plus DPI padding, with shared draw/hit-test geometry and dedicated Japanese/English 96/120/144/192-DPI contracts; typed Color/Palette/Chart tabs; DockHost-owned inspector-header font matched at runtime to the pane-tab 9-point Segoe UI ClearType font and recreated for workspace DPI changes; idempotent Locator presentation suppressing unchanged target-label `WM_SETTEXT`, check-state updates, and neighborhood invalidation, while shorter replacement text receives one background-erasing synchronous redraw, with an actual-window unchanged-message/shortening-restoration regression and a static repaint-contract gate; language-neutral typed history kinds mapped exhaustively to catalog IDs; strict source gate rejecting Japanese bypasses, direct wide-string assignment to product dialog/effect presentation fields, language-branch/raw document captions, history string keys, restoration of the single-line ellipsized Plane badge, and restoration of the fixed owner-draw action width; no hook, partial, or hybrid translation; language-neutral generated document data and explicit user-text composition preserving edit/document-tab/target-document/Light-Table-set text, Japanese file names, and full paths verbatim; measured localized button widths with responsive action-row wrapping; Japanese/English 96/120/144/192-DPI-equivalent font layout tests plus full GUI checks for actual visible button bounds; full English and Japanese GUI smoke covering Tool and Layer/Plane owner draw, ToolTip/MSAA/UI Automation names, Saved/Modified, Locator/Sequence/Light-Table follow/pin/follow, zoom, Core/renderer/device-loss and workspace lifecycle; Help/About; owner-centered work-area-clamped modal dialogs; DPI/theme/layout; keyboard and accessibility coverage; offline Help `謝辞` identifies the locked BLAKE3/PNG production dependencies and full-notice authority through the embedded-help route with dependency-drift, extraction, menu/state and smoke coverage; `Inkpodウェブページ` opens the canonical HTTPS URL through the shell adapter; reproducible [release checklist](windows-release-checklist.md) | High contrast, a physical 200% DPI desktop, complete screen-reader and Japanese IME work remains; the embedded offline Help body is Japanese-only |
| `WIN-002` | Verified | Windows multi-window registry, focus/routing, close/shutdown, sink lifetime, single-instance activation, malformed IPC, x64/ARM64 native smoke; macOS `WindowGroup` with MainActor workspace registry, per-workspace session/view/surface generations, cross-window view move/copy, no process-global active document, and ordered workspace→renderer→Core shutdown | Physical multi-window interaction remains manual evidence |
| `WORKSPACE-001` | In progress | All eight Windows modeless surfaces hosted by DockHost with existing docking/accessibility evidence; macOS maximum-two nonrecursive EditorGroups plus a fixed 54-point tool strip whose explicit disclosure buttons open one Canvas-side transient or optionally pinned standard popover, one five-tab inspector, bottom timeline, reversible 480-point Canvas adaptation, five presets, and bounded layout-v3 persistence with deterministic v1/v2 migration, malformed fallback, and display clamp; active-tool retarget, tool-surface dismissal, menu/shortcut checked state, follow/pin invalidation, and product accessibility audit remain connected; the Windows-only Locator edge AutoHide row maps to native inspector visibility and is `notApplicable` | Windows Reference Check AutoHide edge buttons are not keyboard-reachable with F6/Tab; physical multiple-display macOS interaction is unverified |
| `WORKSPACE-002` | Verified | Issue-time command context, follow/pin/job policy, stale rejection, cross-window drag/move/copy and target-aware pane smoke; macOS view transfer, paint/fill/color/pane routes, and chrome toolbar/menu/shortcut/preset/mirror/InspectorCommands binding share one value reducer, while Tool Options icon disclosure/menu/shortcut routes share one ephemeral transient/pinned presentation state; all Core requests capture exact window/workspace/session/view generation plus document/view/editor revision and never fall back to another active session | — |
| `SESSION-001` | Verified | File identity, multi-session/view isolation, savepoints, close scopes, Save As conflicts, autosave/recovery, malformed-open and native lifecycle smoke; macOS 64-session fixed-thread create/query/close/shutdown, duplicate no-op, close-during-input, ordered destroy, queue saturation, shutdown race, volume/inode/path reservation, exact-window dirty prompt, plus same-session multi-view creation/close and final-workspace session release | Long operations serialize on one observable Core owner lane per frontend |
| `SAFE-001` | Verified | Corruption/mutation corpus, bounded V14/checkpoint/asset/edit-target/selection-option/color-replace rejection, staged publication, failure atomicity, fuzz-target declarations; macOS ABI mismatch/short/NULL/wrong-thread/unknown-status/double-release, missing-static-library, create rollback, bounded-lane saturation, rapid begin/cancel ordering, snapshot/clipboard/chart-preview/task release-all-path, staged file replacement, malformed recovery retention, Sandbox gates, M10 job-owner races, M11 invalid layout/chrome state and stale inspector reopening, exactly-once release over 200 resize/snapshot replacements, plus the current full Thread Sanitizer and M12 release-contract gates | Coverage-guided fuzz execution is not part of the latest local run |
| `PERF-001` | Verified | Fixed ten-scenario quick/full and native workloads; checksum/revision/reuse/rebuild/payload/sample/Present/resource gates; M10 full `batch_preview` evidence; macOS Metal upload/reuse/reject/replace/drawable/device/memory counters, pan/zoom reuse, and M11's 200-resize product proof with hidden draw count zero, reused tiles greater than uploads, and at most four uploads; approved ARM64 revision-max/native envelopes and x64 RGBA16 output-color-guard five-run envelope | Canonical `revision-max` limitations remain; `checkpoint_open` has no retrospective timing envelope; macOS Instruments capture remains extended evidence |
| `PKG-001` | Verified | Static-CRT x64/ARM64 builds, dependency checks, exact four-file ZIP, unsigned MSIX and extracted ABI smoke | ZIP has no file association; administrator MSIX install/uninstall is optional validation |
| `PORT-001` | Verified | Zero-Windows-import acceptance guard, shared Linux/macOS replay/workspace checks, macOS arm64 ABI/CoreHost/product UI/Metal runtime, Sandbox entitlement inspection, security-scoped bookmark/file-authority adapters, typed pasteboard/drop adapters, arm64-only CMake/Cargo/Xcode presets, real arm64 `.xcarchive`, exact-architecture Rust/Swift bridge/test/headless-host/app inspection, non-bypassable Developer ID/notary CLI, current-candidate Developer ID signing, Accepted/no-issue notarization, stapled ticket, Gatekeeper `Notarized Developer ID`, non-clobbering GitHub Release publication with same-commit/same-bytes race convergence, and Tahoe arm64 plus Windows x64/ARM64 CI definitions | Intel Mac is intentionally unsupported; current DMG is not yet published because its source changes are uncommitted; clean-Tahoe launch and physical VoiceOver/multiple-display/sleep-wake evidence remain unverified |
| `DOC-001` | Verified | Metadata, paper/frame/DPI equality, mixed-paper alignment, native round-trip and Windows dialog smoke; macOS typed Cell Properties sheet and no-op/invalid/stale/Cancel routing | — |
| `CELL-001` | Verified | Public image/frame-mode table, every initial layer and RGBA8/16 save/reopen, invalid/overflow/UUID failure atomicity; owned-plan ABI negatives; one-dialog Core-plan summary; three-session Windows production smoke; macOS bounded configurable New Cell sheet, Rust-owned one/three-Cell plan, all-or-none publication, product-host sheet/Cancel test, and rollback without frontend identity consumption | — |
| `CUT-001` | Verified | Individual same-directory Cell-reference descriptor; independent Cut metadata/defaults/history/savepoint; persistent `(CellId, document UUID)` member identity with document-local numeric Cell IDs; default copy only at Cell creation; no-op/invalid/Cancel/stale/overflow/failure/Undo/Redo/save/reopen/recovery and identity/path rejection tests; ABI v15 ownership/negative coverage; issue-time workspace/session/generation and Cut-revision rejection in Windows and macOS New Cut/Properties/Sequence/open/save/history production workflows; macOS stages and identity-checks every relative member before atomic workspace publication and retains individual Cell files and savepoints; x64 Release user-visible creation, existing-Cell independence, Undo/Redo and save/reopen confirmation | — |
| `SEQ-STRUCT-001` | Verified | Bounded ordered insert/remove/move-before/move-after/range-renumber transaction; stable pair identity; separated asset table and ordered membership history; one-step Cut Undo/Redo; current-v26 save/reopen and exact-v25 rejection; caller-owned strided ABI with per-operation failure index; Windows and macOS Sequence timeline buttons/drag/keyboard/renumber production routes reject stale active-Cell retargeting; x64 Release five-Cell reorder/renumber/remove/Undo/save/reopen confirmation | — |
| `DOC-002` | Verified | Stable-ID typed topology; grouped tree operations, validation, save/reopen, ABI spans, Windows evidence; macOS value-projected Layer/Plane inspector with active layer/plane, main/color plane switching, typed create/update/delete and stale/no-op tests | — |
| `DOC-003` | Verified | Tree-ordered bounded multi-target EditorState; grouped duplicate/delete/visibility/editability/compatible merge/convert capability matrix; no-op/invalid/stale/Undo/replay/save-reopen; ABI spans and Windows evidence; macOS tree editor delegates all topology and property semantics to Core | — |
| `ANNOTATION-001` | Verified | Bounded stable-ID Text/Stroke/Leader/Value objects; atomic multi-edit and stroke transactions; UTF-8/geometry/no-op/invalid/Cancel/stale/overflow/failure/Undo/Redo/replay/save-reopen public contracts; ABI v15; Windows Japanese text editor, instruction draw, keyboard selection/move/delete, selection handles, font-fallback and product smoke; macOS M8 value-only editor/stroke routes, CoreText frontend font fallback, Metal overlay, normal/instruction export exclusion and save/reopen integration; x64 Release Japanese text/instruction edit, save/reopen, and PNG exclusion confirmation | — |
| `SHOOTING-FRAME-001` | Verified | Stable-ID optional angled frame; deterministic milli-pixel/binary-turn geometry and five anchors; create/update/delete and preview/Cancel; no-op/invalid/stale/overflow/nonuniform-resample rejection atomicity; Undo/Redo/replay/current-v26 save/reopen; ABI v15 ownership/negative cases; Windows Canvas properties/handles/renderer plus macOS M8 native properties sheet, exact-revision Core coordinate handles, Metal overlay and explicit instruction-raster export; x64 Release user-visible properties/handles, Cancel, save/reopen, and normal/instruction export confirmation | — |
| `VANISHING-POINT-001` | Verified | Bounded stable-ID multiple points including Canvas-exterior coordinates; exact RGBA8/16, opacity, visibility, 1-degree/custom interval and 180-degree phase wrap; viewport-clipped radial snapshot cap; create/update/delete/delete-all and noncumulative preview/Cancel; no-op/invalid/stale/overflow/failure and nonuniform-resample atomicity; Undo/Redo/replay/current-v26 save/reopen and flat-export exclusion; caller-owned ABI records and snapshot-owned spans; Windows dialog/Canvas handles/renderer/device-loss production smoke; macOS M8 native properties/handle routes, bounded radial Metal overlay, exact-target preview ownership and save/reopen; x64 Release two-point, radial snap, Cancel/Undo, and save/reopen user confirmation | — |
| `RENDER-001` | Verified | Existing mixed raster/vector/adjustment evidence plus immutable annotation, shooting-frame, vanishing-point and radial-guide render spans; Windows Direct2D device-loss coverage; Core-owned deterministic thumbnail/flat raster; macOS custom `CAMetalLayer` product Canvas with BGRA sRGB premultiplied blending, immutable tile spans, latest-wins ownership, hidden draw=0, cache/device recovery, Core-ordered mixed passes, document-bounded paper/color-check/transparency presentation over a separate neutral viewport, document-scoped adjustment LUTs, and M11 safe-region resize proof that bounds/backing drawable agree while 200 replacements release exactly once under Metal API Validation; x64 Release vanishing-point rendering confirmation | macOS HDR/wide-color output is intentionally not enabled |
| `VIEW-001`, `VIEW-002` | Verified | Zoom/box/fit/1:1/pan/flip, ruler/guide/grid/snap/transparent-view Core and FFI; macOS exposes the 19 deferred static View commands through one state/router and verifies the resized visible Canvas/backing rectangle at 640/800/1200 points, including the last valid backing pixel, half-open out-of-range boundary, and a 1920×1080 1:1 document remaining visually distinct from a larger 2706×1516 Retina backing viewport without document mutation | The Windows main-window zoom slider is intentionally absent; macOS uses a numeric zoom sheet |
| `SNAP-001` | Verified | View-targeted device/document point resolution; explicit-guide/radial-guide/grid precedence, inclusive threshold and deterministic tie-break; Ctrl bypass; bounds/extreme transform/flip/DPI/stale tests; additive ABI negatives; Windows menu checked-state, line geometry/digest, off/bypass and Undo/Redo production smoke; macOS flipped-NSView/Retina backing coordinates normalize once to top-left half-open device pixels with last-pixel tests; x64 radial snap user confirmation | — |
| `VIEW-003` | Verified | Bounded locator sampling/neighborhood, sparse allocated-tile selection bounds with document-revision caching, DPI/flip/half-open coordinates, active-stroke/filter-preview display priority, committed eyedropper isolation, one-running-plus-latest async coalescing with processed-intermediate presentation, Begin/Append preview plus End/Cancel resynchronization without revision/history publication, FFI buffers, target/pin/stale handling, Windows native continuous-input smoke, and macOS M6 AppKit mouse tracking with follow/pin, Canvas-paper-matched background, centered inspector-width-responsive square-cell layout, shared draw/hit-test geometry, neighborhood edit, auto-scroll and stale-target tests | — |
| `VIEW-004` | Verified | Multi-view revision/history, per-view device-coordinate stroke placement, split/group/window move/copy, same/different-session isolation, active-stroke and native lifecycle smoke; macOS secondary Core view IDs, per-view snapshots/revisions, maximum-two groups, duplicate/close and cross-window rollback tests; M11 chrome transitions preserve session/view identity and reject a suspended inspector target after logical-view replacement | — |
| `VIEW-005` | Verified | Per-view typed AA/centerline/endpoint flags; explicit stable endpoint topology; exact/near/closed/connected Core contracts; current-version save/reopen and noncurrent rejection; bounded borrowed ABI records; Windows menu/shortcut/checked state, Direct2D pixel golden, device-loss smoke, and x64 Release user-visible confirmation; macOS M8 exposes all four exact-target checked View commands and renders bounded centerline/endpoint diagnostics in Metal | — |
| `HIST-001` | Verified | Journal state-machine, full/checkpoint replay equivalence, inactive branches/assets, savepoints, cache rebuild, compaction and Windows Undo/Redo/dirty smoke; macOS M7 standard Command-Z/Shift-Command-Z, dynamic menu, enabled state, jump, branch-after-Undo and product Canvas round-trip use Core history as the sole authority | Compaction intentionally creates a new Genesis with empty history after explicit confirmation |
| `HIST-002` | Verified | Commit-event ordering including inactive branches; deterministic typed primitive/argument formatting; point-in-time bounded replay steps with progress/cancel and queue-tail re-entry; direct maximum-64×64 post-commit composition; query/cancel/no-document stability; Rust-owned ABI builder/snapshot ownership, size-query and negative cases; Windows modeless owner-data list and macOS M7 bounded owner-thread builder, branch History inspector, 64-row visible prefetch, stale rejection and exactly-once release | Each inspector snapshots the in-memory journal when built; Refresh rebuilds it after edits |
| `PAINT-001`, `PAINT-003` | Verified | Stable-target stroke/cleanup/width primitives; pressure, extreme/per-view device coordinates, preview/cancel, replay, ABI, Canvas and Windows smoke; macOS fixed Tool strip plus anchored transient/pinnable option popover and mouse/tablet adapter with pressure/tilt fallback, exact issue-time editor target, ordered begin/sample/end/cancel lanes, pencil/brush/eraser/dust Core routes, Retina product stroke, no-op, invalid, cancel, stale, overflow and allocation-failure atomicity; macOS M8 adds the advanced effect and vector correction routes without duplicating paint semantics | Pencil is fixed at 1.0 document pixel; brush/eraser share the documented diameter range; a physical macOS tablet was not available for manual evidence |
| `PAINT-002` | Verified | Raster/vector capability-table and pixel/geometry goldens for line, two-stage curve, rectangle, ellipse, regular polygon, and click polyline; resolved canonical geometry v2, preview begin/update/commit/Cancel, constraints, Undo/Redo/replay/save-reopen, bounded ABI negatives, Windows staged gestures/renderer route, macOS M8 geometry options and exact-revision Canvas point resolution with preview Cancel/commit integration, ARM64 geometry confirmation, M15 production snap automatic coverage, and x64 user-visible snap/bypass/zoom confirmation | — |
| `PAINT-004` | Verified | Round/square and pressure footprints; fixed-point smoothing off/on and append batching equality; immutable exact native Binary/Grayscale/RGBA8/16 start-color predicate including alpha, detached same-color hits and selection clip; no-op/invalid/stale/overflow/cancel/Undo/Redo/replay/save-reopen; ABI v8 negatives, Windows Tool Options/Canvas smoke, macOS contextual options in the anchored transient/pinned popover plus exact-depth/selection/main-line integration tests, and x64 Release narrow two-color repair, shape/smoothing, Cancel and Undo manual verification | — |
| `FILL-001`, `FILL-002`, `FILL-003` | Verified | Connected/inclusion/closed-region goldens, tolerance/selection/gap/overflow/cancel/no-op/Undo, FFI, Windows target smoke, macOS anchored-popover option controls and exact-target connected/inclusion/closed Core routes, and non-mutating closed-fill drag preview | Gap close follows the documented deterministic native rule |
| `COLOR-REPLACE-001` | Verified | Exact native-depth raster and whole stable vector path/fill replacement; pen/rectangle/polyline/lasso and selection intersection; preview/Cancel, no-op/invalid/stale/overflow, Undo/Redo, replay/save-reopen; ABI negatives, Windows production gesture smoke, macOS M6 bounded four-region preview/apply/Cancel routing plus M8 vector selection/edit surfaces, and x64 Release range isolation/Cancel/Undo/whole-vector-object manual verification | — |
| `COLOR-001` | Verified | Exact RGBA8/16, RGB/HSV editing, eyedropper sources, main-line procedures, codecs, history/replay and Color-pane smoke; macOS converts the system color well through device-independent sRGB while retaining Core-owned exact-depth channels, routes eyedropper/locator edits through Core, and M11 keeps color-judging content in an opaque system well outside custom glass | Display conversion does not replace stored exact-depth color |
| `COLOR-002` | Verified | Independently persisted palette/chart/subpalette; legacy white/native-alpha checks; chart generation/comparison; codecs, history/replay, Windows production routes and ARM64 confirmation; macOS bounded palette/chart/color-check and Core-owned Subpalette/Reference routes now share the single M11 inspector without adding glass behind swatches, chart, or thumbnails | — |
| `COLOR-CHART-PREVIEW-001` | Verified | Same-base noncumulative RGBA8/16 extraction; alpha, maximum/quantization/overflow goldens; immutable UUID/revision-bound preview and task ownership; Cancel/invalid/lock/stale/cross-document/failure atomicity; exact-name and cursor retention; one-step Undo/Redo; current v26 save/reopen; Windows latest-wins Job Progress/comparison/Apply production smoke; macOS M6 owner-thread preview registry with replacement/apply/Cancel/close/shutdown exactly-once release; ARM64 user-visible generation/retry/Cancel/Apply/Undo/save-reopen confirmation | — |
| `COLOR-OUTPUT-QA-001` | Verified | Conservative BT.709 Y′CbCr rational half-up boundaries; exact RGBA8/16 raster/vector visible straight-alpha composite and transparent skip; sparse selection algebra, no-op/Cancel/stale/failure atomicity, Undo/Redo/replay/current-v26 save/reopen; caller-owned ABI negatives; versioned HKCU default-profile codec; Windows task/status production smoke; macOS M6 typed task route and invalid/stale/Cancel/failure atomicity tests; quick/full guard semantic gates and approved x64 wall-clock envelope; x64 Release profile/overlay/pixel-invariance/Undo user-visible confirmation | The sole profile is intentionally labelled as an inkpod conservative QA guard, not formal standards conformance |
| `SEL-001`, `SEL-002`, `SEL-003` | Verified | All shape/mode algebra, color/width operations, layer conversion, vector modes, preview/cancel, ABI and Windows gesture smoke; macOS M7 Selection menu/sidebar/options and exact issue-time Core algebra/layer routes | — |
| `SEL-004` | Verified | Normal/Tight/EnclosedInterior/Drawing/Boundary 8/16-bit goldens; edge/hole/tile/final-pixel bounds; aspect/center/rotation/45-degree and round/square pressure/screen trace; no-op/invalid/stale/Undo/Redo/replay/save-reopen; ABI v8 negatives, Windows production preview smoke, macOS M7 half-open gesture/options and product Canvas Undo/Redo, and x64 Release manual mask/geometry/modifier check | — |
| `CLIP-001` | Verified | Ordered multi-plane typed clipboard; exact RGBA8/16, origin and tree order; cross-plane vector fill topology; coordinate preservation, converted-plane transaction, external DIB, ownership, Cancel/Undo/replay and Windows grouped copy/paste smoke; macOS one-item private typed ticket plus PNG/TIFF, external PNG/TIFF straight-RGBA ingestion, compatible/converted/new-plane floating paste, outside-Canvas positioning, Cancel/stale and exactly-once replacement/close/shutdown release tests | Windows standard import accepts validated 24/32-bit DIB layouts; macOS standard import is PNG/TIFF |
| `XFORM-001`, `XFORM-002` | Verified | Destructive document transforms, frame/guide consistency, floating selection lifecycle, retry/cancel/Undo, FFI and Windows dialog/handle smoke; macOS M7 mirror route and floating transform sheet/Canvas handle/keyboard/accessibility alternatives | — |
| `XFORM-003` | Verified | Half-open five-anchor absolute X/Y contract; shared raster/vector scale→clockwise-rotate→position Core path; noncumulative preview/retry/Cancel; canonical-v3 replay and v26 save/reopen; ABI v10 short/unknown-anchor negatives; Windows dialog/handle/renderer production smoke; macOS M7 five-anchor/noncumulative/outside-Canvas/one-Undo integration and accessible handles; x64 Release 31/31 CTests and user-visible confirmation | — |
| `LT-001`, `LT-003` | Verified | Set/item administration plus previous/next/both-N registration; linear distance opacity, chronological z-order, UUID duplicate preservation, preview/Cancel/no-op/invalid/stale/overflow/failure atomicity, one-step Undo/Redo, replay/save-reopen; canonical-v2/current v26/epoch-23, caller-owned ABI negatives, Windows pane/production smoke, macOS M9 inspector/pin/bulk-registration/Metal snapshot routes, quick/full benchmark gates, and ARM64 user-visible order/opacity/Undo/save-reopen confirmation | — |
| `LT-002` | Verified | Alignment, move/sample/reload/edit-image swap, reference viewer, history isolation, ABI and target-aware Windows/macOS inspector smoke; macOS swap activates only an exact matching live Cell session | — |
| `SEQ-001` | Verified | Natural-order discovery, thumbnails, direct selection and navigation; Prompt/Autosave-before-switch setting; immutable source/target identity and revision; durable exact-native autosave association; staged restore with dirty/history/editor preservation; failure/stale/ABI negatives; Windows setting/owner/production smoke, macOS Core-exported/identified-imported sequence with exact live Cell UUID/revision and direct matching-tab activation, and ARM64 user-visible confirmation | — |
| `SEQ-ENDPOINT-001` | Verified | Closed Stop/Wrap policy; empty/single/stopped/advanced/wrapped Core plans; natural-order gap skipping; issue-time sequence/source/target identity and stale/failure atomicity; additive ABI v15 96-byte caller-owned plan with short/unknown/unsupported negatives; versioned HKCU and macOS UserDefaults codecs with Stop fallback; application-wide menu/shortcut/checked/status route; motion-loop separation; x64 production smoke and user-visible Stop/Wrap, checked-state persistence, restart, and dirty endpoint no-op confirmation | — |
| `SEQ-002` | Verified | Motion Core/ABI state and Windows FPS/loop/pause/step/shortcut smoke; macOS M9 motion controller keeps playback timing outside document mutation and issues exact-target first/last/previous/next routes | — |
| `SHORT-001` | Verified | ABI-v15 platform-neutral key/modifier records; 1–4-stroke pure resolve; prefix-free transaction, exact-conflict exchange, reset, protected standard shortcuts, 1.5-second timeout, persistence rollback, text/marked-IME guard, Windows adapter mapping, and macOS searchable Settings recorder/UI route; M5 adds protected Command-N and M7 adds protected Command-Z/Shift-Command-Z with deterministic valid-v2 migration | Palette `1`-`0` retains its documented no-match fallback; physical Japanese IME composition remains manual evidence |
| `FILTER-001`, `FILTER-002` | Verified | Exact 8/16-bit selection/alpha goldens, catalog, preview/cancel/Undo, shared Job Progress pane, ABI and Windows editor smoke; macOS M8 native Image menu/Filter inspector/sheet routes every catalog entry through Core tasks with invalid/stale/failure atomicity | Filter work runs on the Core worker and reports task progress |
| `FILTER-PREVIEW-001` | Verified | Same-base A→B Core acceptance; existing ABI v9 begin/update/apply/cancel ownership and negatives; 120ms dialog notification debounce; one-running/one-pending latest-wins queue with cooperative cancel and issue-time target generation; shared dialog/Job Progress reporting; Windows production smoke with four preview generations, Cancel, one Undo/Redo; macOS M8 MainActor debounce plus fixed-owner task registry, rapid Apply/Cancel/replacement/close ownership and launched native-sheet Cancel; ARM64 user-visible confirmation | — |
| `EFFECT-001` | Verified | Gradient/airbrush/boundary/blur/stamp acceptance, deterministic pressure/gesture/bounds, ABI and Windows smoke; macOS M8 typed native effect sheet and exact-target Core mutation routes | — |
| `ADJUST-001` | Verified | Non-destructive order/source/opacity/visibility/save-reopen, malformed metadata, alpha RGB preservation, ABI and Windows smoke; macOS M8 adjustment create/edit/toggle/reorder navigation and Core-ordered Metal LUT passes | — |
| `BATCH-001`, `BATCH-002`, `BATCH-003` | Verified | Current-version graph round-trip, complete operation catalog, dry-run/preview/shared Job Progress/cancel/failure/atomic output, FFI and Windows Batch-pane smoke; macOS M10 dedicated `WindowGroup` exposes all 12 operation kinds through 24 concrete typed add commands, ordered editing, file/folder/current inputs, output policies, preview/dry-run/current/all execution, determinate/indeterminate progress, cancel, persistent report, balanced folder leases, stale/collision/permission/save/decode/allocation negatives, and launched product UI coverage | Native `.inkpod` is the only output format; boundary airbrush begins with two colors |
| `BATCH-004` | Verified | Bounded multi-row seed/pair authoring; exact native-depth two-cell extraction with explicit one-to-many resolution; typed replacement/selection/main-line/color/native-file separation; immutable per-run configuration; `.inkbatch` v2 and current `.inkpod` v26/epoch-23 replay; FFI ownership/negative tests, Windows production smoke, x64 Release user-visible manual confirmation, and macOS M10 value-only operation projection, explicit ambiguity sheet, persisted configure-each-run input, immutable run clone, window/app-close cancellation and exactly-once graph/task/report/pair release | — |
| `VECTOR-001`, `VECTOR-002` | Verified | Geometry/render goldens, path editing, width/connect/select/vectorize/rasterize transactions, mixed-order zoom/save/replay, ABI and Windows Canvas smoke; macOS M8 typed geometry/eraser/selection/connect/width/vectorize/rasterize routes, value-only selection projection, Core-ordered Metal vector passes with alternate-rule stencil fill, and save/reopen integration | Rasterize preserves its source layer |

## Maintenance rule

Keep one current row for every `SPEC.md` requirement ID. Replace evidence and
known differences instead of appending milestone or dated verification logs.
Move exact samples to the relevant baseline document and current platform
results to [`implementation-status.md`](implementation-status.md).
