# Compatibility status

This document tracks only the current status, representative evidence, and
known difference for every requirement ID defined in
[`../SPEC.md`](../SPEC.md). Detailed behavior belongs to `SPEC.md`, ownership
and data flow to [`architecture.md`](architecture.md), exact format bytes to
[`file-format.md`](file-format.md), and current platform verification to
[`implementation-status.md`](implementation-status.md).

Status is one of `Not started`, `In progress`, `Experimental`, `Verified`, or
`Blocked`. A user-facing requirement is `Verified` only when its production UI
route and observable result are tested; infrastructure requirements may be
verified at their natural build, ABI, Core, format, or renderer boundary.
Older ABI/native/replay numbers inside a representative-evidence cell identify the
historical gate that first fixed that behavior; only rows explicitly labelled
exact-current/current-only define the present version contract.

## Requirement matrix

| Requirement | Status | Representative evidence | Known difference / next work |
| --- | --- | --- | --- |
| `ARCH-001`, `ARCH-002` | Verified | CMake/Cargo tracks all five crates; architecture guards cover dependency direction, OS imports, private backend isolation, declarative module roots and public integration boundaries | Rust domain crates remain platform-independent; only the private `inkpod-io` filesystem backend uses OS file APIs |
| `ABI-001`, `ABI-002` | Experimental | Exact-current ABI v25 removes layer-kind, Selection-plane, vanishing-point and adjustment-layer records/routes; exposes MainLine/Color/Raster image roles plus document-owned saved-selection operations. Application-owned I/O jobs, bounded spans, cancellation, recovery metadata, Batch v5 and standalone Subpalette ownership remain | ABI v1-v24 callers must rebuild. Rust/header/C11/C++20 and current Windows gates are rerun in this change; unrun platform/manual checks remain tracked under `IO-003` |
| `IO-001` | Experimental | Current-only native v31/runtime epoch 27; standard layers require one MainLine and one Color plane with optional Raster planes. DOCM is mandatory; current selection, ordered stable-ID saved masks and sparse fill protection are outside the image tree. Archive schema 7, metadata schema 8, document digest schema 12/domain 10 and snapshot-composite schema 5 are current; Cut schema 3/epoch 25 remains separate | Non-v31 files are rejected without migration; compression code 0 only; remaining platform/manual checks are tracked under `IO-003` |
| `IO-002` | Verified | PNG/TIFF/TGA/BMP 8/16-bit, alpha/DPI round-trips; all seven standard TGA 2.0 image types, true-color/index/palette depths, grayscale, uncompressed/RLE, four origins, old/new footer, Extension/Developer Areas, alpha semantics, color correction, postage stamp, scan-line table, malformed/mutation corpus and `tga_v2` fuzz target; shared Core/ABI import and Windows smoke | Explicit flat-raster export remains distinct from ordinary native-plus-companion pair save; product TGA export keeps the stable top-left 32-bit uncompressed default while alternate TGA storage is exposed by the typed Rust format API |
| `IO-003` | Experimental | One application-owned `inkpod-io` manager owns editable native/recovery, editable PNG/TIFF/TGA/BMP, sequences, Subpalette/Reference, Light Table add/reload, and Batch file/folder/preview I/O. Bounded workers, physical/path locks, cancellation and polling separate read completion from loaded-image counts. The shared LRU caps 10,000 images, 512 MiB encoded per file, 8 GiB encoded and 8 GiB decoded, including reservations and live leases. Ordinary save prepares, flushes and revalidates native plus same-stem raster outputs, then publishes path/savepoints after installation; partial failure retains recovery evidence. Automatic sequence failure preserves the successful single-image open. The statusbar displays localized job names, a native bar, selection and cancellation from bounded cached I/O progress. Request identity/generation and the exact issuing workspace remain fixed; UI refresh performs no Rust polling or file I/O and never substitutes read/loaded counts for work units. Decoded sequence-display allocations reserve at most eight sources/128 MiB per shared manager, including active, prepared and snapshot-retained payloads, within the existing 8 GiB decoded cap; accounting lasts until the final lease is released. The latest Rust workspace passes 698 tests across 22 nonempty suites, including 256 public Core contracts and 60 ABI tests, with one existing Release-only ignore | All 46 CTests pass on ARM64 Debug and x64 Release; each standalone Renderer test also passes five independent repetitions. Earlier 41/45-test and renderer-failure records remain historical baselines. Physical file-management flows remain pending. Pair save does not promise filesystem-level atomicity across two files. GUI icons, palette/chart, shortcut/settings, clipboard memory images, test fixtures and Cut Cell validation are outside this migration |
| `WIN-001` | In progress | Startup/shutdown; explicit registration of the standard, bar, tab, List-View/Header, Up-Down, Progress Bar and Hot Key Common Controls classes; Windows 11 system-dark title-bar opt-in for each workspace window; human-readable `%LOCALAPPDATA%\\inkpod\\Settings\\inkpod-settings.json` `general.uiLanguage` preference with invalid-file current-default fallback; first-preferred `ja` system resolution with non-Japanese/detection-failure English fallback; the Preferences language selector; one generated, typed Japanese/English catalog for product-owned text with exact format-signature parity; exact equality of the ja-JP/en-US resource-identifier sets, including all 313 localized menu command IDs, plus all 321 state-owned command IDs including the eight pane-local actions, with explicit exact-language loading and no Win32 fallback lookup; typed Tool labels shared by owner draw, tooltip, MSAA and UIA, with all 14 permanent Tool commands resolved through Windows-only `ToolIconId` values to a fixed Fluent UI System Icons mask atlas and compact localized text fallback; Layer/Plane visibility/editability state cells, its six common action buttons, and the Color/Locator/Sequence/Light Table document-pin actions use system-color, DPI-scaled Fluent icons while retaining their full localized accessible names; both full product smoke modes retrieve every real ToolTip value and compare it to the same label ID; Layer/Plane row role/format/visibility/editability and accessible rows are resolved before drawing; 56-DIP Layer rows use 48-by-36-DIP thumbnails and 48-DIP Plane rows use 32-by-32-DIP pre-resolved typed badges in an 8-point font, while adjacent detail and accessibility retain the full role/format; Layer/Plane owner-draw visibility/editability cells use centered 32-by-32-DIP buttons around unchanged 16-DIP icons with a 4-DIP gap and shared draw/hit-test geometry; the common target footer uses six 24-by-24-DIP Add/Copy/Delete/Move Up/Move Down/Properties icon buttons on one standard-width row, wraps only at narrower widths, and switches full localized Tooltip/MSAA/UIA action names with its Layer/Plane target; typed Color/Palette/Chart tabs; DockHost-owned inspector-header font matched at runtime to the pane-tab 9-point Segoe UI ClearType font and recreated for workspace DPI changes; idempotent Locator presentation suppressing unchanged target-label `WM_SETTEXT`, check-state updates, and neighborhood invalidation, while shorter replacement text receives one background-erasing synchronous redraw, with an actual-window unchanged-message/shortening-restoration regression and a static repaint-contract gate; language-neutral typed history kinds mapped exhaustively to catalog IDs; strict source gate rejecting Japanese bypasses, direct wide-string assignment to product dialog/effect presentation fields, language-branch/raw document captions, history string keys, restoration of the single-line ellipsized Plane badge, and restoration of measured status-cell sizing; no hook, partial, or hybrid translation; language-neutral generated document data and explicit user-text composition preserving edit/document-tab/target-document/Light-Table-set text, Japanese file names, and full paths verbatim; measured localized button widths with responsive action-row wrapping; Japanese/English 96/120/144/192-DPI-equivalent font layout tests plus full GUI checks for actual visible button bounds; full English and Japanese GUI smoke covering Tool and Layer/Plane owner draw, ToolTip/MSAA/UI Automation names, Saved/Modified, Locator/Sequence/Light-Table follow/pin/follow, zoom, Core/renderer/device-loss and workspace lifecycle; Help/About; owner-centered work-area-clamped modal dialogs; DPI/theme/layout; keyboard and accessibility coverage; offline Help `謝辞` identifies the locked BLAKE3/PNG/Fluent icon dependencies and full-notice authority through the embedded-help route with dependency-drift, extraction, menu/state and smoke coverage; `Inkpodウェブページ` opens the canonical HTTPS URL through the shell adapter; `設定ファイルを開く` materializes a missing current settings file without replacing invalid JSON and opens the exact fixed path through the Windows shell association; reproducible [release checklist](windows-release-checklist.md) | The latest ARM64 Debug and x64 Release runs each pass all 46 CTests, including full English/Japanese GUI smoke. Dark presentation is limited to the system title bar; high contrast, a physical 200% DPI desktop, complete screen-reader and Japanese IME work remains; the embedded offline Help body is Japanese-only |
| `WIN-002` | Verified | Multi-window registry, focus/routing, close/shutdown, sink lifetime, single-instance activation, malformed IPC, x64/ARM64 native smoke | — |
| `WORKSPACE-001` | In progress | DockHost retains the persistent fixed-width Tool strip, Color, Layer/Plane and auxiliary surfaces, while Tool Options remains a bounded owned popup. The Job Progress pane and its Window-menu command are removed; transient statusbar progress is scoped to each workspace and excluded from layout persistence. The singleton Tool strip has no zone-extent splitter, cannot float or AutoHide, and is controlled only by its direct visibility toggle. The right zone is a bounded dynamic list of stable-ID tabs: every tab is nonempty, pane membership is unique, panes and tabs reorder, panes move between tabs, insufficient minimum height creates another tab, and deleting the selected last pane chooses previous/next/first deterministically. The first pane supplies the label; the full pane list supplies the accessible description; narrow-width suppression is transient. Geometry-only Splitter relayout preserves tab items and pane data, skips unchanged child placement, and synchronously repaints width-dependent Layer/Plane owner-draw rows after geometry changes. Color performs all child placement without interim redraw, anchors its Common Controls tab behind the active page, raises only the visible page controls, then synchronously completes a bounded per-control repaint; owner-drawn color surfaces avoid erase while standard controls erase before repaint. Width changes synchronously erase the bounded union of the Color target and Pin rows, and the final product-smoke probe proves that a sub-gap Pin move repaints the vacated frame after a real DockHost layout. Its combined swatch, main-line and drawing-color labels, and HSV/opacity picker reproduce the corresponding live themed-tab pixels instead of using fixed system-face fills; antialiased edges compose over the captured surface, and the picker cache is invalidated after tab/theme/system-color changes. Right-tab stack-boundary changes additionally erase and synchronously repaint the bounded strip between the old and new boundary, including intersecting pane headers and children. Locator and Light Table perform their `WM_SIZE` child placement without interim redraw and then synchronously erase and repaint the complete clipped pane subtree; actual-window height-resize probes cover their bottom-anchored controls and require no deferred pane or child update region. Color has a 300-DIP minimum including its Dock header and a 360-DIP preferred floating height; mouse and keyboard boundary changes compute both adjacent limits from the descriptors and current pair extent, stop with stable weights at either minimum, and normalize legacy out-of-range ratios toward valid movement. The x64 Release workspace-layout test covers 96/192-DPI scaling, both limits, and repeated `NoOp`; the product smoke resizes the actual Color content to its minimum and proves its tabs, swatches, picker, numeric field, and Apply button all paint with nonempty bounds and no deferred update region. It separately renders the tab, combined swatch, both owner-drawn labels, and picker, and requires exact background-pixel equality at corresponding coordinates. It also proves zero Tool-tab deletion and Color/Layer list reset messages, no deferred Layer-list paint after right-zone resize, and complete-or-minimum-stopped stack-boundary painting with physical-position restoration. The current settings JSON round-trips readable tab keys/order/membership/selection/next-key and performs no registry or old-file migration. Five presets contain no empty or duplicate membership. Window-menu Locator/Sequence/LightTable/Subpalette/Reference/Batch commands are direct checked toggles; Color retains its pane-local pin/follow action while Batch exposes no pin row. Static model/persistence contracts require the retired pane's absence, cached work counters and window-specific statusbar wiring. Final ARM64 Debug/x64 Release English/Japanese product smokes cover these routes, including cached progress before Core activation and split-failure cleanup/restoration at the real snapshot-sink capacity limit. | Reference Check AutoHide edge buttons are not keyboard-reachable with F6/Tab |
| `WORKSPACE-002` | Verified | Issue-time command context, follow/pin/job policy, stale rejection, cross-window drag/move/copy and target-aware pane smoke | — |
| `SESSION-001` | Verified | File identity, multi-session/view isolation, savepoints, close scopes, Save As conflicts, autosave/recovery and malformed-open contracts remain. File requests capture the issue-time owner and generation; final installation is fenced, wrong-owner results cannot finalize, and public Core clones have independent file authority | Core mutation and publication remain on one observable single-writer `CoreHost` lane; filesystem reads/writes run in the separate bounded parallel Rust I/O service; current migration validation is tracked by `IO-003` |
| `SAFE-001` | Verified | Corruption/mutation corpus, bounded V14/checkpoint/asset/edit-target/selection-option/color-replace rejection, staged publication, failure atomicity, fuzz-target declarations | Coverage-guided fuzz execution is not part of the latest local run |
| `PERF-001` | Experimental | All nine unchanged Core workflows retain their counter/checksum gates and canonical `revision-max` expression. The v31/catalog-v5 InkScript quick workload keeps every non-byte counter and the 64–107 ms envelope; current bytes are 23,872 input, 35,808 reads and 90,688 installed, with checksum `ae0d04681b2f5a63`. One current Release run completed in 90.7331 ms | Approved historical ARM64/x64 and foreground sequence measurements remain recorded in the benchmark/sequence documents. A full current multi-process timing series and unrun Windows configurations remain; background latency is not guaranteed |
| `PKG-001` | Verified | Static-CRT x64/ARM64 builds, dependency checks, exact four-file ZIP, unsigned MSIX and extracted ABI smoke; the selected Fluent SVG provenance/hashes/MIT notice are fixed and the generated atlas is embedded in the packaged executable, so normal and distribution builds remain offline | ZIP has no file association; administrator MSIX install/uninstall is optional validation |
| `PORT-001` | Verified | Architecture guards prohibit Windows dependencies in Core/image/format and confine OS file identity/replace operations to the private `inkpod-io` backend. Shared Linux/macOS replay/workspace checks remain in CI; Fluent asset names and icon IDs stay in Windows sources | Non-Windows execution was not rerun locally for this migration. Future sandboxed frontends still need platform file-authority adapters; the shared I/O manager is not an OS permission bypass |
| `DOC-001` | Verified | Metadata, paper/frame/DPI equality, mixed-paper alignment, native round-trip and Windows dialog smoke | — |
| `CELL-001` | Verified | Public image/frame-mode table, every supported initial MainLine/Color format and RGBA8/16 save/reopen, invalid/overflow/UUID failure atomicity; owned-plan ABI negatives; one-dialog Core-plan summary; three-session Windows production smoke; x64 Release image/frame-mode, 16-bit three-Cell, properties and Cancel manual check | — |
| `CUT-001` | Verified | Individual same-directory Cell-reference descriptor; independent Cut metadata/defaults/history/savepoint; persistent `(CellId, document UUID)` member identity with document-local numeric Cell IDs; default copy only at Cell creation; no-op/invalid/Cancel/stale/overflow/failure/Undo/Redo/save/reopen/recovery and identity/path rejection tests; ABI v17 ownership/negative coverage; issue-time workspace/session/generation and Cut-revision rejection in Windows New Cut/Properties/Sequence-pane/open/save/history production smoke; x64 Release user-visible creation, existing-Cell independence, Undo/Redo and save/reopen confirmation | — |
| `SEQ-STRUCT-001` | Verified | Bounded ordered insert/remove/move-before/move-after/range-renumber transaction; stable pair identity; separated asset table and ordered membership history; one-step Cut Undo/Redo; historical v27 save/reopen and exact-v26 rejection; caller-owned strided ABI with per-operation failure index; Sequence-pane buttons, drag, keyboard and renumber dialog production smoke with stale active-Cell retarget rejection; x64 Release five-Cell reorder/renumber/remove/Undo/save/reopen confirmation | — |
| `DOC-002` | Verified | Stable-ID typed topology; grouped tree operations, validation, save/reopen, ABI spans, Windows marker/accessibility/status/menu/smoke, and x64 Release confirmation | — |
| `DOC-003` | Verified | Tree-ordered bounded multi-target EditorState; grouped duplicate/delete/visibility/editability/compatible merge/convert capability matrix; no-op/invalid/stale/Undo/replay/save-reopen; ABI spans; Windows marker/accessibility/status/menu/smoke; x64 Release grouped main-line/color paste, one-step Undo and active-plane-only brush manual check | — |
| `SHOOTING-FRAME-001` | Verified | Stable-ID optional angled frame; deterministic milli-pixel/binary-turn geometry and five anchors; create/update/delete and preview/Cancel; no-op/invalid/stale/overflow/nonuniform-resample rejection atomicity; Undo/Redo/replay and historical v27 save/reopen; ABI v17 ownership/negative cases; Canvas properties/handles/renderer, explicit instruction-raster production smoke, and x64 Release user-visible properties/handles, Cancel, save/reopen, and normal/instruction export confirmation | — |
| `RENDER-001` | Verified | Immutable raster render spans plus the independent shooting-frame overlay; standard-layer order, visibility, opacity and alpha are shared by Canvas, thumbnail and flatten. Snapshot-composite schema 5 contains no adjustment LUT or radial-guide record; device-loss rebuild uses the retained snapshot | — |
| `VIEW-001`, `VIEW-002` | Verified | Zoom/box/fit/1:1/pan/flip, ruler/guide/grid/snap/transparent-view Core, FFI, coordinate and Windows gesture/render smoke | The main-window zoom slider is intentionally absent |
| `SNAP-001` | Verified | View-targeted device/document point resolution; explicit-guide/grid precedence, inclusive threshold and deterministic tie-break; Ctrl bypass; bounds/extreme transform/flip/DPI/stale tests; additive ABI negatives; Windows menu checked-state, line geometry/digest, off/bypass and Undo/Redo production smoke | — |
| `VIEW-003` | Verified | Bounded locator sampling/neighborhood, sparse allocated-tile selection bounds with document-revision caching, DPI/flip/half-open coordinates, active-stroke/filter-preview display priority, committed eyedropper isolation, one-running-plus-latest async coalescing with processed-intermediate presentation, Begin/Append preview plus End/Cancel resynchronization without revision/history publication, FFI buffers, target/pin/stale handling, and native continuous-input pre-mouse-up/capture-cancel pane smoke | — |
| `VIEW-004` | Verified | Stable view/session caption lookup preserves untitled/named tab labels and model order while selecting tabs. Closing the final canvas tab leaves an empty workspace with New/Open available. Owner/CoreHost/command regressions and complete ARM64 Debug/x64 Release English/Japanese product smokes cover these routes, including failed New/Open/Save, cancellation, pinned/following pane captions, another workspace, and split-failure restoration. Retained baseline: multi-view revision/history, per-view device-coordinate stroke placement, split/group/window move/copy, same/different-session isolation, active-stroke and native lifecycle smoke, DPI-scaled 9-point Segoe UI document-tab font for initial and split groups, bounded per-view Common Controls close buttons with system-color DPI-scaled icons, exact stable-view `Close View` routing, inactive-tab close with active-view preservation, and drag/close separation | See [SPEC.md](../SPEC.md) sections 2 and 4; physical/platform coverage gaps remain under `WIN-001` |
| `VIEW-005` | Verified | Per-view typed AA/centerline/endpoint flags; explicit stable endpoint topology; exact/near/closed/connected Core contracts; current-version save/reopen and noncurrent rejection; bounded borrowed ABI records; Windows menu/shortcut/checked state, Direct2D pixel golden, device-loss smoke, and x64 Release user-visible confirmation | — |
| `HIST-001` | Verified | Journal state-machine, full/checkpoint replay equivalence, inactive branches/assets, savepoints, cache rebuild, compaction and Windows Undo/Redo/dirty smoke | Compaction intentionally creates a new Genesis with empty history after explicit confirmation |
| `HIST-002` | Verified | Commit-event ordering including inactive branches; deterministic typed primitive/argument formatting; point-in-time bounded replay steps with progress/cancel and queue-tail re-entry; direct maximum-64×64 post-commit composition; query/cancel/no-document stability; Rust-owned ABI builder/snapshot ownership, size-query and negative cases; nonblocking published menu/status state; dynamic open-native-session submenu, one modeless owner-data list per document, loading row, shared native statusbar name/bar/selector/cancel, bounded generation-tagged task entries for simultaneous document dialogs, cooperative visible-row prefetch, dialog reuse/close and Windows production smoke | The dialog snapshots the in-memory journal when opened; reopen it to reflect later edits |
| `PAINT-001`, `PAINT-003` | Verified | Stable-target stroke/cleanup/width primitives; exact start-pixel Pencil auto-erase for Binary and imported straight-RGBA MainLine; pressure, extreme/per-view device coordinates, preview/cancel, Undo/Redo, replay, ABI, Canvas and Windows product smoke | Pencil is fixed at 1.0 document pixel; brush/eraser share the documented diameter range |
| `PAINT-002` | Verified | Raster pixel/geometry goldens for line, two-stage curve, rectangle, ellipse, regular polygon, and click polyline; resolved canonical geometry v2, preview begin/update/commit/Cancel, constraints, Undo/Redo/replay/save-reopen, bounded ABI negatives, Windows staged gestures/renderer route, ARM64 geometry confirmation, M15 production snap automatic coverage, and x64 user-visible snap/bypass/zoom confirmation | — |
| `PAINT-004` | Verified | Round/square and pressure footprints; fixed-point smoothing off/on and append batching equality; immutable exact native Binary/Grayscale/RGBA8/16 start-color predicate including alpha, detached same-color hits and selection clip; no-op/invalid/stale/overflow/cancel/Undo/Redo/replay/save-reopen; ABI v8 negatives, Windows Tool Options/Canvas smoke, and x64 Release narrow two-color repair, shape/smoothing, Cancel and Undo manual verification | — |
| `FILL-001`, `FILL-002`, `FILL-003` | Verified | Connected/inclusion/closed-region goldens, tolerance/selection/gap/overflow/cancel/no-op/Undo, FFI and Windows target smoke. Common-raster open retains exact RGBA8/16 source pixels in the protected main-line plane and fills the separate color plane. A successful fill selects Color; an explicit user MainLine choice exits Fill while automatic pane refresh keeps an intentional Fill active. Synthetic four-codec tests and the supplied 1754x1240 TGA verify five closed fills, edge overflow abort, unchanged main-line checksum, Undo/Redo and native save/reopen. See [diagnosis](raster-import-fill-diagnosis.md). | Grayscale coverage remains display-only under the MainLine pixel-format contract; imported RGBA main-line pixels use exact nonwhite/positive-alpha boundaries. |
| `FILL-004` | Verified | Dedicated sparse fill-protection tiles, hard-boundary behavior in seed/closed/extension fill, wall-tile proportional allocation, replacement semantics, Undo/Redo/replay/save-reopen and snapshot/cache revision tests; complete Rust suite and x64 Debug/Release plus ARM64 Debug product smokes | — |
| `COLOR-REPLACE-001` | Verified | Exact native-depth raster replacement; pen/rectangle/polyline/lasso and selection intersection; preview/Cancel, no-op/invalid/stale/overflow, Undo/Redo, replay/save-reopen; ABI negatives, Windows production gesture smoke, and x64 Release range isolation/Cancel/Undo manual verification | — |
| `COLOR-001` | Verified | Independent per-command RGBA8/16 state and restoration, RGB/HSV editing, eyedropper sources, main-line procedures, codecs, history/replay and Color-pane smoke. Yellow Fill → explicit MainLine → black Pencil → Fill round trips both command colors through the Core-owned editor state and Windows pane | Display conversion does not replace stored exact-depth color |
| `COLOR-002` | Verified | Independently persisted palette/chart state; legacy white/native-alpha checks; chart generation/comparison; codecs, history/replay, Windows production routes and ARM64 user-visible confirmation. Current-color and Subpalette registration query the issuing document's palette and reuse the first exact native-depth straight-RGBA match across all groups, including a full palette. Duplicate registration selects the existing row without changing document revision/history/dirty; distinct alpha/depth/component values still append through the existing Rust primitive. `RunPaletteRegistrationSmoke` covers both buttons, group/row selection, repeated registration, exact-depth distinctions, Undo/Redo, and capacity failure atomicity | Existing and file-loaded duplicates are retained. External reference-image behavior is owned separately by `SUBPALETTE-001` |
| `SUBPALETTE-001` | Experimental | ABI v25 retains a standalone read-only Rust catalog/view with no hidden editable Core, Genesis or history. Path-only jobs use the shared I/O manager; staged replacement preserves the old catalog on failure. Cached navigation retains exact-depth sampling and display leases; hidden/occluded surfaces defer presentation without discarding a prepared catalog | Interactive file-dialog and unrun platform checks remain; see [prior correction evidence](subpalette-first-load-diagnosis.md) |
| `COLOR-CHART-PREVIEW-001` | Verified | Same-base noncumulative RGBA8/16 extraction; alpha, maximum/quantization/overflow goldens; immutable UUID/revision-bound preview and task ownership; Cancel/invalid/lock/stale/cross-document/failure atomicity; exact-name and cursor retention; one-step Undo/Redo; historical v27 save/reopen; Windows latest-wins statusbar-progress/comparison/Apply production smoke; ARM64 user-visible generation/retry/Cancel/Apply/Undo/save-reopen confirmation | — |
| `COLOR-OUTPUT-QA-001` | Verified | Conservative BT.709 Y′CbCr rational half-up boundaries; exact RGBA8/16 raster visible straight-alpha composite and transparent skip; sparse selection algebra, no-op/Cancel/stale/failure atomicity, Undo/Redo/replay and historical v27 save/reopen; caller-owned ABI negatives; human-readable settings-JSON default-profile field; Windows task/status production smoke; quick/full guard semantic gates and approved x64 wall-clock envelope; x64 Release profile/overlay/pixel-invariance/Undo user-visible confirmation | The sole profile is intentionally labelled as an inkpod conservative QA guard, not formal standards conformance |
| `SEL-001`, `SEL-002`, `SEL-003` | Verified | Raster shape/mode algebra, color/width operations and preview/cancel remain. Current selection is document-owned; stable-ID named masks support save, replace/add/subtract, rename, delete, Undo/Redo, geometry transforms, native round-trip, ABI and Windows commands without a Selection layer/plane | — |
| `SEL-004` | Verified | Normal/Tight/EnclosedInterior/Drawing/Boundary 8/16-bit goldens; edge/hole/tile/final-pixel bounds; aspect/center/rotation/45-degree and round/square pressure/screen trace; no-op/invalid/stale/Undo/Redo/replay/save-reopen; ABI v8 negatives, Windows production preview smoke, and x64 Release manual mask/geometry/modifier check | — |
| `CLIP-001` | Verified | Ordered multi-plane raster clipboard; exact RGBA8/16, origin and tree order; coordinate preservation, converted-plane transaction, external DIB, ownership, Cancel/Undo/replay and Windows grouped copy/paste smoke | Standard import accepts validated 24/32-bit DIB layouts |
| `XFORM-001`, `XFORM-002` | Verified | Destructive document transforms, frame/guide consistency, floating selection lifecycle, retry/cancel/Undo, FFI and Windows dialog/handle smoke | — |
| `XFORM-003` | Verified | Half-open five-anchor absolute X/Y contract; raster scale→clockwise-rotate→position Core path; noncumulative preview/retry/Cancel; canonical-v3 replay and historical v27 save/reopen; ABI negatives; Windows dialog/handle/renderer production smoke; x64 Release 31/31 CTests and user-visible five-anchor dialog/handle, Cancel and Undo confirmation | — |
| `LT-001`, `LT-003` | Verified | Set/item administration plus previous/next/both-N registration; linear distance opacity, chronological z-order, UUID duplicate preservation, preview/Cancel/no-op/invalid/stale/overflow/failure atomicity, one-step Undo/Redo, replay/save-reopen; historical canonical-v2/v27/epoch-24, caller-owned ABI negatives, Windows pane/production smoke, quick/full benchmark gates, and ARM64 user-visible order/opacity/Undo/save-reopen confirmation | — |
| `LT-002` | Verified | Alignment, move/sample/reload/edit-image swap, reference viewer, history isolation, ABI and target-aware Windows smoke | — |
| `SEQ-001` | Verified | A single horizontal thumbnail row scrolls left/right, supports Left/Right keys and a closeable pane tab, and removes the separate Previous/Next buttons. Dock minimum/preferred heights are 168/184 DIP. Fresh PNG/TGA/TIFF/BMP cells establish clean document and editor baselines; selecting them writes no `.inkpod` file and grants no ordinary-save path authority. Actual edits become dirty and Undo can return to the clean baseline. Core/FFI regressions retain no-op/bind, delayed discovery, discard, recovery, history and save-authority contracts. Natural-order discovery, thumbnails, Prompt/Autosave-before-switch, immutable source/target identity and staged restore remain. Warm pristine-source revisits now reuse immutable CPU/GPU display payloads under separate eight-source/128 MiB caps, exact raster checksums and thumbnails. Queued clean replacement preserves directional input and same-size view state; unchanged-catalog selection preserves the thumbnail list, focus and scroll. Presentation epochs fence edits until the selected document has been presented. See [SPEC.md](../SPEC.md) section 4 and [the performance record](sequence-switch-performance.md) | Five independent foreground processes (640 warm TGA switches) meet the two-refresh target; ARM64 Debug and x64 Release each pass all 46 CTests. Initial/cache-miss, edited/recovered and background latency is not covered by the warm result. Whole-sequence editable-document residency remains out of scope |
| `SEQ-ENDPOINT-001` | Verified | Closed Stop/Wrap policy; empty/single/stopped/advanced/wrapped Core plans; natural-order gap skipping; issue-time sequence/source/target identity and stale/failure atomicity; ABI v17 caller-owned plan with short/unknown/unsupported negatives; human-readable settings-JSON `animation.sequenceEndpoint` field with Stop fallback; application-wide menu/shortcut/checked/status route; motion-loop separation; x64 production smoke and user-visible Stop/Wrap, checked-state persistence, restart, and dirty endpoint no-op confirmation | — |
| `SEQ-002` | Verified | Motion Core/ABI state and Windows FPS/loop/pause/step/shortcut smoke | — |
| `PREF-001` | Verified | Settings remain exact-current v3 JSON and reject older versions without migration; native v31/replay 27 and ABI v25 are current. Existing owner-centered Preferences, persisted application/workspace fields, DPI/theme/localization and atomic settings behavior are unchanged by the document-model simplification | Language changes take effect on the next launch; physical high-contrast, 200%-DPI, screen-reader, and IME confirmation remains under `WIN-001` |
| `SHORT-001` | Verified | All 321 actual command-state entries appear in the grouped editor with stable command keys, primary/secondary assignments, command/key/context filters, changed/unassigned/conflict categories, record/clear/reset, conflict navigation/clear/swap, dedicated and width-validated `None`/`Ctrl`/`Shift`/`Alt`/`Win` modifier layers, and clickable/drag-target physical-key visualization. Built-in completeness and collision freedom, editable-profile unassignment, exact-conflict staging with disabled Apply/OK, prefix rejection, action capability validation, Global/Canvas/Timeline/Pane resolution, logical/physical matching, Auto/JIS 109/US ANSI 104 layouts, exact-current v3 bounded human-readable `.inkshortcuts` JSON atomic import/export and settings-JSON persistence with rejection of older versions (v1/v2) and no legacy migration, text-focus guard, sequence timeout, Hold release restoration, and Toggle repeat suppression have model, Windows, localization, and product-smoke coverage | Palette `1`-`0` retains its documented no-match fallback; Windows-reserved combinations are shown/configurable when delivered to the app, but no global hook is installed |
| `FILTER-001`, `FILTER-002` | Verified | Exact 8/16-bit selection/alpha goldens, catalog, preview/cancel/Undo, shared native statusbar name/progress bar/selector/cancel, ABI and Windows editor smoke | Filter work runs on the Core worker and reports task progress |
| `FILTER-PREVIEW-001` | Verified | Same-base A→B Core acceptance; existing ABI v9 begin/update/apply/cancel ownership and negatives; 120ms dialog notification debounce; one-running/one-pending latest-wins queue with cooperative cancel and issue-time target generation; shared dialog/statusbar job reporting; Windows production smoke with four preview generations, Cancel, one Undo/Redo; ARM64 user-visible confirmation | — |
| `EFFECT-001` | Verified | Gradient/airbrush/boundary/blur/stamp acceptance, deterministic pressure/gesture/bounds, ABI and Windows smoke | — |
| `BATCH-001`, `BATCH-002`, `BATCH-003` | Experimental | Exact-current `.inkbatch` v5 retains the fixed Input → ordered operations → Output graph, natural order/range/dedup, bounded collision preflight and isolated contact-sheet preview. File/folder work uses the shared Rust manager through ABI v25. Issue-time active-document state, cancellation, one-Undo publication and staged new-tab results remain | Physical screen-reader/high-contrast checks and unrun platform/configuration checks remain |
| `BATCH-004` | Experimental | Public authoring remains Color Replace, Move to Color Plane, Masking and Erase. Semantic targets are Color or Raster plane roles; layer-kind selectors are removed. Fixed-ID targets remain supported, overlaps are deduplicated, and replacements commit through private `ApplyBatchOperations/canonical-v3` as one Undo unit. `.inkbatch` v5/operation v4 and ABI v25 round-trip the closed selector set | Current Windows product gates are rerun in this change; physical accessibility checks remain |
| `SCRIPT-001` | In progress | Exact-current registry schema/language/file v2, catalog/owner manifest v5, replay epoch 27, native v31 and ABI v25 are aligned. The public catalog exposes exactly 73 commands and rejects older resources; private Batch v5 orchestration is excluded | Source lifecycle/editor, product `.inkscript` acceptance and the M36 full performance gate remain pending |
| `SCRIPT-002` | In progress | Owner manifest v5 is a bijection over all 73 exposed commands and surviving replayable primitive owners. Removed layer/selection/vanishing/adjustment assignments remain tombstones; session/query/view/preview and private Batch v5 orchestration stay excluded | A future public InkScript connection requires an explicit catalog/file-version decision; no `.inkscript` product route was added |
| `SCRIPT-003` | In progress | The least-privilege Windows adapter and bounded engine route retain ordered authority, nonblocking continuations, pointer-free results and owner-thread cancellation/release. Real output follows native v31/epoch-27 save/reopen, Undo/Redo, cache-free replay, ID high-watermark and both savepoints | Product `.inkscript` acceptance remains absent; Batch v5 uses its own closed typed graph and staged-result ABI |
| `SCRIPT-004` | In progress | M05B provides typed closed-fragment dependency rules. User-confirmed M24 exports one/linear active-or-inactive journal selections from canonical authority with exact parent assertions, typed result references, strict selectors, portability metadata, resource/cancel atomicity and cache-free replay. User-confirmed M25 exposes that exporter through a bounded event span and generation-bound opaque fragment with two-stage canonical UTF-8 copy; M26 changes no exporter semantics; visualization summaries/thumbnails remain excluded | Destination rebind/paste transaction, Batch/History clipboard and source-preserving structured editor remain pending |
| `SCRIPT-005` | In progress | Batch v5 and the 73-command InkScript surface share exact-current v31/epoch-27 primitive semantics while remaining separate product contracts. M13's workload, non-byte counters, sample policy and 64–107 ms x64 envelope are unchanged | The Release-only quick semantic gate and full M36/product route status are reported in implementation status; `.inkbatch` v5 remains independent |

For `WORKSPACE-001`, `VIEW-004` and `SEQ-001`, document, right-group and pane-tab
close icons now use one Windows painter while retaining their standard buttons,
accessible names, hit bounds and distinct stable-target close routes. The existing
Sequence and document-tab product smokes compare exact pixels against the
right-group button for normal/pressed/disabled/focused/pressed-focused states,
with and without hover, at the same device-pixel bounds. The 192-DPI regression
fails with the former Sequence caption frame and passes with the shared borderless
glyph. All 46 x64 Release CTests, including English/Japanese product smokes, pass;
ARM64 Debug's four targeted tab/dock-boundary, owner-model and Japanese
product-smoke tests also pass. A normal Japanese ARM64 window confirms consistent icons and pane-only
Sequence close. The full physical DPI/high-contrast/screen-reader gaps remain.

The current `WORKSPACE-001` Color-resize evidence additionally captures the old and
new target-field/Pin-button bounds, clips out their current child windows, and
synchronously repaints the exposed parent area before repainting the children. A
focused smoke contract moves Pin by less than the target-to-Pin gap and requires
that parent-background erase, preventing the old themed button frame from remaining.

The `PAINT-001`/`PAINT-003` Canvas stroke smoke derives cancellation and color
samples from the current presented paper bounds in client device pixels. Its
fixed viewport/zoom/pan fixture keeps the whole paper visible beyond the former
x=80..270 inputs, so those old margin-only inputs reproduce failure `42` without
relying on the runner's desktop size or DPI. Cancellation must first produce a
real captured preview and then preserve both plane checksums, revision and flags;
the color stroke must commit exactly one revision, change color and protect the
main line. The two-stroke metric, Undo/Redo and save/reopen checks remain intact.
Color-stroke failures include input/paper/client coordinates, DPI, tool/plane,
revision and both checksums. Product input, coordinate conversion and stroke
semantics are unchanged.

The current `WORKSPACE-001` right-pane evidence additionally covers thresholded
same-strip drag reordering of stable-ID top-level tabs, a DPI-scaled owner-drawn
close icon that atomically hides every member pane and removes the addressed tab,
and top-level labels/descriptions resolved from the existing localized pane
catalog at the Windows presentation boundary. Color/Palette/Chart use semantic
IDs and support reorder-only drag within their existing Common Controls tab;
the active page and child windows remain loaded, and outside/cancel paths are
nonmutating. Splitter focus gain and loss synchronously repaint the rule so the
highlight cannot remain after focus moves elsewhere. Static DockHost/model tests,
the workspace-layout executable, localization/catalog checks, and English and
Japanese ARM64 product smokes cover these paths. The Color inner-tab order is
session-local and is intentionally absent from the workspace settings JSON and `.inkpod`.

The Locator setup for that drag/close smoke accepts either an explicit move to
a new tab or `NoOp` when height-aware restoration already created its singleton.
Both paths still require exactly one additional tab, Locator-only membership,
matching model/control selection and visible dock state before drag/close.
The workspace-layout regression covers exact-fit and one-pixel-short heights
at 96/120/144/192 DPI from both combined and split Color/Layer tabs, including
stable IDs, selection and complete tab contents across a repeated singleton
move. Setup failures report the stage/result, DPI, initial right-zone height
and bounded tab membership; product placement rules are unchanged.

The current product catalog contains 313 localized menu command IDs and 321
state-owned command IDs. Job Progress has no pane or Window-menu command;
language and shortcut reset are owned by Preferences. Batch has no pane-header
document-target label or pin action, while issue-time session/generation capture
remains unchanged. These current counts are used by the menu, command-state and
shortcut catalogs.

The current `SUBPALETTE-001` evidence uses the standalone catalog. Subpalette
is application-scoped per workspace, has no document target or pin/follow row,
and uses icon actions for opening files/folders, previous, next, fit, and
one-to-one plus a sampled-color registration tile instead. The seven actions
share a compact standard-width row, wrap only when necessary, and expose
localized operation-name tooltips.

## Maintenance rule

Keep one current row for every `SPEC.md` requirement ID. Replace evidence and
known differences instead of appending milestone or dated verification logs.
Move exact samples to the relevant baseline document and current platform
results to [`implementation-status.md`](implementation-status.md).
